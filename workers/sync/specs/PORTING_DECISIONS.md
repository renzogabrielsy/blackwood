# Porting Decisions — Binding Rulings for All Wave-3 Porters

_2026-07-04, orchestrator rulings on the ambiguities the spec pass flagged. These are BINDING for the TS port. Governing principle: **classify-parity is bug-for-bug against the Python oracle; crash bugs and DB-constraint violations get fixed with a documented deviation.** Renzo may veto any of these; until then they stand._

| # | Ambiguity (see specs for detail) | Ruling | Why |
|---|---|---|---|
| 1 | `_lab_diff_is_immaterial` code contradicts its own docstring (`mc 11.5 vs 11` is MATERIAL in code) | **Port the CODE behavior** (material). Fix only the comment. Parity = what the oracle *does*, not what it *says*. | Golden-master discipline; changing materiality silently changes what gets auto-written. |
| 2 | `sync_gsheet._apply_from_compact` returns bare int `1` on the >50-NEW / confidence gates → caller crash under the contract CLI | **Fix properly in TS** (return a real gate-failure envelope; nothing applied). This is a live crash bug, not behavior. | A port that faithfully crashes is not parity. DEVIATION-LOGGED. |
| 3 | L-018 gap is live: gsheet apply honors only `skip`, never `decision`, on changed rows (once corrupted a real row) | **Fix in TS**: honor `decision` per the ledger's intent. DEVIATION-LOGGED; parity harness must carry an explicit expected-difference entry for this case. | The ledger rule is the contract; the Python never caught up. Porting the corruption path forward is indefensible. |
| 4 | gsheet RC IN extractor computes `true_weight_kg`/`deduction_note` but apply never writes them (email pipeline does) | **TS writes them** (align to the email pipeline + L-021 intent). DEVIATION-LOGGED. | Two pipelines writing different column sets for the same row type is the bug, not the feature. |
| 5 | `dt_mins >= 60` split documented in ledger but never implemented → DB CHECK violation on any ≥60-min downtime day | **Implement the split in TS** (hrs += mins//60, mins %= 60). DEVIATION-LOGGED. | Latent crash against a DB constraint; the ledger already defines the correct behavior. |
| 6 | `norm_int` diverges between pipelines (truncate in classify_deliveries vs round in classify_gsheet) | **Preserve each pipeline's own behavior** — port both variants as-is, named `normIntTruncate` / `normIntRound`, used exactly where the Python used each. Unification is post-M6 work. | Parity first; silent unification would shift natural-key matching. |
| 7 | Judgment L-rules (L-002/003/010/011/012/013/024) — codify or keep human? | **Keep human-adjudicated** (FLAGGED → held). Do NOT codify in this migration. | Matches the held-row philosophy and the migration's no-feature-additions scope. |
| 8 | L-009/L-032 audit RPC workaround — replicate or re-architect? | **Replicate as-is** (`write_ingestion_audit` / `stamp_ingestion_audit` via db.ts). They are live, verified, locked down. | Not this migration's problem to redesign. |

## Deviation log discipline
Rulings #2–#5 make the TS intentionally differ from the oracle. The parity harness (Wave 2) must model these as **expected differences** (a small `expected-deviations.json` keyed by rule id + case), so parity stays a hard gate everywhere else. Any parity diff NOT covered by that file is a porter bug, full stop.

## Post-port business-rule changes (NOT deviations — both engines changed in lockstep)
_These are real behavior changes applied IDENTICALLY to the Python oracle AND the TS port, so parity stays green with NO `expected-deviations.json` entry. Distinct from rulings #2–#5, which make TS diverge FROM the oracle. When you change a rule for real, change BOTH sides and rebuild the oracle (`npm run build:oracle --type <t>`) — do NOT reach for a deviation._

| L-rule | Change | Files (both sides) |
|---|---|---|
| **L-034** (2026-07-07, rc_out) | Month-boundary false-flag fix, two parts: (1) widen the rc_out dedup compare-set floor to `min(min(extracted date), watermark−3d)` so the workbook's oldest sheet rows are compared against their saved DB copies (the asymmetric `watermark−3d` window false-flagged 5 already-saved June-30 rows); (2) when a natural-key MATCH's ONLY diff is `production_batch`, NOOP + emit a `soft_warnings[]` note, never a VALUE_CHANGED or a hold. Oracle rebuilt (both fixtures carry `soft_warnings: []`; parity 12/12). | window: `sync_rc_out.py` + `src/reports/rc_out/index.ts`; classify: `classify_rc_out.py` + `src/reports/rc_out/classify.ts` |
| **L-037** (2026-07-07, rc_out) | Balance-integrity guard: classify HOLDS (→ `flagged`, never written) any row whose scraped DAY TOTAL disagrees with the block's own **STRT − END** (`abs((strt−end)−day) > 1 kg`) or whose STRT breaks continuity from the immediately-prior **same-(whse_label, block_no), same-day** section (`abs(strt − prevEnd) > 1 kg`). Catches the June-10 cross-block cumulative (leg-2 DAY TOTAL stored 31,745 = day-opening−leg-2-end instead of its own 20,932) and blocks it from either inserting as NEW or overwriting a corrected DB row as VALUE_CHANGED. Guard runs AFTER batch-resolution, BEFORE natural-key routing; fires only when the needed balances are BOTH present (a blank STRT/END is never held). The **pallet-Net sum is deliberately NOT a trigger** — real reports list pallets only partially (pathway/SUNDRY zones carry none), so a net-sum gate false-holds constantly (verified across the full May+July corpus, where STRT−END==DAY exactly but net-sum diverges widely). No new output keys (reuses the `flagged` bucket); guard is inert on both fixtures so only the edge workbook's filler STRT (`9999`→`day_total`) moved in the oracle. Parity 12/12. | classify: `classify_rc_out.py` + `src/reports/rc_out/classify.ts`; edge geometry: `scripts/build_fixtures.py::_proposed_section` |
| **FEED-label detection** (2026-07-11, rc_out, production incident) | `is_feed` was `"FEEDING AREA" in whse.upper()` (exact-phrase substring). The JULY 8/9 2026 day-tabs' section #1 uses the operator's newer header `"FOR FEEDING"`, which that check missed — the section extracted as a numbered block (`"JULY-26-BLK1"`, a real unrelated batch at D-19B) instead of a FEED batch (`"JULY-26-FEED1"`), draining D-19B by 19,605 kg and closing it in error (Renzo hand-corrected the DB; arbitrated as a confirmed bug, not a judgment call). Fixed to `FEED_LABEL_RE = r"\bFEED(?:ING)?\b"` — case-insensitive whole-word match, so `"FEEDING AREA"` (legacy template) and `"FOR FEEDING"` (current template) both match, while a real block label like `"A-16D"`/`"16A NEAR PATHWAY"` does not. Checked every WHSE label across BOTH existing rc_out fixtures (`rc_out_real_latest`, `rc_out_edge`) old-vs-new detector — zero diffs, so **no oracle rebuild was required**; parity stayed 12/12 unchanged. Verified against the real production workbook (Storage `789775c4-2b61-498a-9a54-9c5a01484a89/rc_out/PROPOSED DAILY REPORT JULY 2026.xlsx`) before and after the fix. | `.claude/skills/sync-ictc/scripts/extract_proposed_daily.py` (`FEED_LABEL_RE`, `is_feed` line); `src/reports/rc_out/extract.ts` (`FEED_LABEL_RE`, `isFeed` line); test: `test/reports/rc_out.test.ts` describe `"extract — FEED-label detection"` |

| **L-042** (2026-08-13, deliveries) | **An operator's shorthand is a naming convention, not malformed input — and the SAME lesson as the FEED-label row above, on the sibling extractor.** Two changes, both lockstep. **(1)** `FEEDING_AREA_RE` was `^FEEDING\s+AREA\s*(\d*)$` — the SHEET's spelling. MC writes `FEEDING # 1`, so the label fell through to the raw-value branch: truthy (passed the malformed guard), not pattern-valid (never auto-created), therefore **held on every run forever** — 2026-08-05 / AAV 6111 / 19,185 kg was stuck a week. Widened to `^FEEDING(?:\s*(?:AREA|NO))?\s*[#.:-]?\s*(\d*)\s*\.?$`, producing EXACTLY the existing `"{MMM}-{YY}-FEED{N}"` output for every accepted spelling; the anchor stays strict (leading `FEEDING`, digits only after the designator) so `FEEDING AREA A`, `FEEDING AREA 1 AND 2`, `RE-FEEDING 1` and bare `FEED` are still rejected. **The non-obvious half:** translating alone would have *moved* the stall, not removed it — the extractor derives the code from the FULL month name (`AUGUST-26-FEED1`) while the DB spells that batch **`AUG-26-FEED1`** (August feed is `AUG-`, August blocks are `AUGUST-`; both conventions live), so under L-040b's tier-1 identity the row matches and then "disagrees" on `batch_code`, turning a held `unmapped` into a held `cross_batch_reassignment`. `src/lib/batchCodeAlias.ts` (new) is THE definition of "same code, spelled differently", built on the alias table that already existed (`batchCodeFallbacks`), and it is used in TWO places answering different questions: `batchCodeAliasEqual` in `field_differences` (do the sources really disagree — makes the 08-05 row a clean NOOP) and `resolveKnownBatchCodeAlias` in the guard (what a genuinely NEW row is WRITTEN as, borrowing L-033b's property verbatim: it can only ever point at a batch that ALREADY EXISTS and never overrides a code that already resolves). Skipping the second would have replaced an obviously-junk `FEEDING # 3` batch with a plausible duplicate `AUGUST-26-FEED3` beside the real `AUG-26-FEED3` — a loud wrong traded for a quiet one. **NOT a fuzzy matcher:** only the existing table's prefix pairs collapse and only when year + suffix are byte-identical, so `JULY-26-BLK9` vs `JUNE-26-BLK9` (the L-033 phantom, which BOTH deliveries fixtures turn on) is untouched — that survival is the proof it does not over-collapse. **(2)** The malformed guard called MC's not-yet-assigned overnight rows "malformed"; they now go to a new `awaiting_assignment` bucket + `summary.awaiting_assignment_count`, never held, reported as an `awaiting_batch_assignment` finding whose severity escalates with age. The split is by a POSITIVE signal — the row must carry a **truck plate** — which is what keeps an **orphan wet-recovery sub-row** (defined by having no plate, no batch, no block) in MALFORMED where it belongs. Oracle rebuilt (`npm run build:oracle -- --type deliveries`); the ONLY movement was the two new empty keys, so parity is green with **NO `expected-deviations.json` entry**. | extract: `.claude/skills/sync-ictc/scripts/extract_rc_deliveries.py` (`FEEDING_AREA_RE`) + `src/reports/deliveries/extract.ts`; alias: `src/lib/batchCodeAlias.ts` (new) + `classify_deliveries.py` (`batch_code_alias_equal` / `batch_code_spellings`); classify: `src/reports/deliveries/classify.ts` + `classify_deliveries.py`; guard: `scripts/parity_guards.py` (`_alias_of`); apply/plumbing: `src/reports/deliveries/{apply,index}.ts` + `src/workflows/normalizeReport.ts`; app: `app/(app)/sync/types.ts` + `lib/sync/{cases-fold,findings}.ts`; oracle: `fixtures/deliveries/oracle/*.json`; tests: `test/reports/deliveries-feeding-label.test.ts` (new, 17) + `scripts/verify-awaiting-batch-assignment-fold.ts` (new, 8) |

| **BUG-005** (2026-07-17, rc_out + gsheet) | **`production_batch` month-name canonicalization — full uppercase word for all 12 months.** The PROPOSED extractor derived `production_batch` from `strftime("%b").upper()` (a 3-letter abbreviation) with full-name overrides for **May/June ONLY** — a legacy quirk that conformed to whatever spelling happened to be observed in the DB at the time. The gsheet extractor meanwhile read the Sheet cell literally, and the Sheet has always written **full** month names. Two writers of the same column, two conventions → `rc_out.production_batch` accumulated BOTH `JUL` (28 rows) and `JULY` (137 rows), plus a stray `APR` vs `APRIL`. Since `encodeCampaign()` (`app/(app)/inventory/rc-movement/actions.ts`) keys on the raw string, `JUL-2026` ≠ `JULY-2026` and the RC Movement campaign picker listed the same month as two phantom campaigns. Fixed by making the **full uppercase name canonical** (the Sheet's established convention, and the spelling the large majority of live rows already carry) and routing BOTH writers through ONE shared pure module, `src/lib/months.ts` (`MONTH_NAMES` / `monthName(n)` / `canonicalMonthName(raw)`), so the two conventions structurally cannot diverge again. rc_out derives via `monthName(txnDate.month)`; gsheet defensively canonicalizes the cell it reads via `canonicalMonthName()` (a no-op on real full-name data; an unrecognized value passes through **verbatim**, so nothing the extractor was told to capture literally is ever silently rewritten). **This is a canonicalization bug, NOT a source disagreement** — both witnesses agree on the fact, one writer failed to normalize its spelling — so it does not route through Sync Review arbitration. Left unfixed, gsheet classify (`gsheet/classify.ts:176-184`) would raise a pointless `production_batch` diff case every time the Sheet caught up to a PROPOSED-written month. **NOT a deviation**: the Python oracle changed in lockstep (new `MONTH_NAME_UPPER` map replacing the `%b`+overrides block), oracle rebuilt (`npm run build:oracle --type rc_out`) — the rebuild moved **exactly 12 lines, all `production_batch` `"JUL"`→`"JULY"`, nothing else** — so parity is 12/12 with NO `expected-deviations.json` entry. Note the `batch_code` month prefixes (`PRIMARY_MONTH_PREFIX`/`FALLBACK_MONTH_PREFIX`/`MONTH_PREFIX_ALIASES`, incl. the SEPT/SEP asymmetry) are a DIFFERENT identifier with its own deliberately mixed convention and are **deliberately left untouched** — do not unify them. Data repair of the existing `JUL`/`APR` rows ships separately (BUG-005 step 2). | `.claude/skills/sync-ictc/scripts/extract_proposed_daily.py` (`MONTH_NAME_UPPER`, `production_batch` line); `src/lib/months.ts` (new); `src/reports/rc_out/extract.ts` (removed `MONTH_ABBR_B`); `src/reports/gsheet/extract.ts`; oracle: `fixtures/rc_out/oracle/*.json`; test: `test/reports/rc_out.test.ts` (`mkRow` default) |

| **L-040b** (2026-08-08, deliveries + gsheet) | **The delivery identity is now TWO-TIER, and the parity decision was (a) change the ORACLE, not (b) log a deviation.** The natural key was `(transaction_date, batch_code, block_loc, weight_kg)` — no truck plate, and three human-correctable facts. Correct a batch code, a block or a weight and the classifier stopped recognising the row, reported it NEW, and the pipeline inserted a second copy; that is how the 2026-02-04 block swap and every `FEEDING # 1` / `JULY-26-FEED1` spelling mismatch duplicated (L-040 removed 9 such rows). Identity now lives in ONE shared definition — `src/lib/deliveryIdentity.ts`, mirrored in `classify_deliveries.py` and **imported** by `classify_gsheet.py` — with **TIER 1** `(transaction_date, norm_plate(truck_plate), sacks)` (measured UNIQUE across all 1,545 live deliveries carrying both) and **TIER 2** the legacy key verbatim, for the 143 plateless rows. Lookup is tier 1 then tier 2, so the matching set is a strict SUPERSET of the old key's: the change can only turn an insert into a match, never the reverse. The three fields that left the key ENTERED the diff (omitting that would turn a duplicate into a silent NOOP — worse), and a disagreement on any of them is routed to a NEW `identity_diff` bucket which the guard folds into `flagged` (`L040_identity_diff` → existing `HeldKind` `cross_batch_reassignment`) so a **human arbitrates it** — never a Sheet-wins UPDATE, never an insert. gsheet additionally moved its identity lookup BEFORE the UNMAPPED verdict, because a known truckload under an unrecognised code is a naming disagreement, and deciding UNMAPPED first let a pattern-valid unknown code auto-create a batch and insert a second copy of a delivery already in the DB. **WHY (a) AND NOT (b):** ruling #2–#5 deviations exist for places the ORACLE carries a live crash or DB-constraint bug; this is a deliberate business-rule change to a rule both engines implement, so recording it as a deviation would have permanently blinded parity to the single highest-risk function in the pipeline — a parity harness that expects the two engines to differ on the identity rule can no longer detect a real divergence in it. Both engines changed in lockstep, the oracle was rebuilt, and parity is **12/12 with NO `expected-deviations.json` entry** — the L-034 / L-037 / BUG-005 discipline. Oracle movement was confined to the new buckets/fields and the rows they re-routed (deliveries: 1 NEW → identity_diff per fixture; gsheet rc_in: 6 rows → `identity_diff`, `rc_out` untouched). **PARITY TRAP (new, will recur):** the refusal `reason` is a STRING and the canonicalizer does not normalize numbers inside strings — Python `str(18827.0)` is `'18827.0'`, JS `String(18827)` is `'18827'` — so interpolating a weight or sack count into a message needs a JS-style renderer on the Python side (`_js()`), the same trap as the `dup_noop` natural_key. **DRY RUN (a condition of the change):** `scripts/delivery-identity-dryrun.ts` runs the frozen `classifyCase` for BOTH writers against the live DB and the real current sources, twice — this tree vs a pristine `git worktree` at HEAD, so the OLD side is the actual old code and not a re-implementation — behind a DbClient proxy whose every write throws. It reclassified exactly 5 live rows, all toward holding: 1 silent `dup_noop` → a named held diff, 4 vague `unmapped` → named held diffs, 0 on the rc_out side. | identity: `src/lib/deliveryIdentity.ts` (new) + `.claude/skills/sync-ictc/scripts/classify_deliveries.py`; classify: `src/reports/deliveries/classify.ts` + `src/reports/gsheet/classify.ts` + `.claude/skills/sync-ictc/scripts/classify_gsheet.py`; guard: `scripts/parity_guards.py`; apply (guard columns): `src/reports/deliveries/apply.ts` + `src/reports/gsheet/apply.ts`; oracle: `fixtures/{deliveries,gsheet}/oracle/*.json`; tests: `test/reports/delivery-identity.test.ts` (new, 25) + `test/reports/gsheet.test.ts` + `test/reports/gsheet-idempotency.test.ts`; dry run: `scripts/delivery-identity-dryrun.ts` + `scripts/identity-dryrun-classify.ts` (new) |

| **L-048** (2026-09-03, rc_out) | **A worksheet name is typed by a HUMAN — the day-tab reader now accepts the separator a person actually writes.** `SHEET_NAME_RE` was `^([A-Z]+)\s+(\d{1,2})\s*$` — a bare SPACE between the month word and the day, and nothing else. MC's `260902 PROPOSED DAILY REPORT SEPTEMBER 2026.xlsx` names its tabs `Aug. 29` / `Sep. 1` / `SEP. 2` (a **period** after the abbreviation), so all three were skipped, `extract_proposed_daily` returned **ZERO rows from a workbook holding 10 feedings / 82,837 kg**, and the run reported `succeeded` while `rc_out` stalled at 2026-08-28 (run `cc8c66f9`). Widened in BOTH engines to `^\s*([A-Za-z]+)\s*\.?\s*(\d{1,2})\s*\.?\s*$`: optional period after the month token, any/no whitespace around the separator, an optional trailing period, any case. The anchor stays strict, so `SUMMARY`, `TOTAL`, `Sheet1` ("SHEET" is not a month), `Aug 32` (invalid day, refused by `date()` / `isValidCalDate` exactly as before) and `JANUARY 2026` (the RC MOVEMENT month-tab shape — 4 digits can never be a day) are all still refused. Third instance of L-039's and L-042's lesson, and named as such. On the TS side the local 24-entry `MONTH_NAME_TO_NUM` map was **deleted** in favour of `lib/months.ts::monthNumberFromToken`, THE one month-token table (the same one Czarina's price-tab resolver reads); the Python keeps its own identical map because the engines cannot share a module. **WHY THIS IS A POST-PORT CHANGE AND NOT A DEVIATION** (the L-034 / BUG-005 / L-040b discipline): it is a deliberate business-rule change to a rule both engines implement, so recording it as an expected deviation would permanently blind parity to the function that decides WHICH DAY a feeding belongs to. Both regexes moved in lockstep. **NO ORACLE REBUILD WAS REQUIRED** — checked the way the FEED-label fix was checked: every sheet name in BOTH rc_out fixtures (`rc_out_real_latest` = `JUNE 30` / `JULY 1` / `JULY 2`; `rc_out_edge`) parses identically old-vs-new, so parity stayed **12/12 unchanged with no `expected-deviations.json` entry**. Verified against the real Storage objects before and after: the September workbook goes 0 rows → 10 rows (`Aug. 29` 3 sections / 12,314 kg, `Sep. 1` 3 / 41,867 kg, `SEP. 2` 4 / 28,656 kg), and the two August workbooks (`260828`, `270826`) are unchanged at 92 and 88 rows, 22 / 21 tabs parsed, zero unparsed. | `.claude/skills/sync-ictc/scripts/extract_proposed_daily.py` (`SHEET_NAME_RE`); `src/reports/rc_out/extract.ts` (`SHEET_NAME_RE`, `sheetNameToDate`, removed `MONTH_NAME_TO_NUM`); test: `test/reports/rc_out-sheet-names.test.ts` (new, 32) |

## Apply-phase deviations (not classify — parity unaffected)
_Changes below live entirely in the orchestrator/apply layer (`index.ts`/`apply.ts`), never
`classify.ts`. They do not touch the classify oracle and require no `expected-deviations.json`
entry or oracle rebuild — the parity harness only exercises `classifyCase`, which is untouched._

| Date | Report | Change | Files |
|---|---|---|---|
| 2026-07-11 | rc_out | **Date-scoped gate quarantine** (production incident, run `83d17774`). The run-wide "any HARD gate trip halts apply entirely" behavior (Python `sync_rc_out.py::phase_apply` step 1) over-blocked: a stale drift on long-settled dates in the PROPOSED workbook's history froze same-day clean feedings too. Replaced with partition-level quarantine: each gate identifies the SPECIFIC `transaction_date`(s) genuinely at risk; only actionable rows on those dates are held, everything else writes normally. GATE 1 additionally gained a witness-corroboration downgrade (a drifted date whose DB sum already matches the movement sheet is NOT quarantined — informational note only). GATE 2's held-row detail is now isolated to real O-vs-M excess (previously could trip + report an EMPTY `drift_dates[]` when the underlying cause was actually a GATE-1-shaped P-vs-M drift riding the same reconcile pass). See `specs/rc_out.md` → "4a. Gates & quarantine" for full detail. | `src/reports/rc_out/index.ts`, `src/reports/rc_out/apply.ts` |
| 2026-07-11 | gsheet, rc_out | **Batch auto-create policy (Renzo, approved) — REVERSES the "never auto-create a batch" rule** (`gsheet.md` §5 rule #6, `rc_out.md`'s `never-auto-create-batch` codified rule, `deliveries.md`'s note about NOT deriving a new code — that one is UNCHANGED, see below). A `batch_code` a source names that has no row in `batches` yet is now AUTO-CREATED, provided it is **pattern-valid**: a recognized month-prefix (JAN/JANUARY/…/SEPT, incl. the SEPT/SEP asymmetry) + `-YY-` + a kind+number suffix (`BLK6`, `FEED1`, `SUNDRY2`, …) — `src/lib/batchAutoCreate.ts::isPatternValidBatchCode`. A pattern-INVALID code (a likely typo) still holds/unmapped exactly as before. Before creating, the code (+ its regenerated month-prefix fallbacks) is resolved against the in-memory `batch_code→batch_id` lookup the classify layer already built — an ALIAS of an existing batch (e.g. `JUL-26-BLK6` when `JULY-26-BLK6` exists) resolves to it instead of creating a duplicate. The new batch follows the SAME template as the human-confirmed "create this batch" Sync Review action (`lib/sync/create-batch-plan.ts::deriveBatchFields`, app-side — mirrored, not imported, in `batchAutoCreate.ts`): `location_ref` = block_loc or `'FEED'`, `status='STORED'`, `current_weight=0`, `avg_cost=null`. The DB write goes through the NEW `DbClient.upsertBatchIfAbsent` (ON CONFLICT batch_code DO NOTHING + re-select) so the PARALLEL writer lanes (deliveries + rc_out both feeding a brand-new batch the same run) can't duplicate or crash each other. Every auto-create writes a `batches` audit_logs row (`write_ingestion_audit`) and an info-level run finding (`result.reports[type].apply.auto_created_batches[]` → `lib/sync/findings.ts::fromAutoCreatedBatch`, severity `'info'`) so it's visible in the Sync panel. Lives ENTIRELY in the apply layer (gsheet's UNMAPPED loop in `apply.ts`; rc_out's `unmapped` bucket in `apply.ts`) — `classify.ts` in BOTH reports is untouched, so parity stays 12/12 with no `expected-deviations.json` entry. `deliveries/apply.ts`'s existing defensive batch-upsert (for its OWN already-classify-resolved code) is UNCHANGED by this policy — it never held an unmapped code in the first place. | `src/lib/batchAutoCreate.ts` (new), `src/lib/db.ts` (`upsertBatchIfAbsent`), `src/reports/gsheet/apply.ts`, `src/reports/gsheet/index.ts` (compact `full` payload), `src/reports/rc_out/apply.ts`, `src/workflows/normalizeReport.ts` (`auto_created_batches` passthrough), `app/(app)/sync/types.ts`, `lib/sync/cases-fold.ts`, `lib/sync/findings.ts` |
| 2026-07-12 | rc_out | **Date-settlement ledger (Renzo's directive) — skips already-balanced historical dates entirely.** New table `rc_out_date_settlements` (migration `20260712010000_rc_out_date_settlements.sql`): once a `transaction_date`'s DB feeding total agrees with the RC MOVEMENT sheet's daily total within tolerance (50kg — reuses the existing GATE tolerance), that date is SETTLED and every future run skips it completely (no extract-compare, no classify, no reconcile, no gate, no flags). Motivation: the PROPOSED workbook permanently carries every day-tab the operator has ever filled in, so even the date-scoped quarantine fix above still re-walked the ENTIRE history through both HARD gates on every run. Pure settle-criterion core lives in `src/workflows/settlement.ts::computeQualifyingSettlements` (mirrors the `creationRaceHolds.ts` pure-core/IO-wrapper split); computed + written by a DEDICATED new stage `workflows/runSync.ts::persistSettlements` (Stage 2d) — NEVER inside `classify.ts` (parity-frozen) and NEVER inside the read-only `rc_movement_audit` report (which must stay "never writes to the DB"). Full-history backfill: reads rc_out sums since a fixed `2025-01-01` floor (not the auditor's own narrower 30-day lookback), so the first run after this ships settles every already-balanced historical date at once. TWO skip chokepoints, both apply-layer only: `reports/rc_out/index.ts::runReport` filters `proposed.rows` by the settled-date set right after `extractProposed`, before the gate `reconcile()` calls and before `classifyRcOut`; `workflows/runSync.ts::reconcileRcOutShadow` filters the re-extracted `proposed`/`gsheetRcOut` rows the same way before bucketing into the R1 reconciliation engine. `classify.ts` in rc_out is completely untouched by this change — parity stays 12/12 with no `expected-deviations.json` entry. See `specs/rc_out.md` §4b "§ Settlement" for the full mechanism. | `supabase/migrations/20260712010000_rc_out_date_settlements.sql` (new), `src/workflows/settlement.ts` (new), `src/lib/db.ts` (`readSettledDates`, `insertSettlements`), `src/workflows/runSync.ts` (`persistSettlements` stage, settled-date filter in `reconcileRcOutShadow`), `src/reports/rc_out/index.ts` (settled-date filter in `runReport`) |
| 2026-07-27 | gsheet | **Last-instant idempotency guard on the Sheet write path (Renzo, approved) — RETIRES the "gsheet uses plain `db.insert()`" asymmetry** (`gsheet.md` §5 "Idempotency mechanism" + porting trap #4; `SHARED.md` §2.5 note). gsheet's NEW loops now call `db.insertIfAbsent()` for BOTH `deliveries` and `rc_out`, with the SAME natural keys the email writers use — `deliveries` `(transaction_date, batch_code, truck_plate, weight_kg, sacks)`, `rc_out` `(transaction_date, batch_id, destination)` — exported as `GSHEET_DELIVERIES_NATURAL_KEY` / `GSHEET_RC_OUT_NATURAL_KEY` so the two paths provably agree on what "the same row" is. **Why the old design was wrong:** "the classifier's fresh-DB-window decision is the idempotency guard" holds only while gsheet is the SOLE writer of the table, and it is not — the email pipelines write `deliveries` too. The classifier's snapshot is taken at the START of the run, so it is a time-of-check/time-of-use window, not an authority. Confirmed production incident (BUG-016, `docs/BUG_LEDGER.md`): on 2026-07-15 the email path inserted a C-11B delivery at 01:36:01Z and gsheet, still acting on its pre-01:36 snapshot, blind-inserted a second identical copy at 03:43:56Z — +24,024 kg of phantom inventory on block C-11B (app 84,753 vs Sheet 60,729) plus inflated RC IN / MTD totals. A guard hit is **surfaced, not silent**: it becomes a held row using the email path's EXISTING vocabulary (`reason` and `kind` both `already_exists`, detail `"idempotent skip (natural key already in DB)"`) — no new `HeldKind` invented (`HeldKind` is frontend-locked). `ModeApplyResult.skipped[]` gained an optional `reason` so `applyGsheet` can pass that word through instead of the generic `"skipped"`; every other skip is unchanged. The 2026-07-11 auto-create write helpers (`writeRcInDelivery`/`writeRcOutRow`) are guarded too and return a discriminated `already_exists` outcome, so a duplicate there holds as `already_exists` rather than being mis-reported as `unmapped_batch_code`. **Inherited trade-off (deliberate, `lib/db.ts:13`):** two genuinely-identical truckloads on the same date/batch/truck/weight/sacks are indistinguishable on the natural key and the second is suppressed — the email path has always accepted this, and a suppressed row is HELD (visible + re-appliable), never dropped, whereas the pre-fix blind insert produced a silent invisible duplicate. **rc_out scope note:** under the default `SYNC_RCOUT_RECONCILE_CUTOVER=on` the rc_out mode is skipped WHOLE in `applyGsheet`, so that guard is inert today; it was added because the flag is a documented one-line revert, and with it OFF gsheet and the PROPOSED writer target `rc_out` simultaneously — the identical two-writer race. Apply-layer only; `classify.ts` untouched, parity 12/12, no `expected-deviations.json` entry. | `src/reports/gsheet/apply.ts`, tests: `test/reports/gsheet-idempotency.test.ts` (new), `test/reports/gsheet.test.ts` + `test/reports/gsheet-autocreate.test.ts` (stubs gained `insertIfAbsent`) |
| 2026-09-03 | rc_out | **An empty read of a NON-EMPTY workbook is LOUD, and never consumes the email (L-048).** Widening the tab-name regex (above) fixes the tabs we have SEEN; what made the September stall invisible for two days is that **nothing said the file had not been read** — the three skipped tabs existed only as strings in `soft_warnings`, which is not a findings channel, so the run labeled the email, advanced the watermark and reported `succeeded` with no finding at all. `extractProposed` now returns `sheetsParsed` / `sheetsUnparsed` (structured, not prose); `index.ts` folds a non-empty `sheetsUnparsed` into ONE `source_tab_notes` entry via the single constructor `reports/sourceTabs.ts::sourceTabsNote` — **`high` when 0 of N tabs parsed, `attention` when some did**, NAMING BOTH LISTS (the L-039 discipline: one list is a complaint, two lists side by side show the convention that moved). **The load-bearing half:** when `tabs_read === 0`, `compact.source_unconsumable` makes `apply.ts` skip BOTH the Gmail label and the watermark advance, because every primary query ends `-label:"Blackwood-Processed"` and labeling a file it could not read is the sync throwing the source away; nothing was written, so there is nothing to be idempotent about. A PARTIAL failure consumes normally. Nothing is HELD (no durable case to close by hand) and the channel carries no ₱. Proven necessary by a case the regex can never fix: the 2026-09-01 workbook (`6b555102`) has ONE sheet named literally `Sheet1` carrying the 08-29 feedings, with the date only in cell J1 — the pure `tabs_read = 0` case; **no date-from-header inference was built**, because guessing a transaction date from a title cell is a quiet wrong traded for a loud one. Apply/orchestrator layer only; `classify.ts` untouched, parity 12/12, no `expected-deviations.json` entry. | `src/reports/sourceTabs.ts` (new), `src/reports/rc_out/{extract,index,apply}.ts`, `src/workflows/normalizeReport.ts`, `app/(app)/sync/types.ts`, `lib/sync/{cases-fold,findings}.ts`, tests: `test/reports/rc_out-sheet-names.test.ts` (new, 32) + `scripts/verify-findings.ts` (+4) |
| 2026-09-03 | deliveries | **"Not received" must not mean "already eaten" (L-048 part 3).** Run `f1e9f342` (a manual Run Sync at 03:13Z) reported *"No RC DELIVERIES report arrived"* about an email run `cc8c66f9` had ingested and LABELED ninety minutes earlier — a processed email is excluded from every later mailbox query, so a second run of the day legitimately finds nothing. A finding that cries wolf on every manual run is one an operator learns to skip, which is exactly how the failure it exists to catch gets through. `reportNotReceivedNote` now takes the stream's `ingestion_watermarks` row (new read `DbClient.readIngestionWatermark`, non-fatal, never throws) and sets `already_processed` when `last_email_received_at` — else `last_run_at` — falls on the run's own **Asia/Manila** day; `last_run_at` is stamped from inside apply, which only runs when a source file was present, so a stamp from today means an email WAS consumed today. It is a **DOWNGRADE to `info` with a different sentence, never a silence** (silence would be L-044's reassuring line in a new costume), and an unreadable/absent bookkeeping row leaves the finding at full volume — an unknown must never quieten an alarm. | `src/lib/db.ts` (`readIngestionWatermark`), `src/reports/reportNotReceived.ts` (`alreadyProcessedToday`), `src/reports/deliveries/index.ts`, `src/workflows/normalizeReport.ts`, `app/(app)/sync/types.ts`, `lib/sync/findings.ts`, tests: `test/reports/deliveries-report-not-received.test.ts` (+5) + `scripts/verify-findings.ts` (+1) |
| 2026-07-13 | rc_out | **Patio write-skip (confirmed data-integrity bug) — drops PROPOSED rows at a known patio alias BEFORE they can reach the write path.** rc_out's natural key is `(transaction_date, batch_id, destination)` — NO `block_loc` (`apply.ts:13`). The PROPOSED report mis-derives a numbered `BLK` batch code for sun-drying PATIO feedings (which are really the Sheet's `SUNDRY` batches at coded `PCA`/`PCB` blocks — see `src/reconcile/blockAliases.ts`), and that phantom row then COLLIDES on the natural key with a genuine, unrelated block feeding attributed to the same derived batch, so every sync run OVERWRITES the real feeding with the patio duplicate. Live-audit-confirmed incident: rc_out row `0238c58d` flip-flopped 6× between `"JAN-26-BLK17 @ A-11B" 7045kg` (real) and `"JAN-26-BLK17 @ 15A MIDDLE SIDE" 7494kg` (patio duplicate of the already-stored `MARCH-26-SUNDRY7 @ PCA-15C` 7494kg). `reports/rc_out/index.ts::runReport` now drops any proposed row whose `block_loc` is a known patio alias (`src/reconcile/blockAliases.ts::isKnownPatioAlias`) in the SAME filter pass as the settled-date skip above, right after `extractProposed`, before the gate `reconcile()` calls and before `classifyRcOut` — a normal coded block (`A-11B`, `C-10A`) or a FEED row (`block_loc = null`) is never affected (`isKnownPatioAlias` returns `false` for both). These feedings are Sheet-owned: PROPOSED cannot attribute the correct SUNDRY batch, and the feeding already exists as the Sheet's SUNDRY row, so PROPOSED writing it can only duplicate/clobber. The reconciler still SEES proposed's patio rows for reconciliation purposes (findings, visibility) — this fix only stops PROPOSED from WRITING them. A one-line run-summary note (`"Skipped N patio feeding(s) on the write path…"`) fires only when `N > 0`. `classify.ts` in rc_out is completely untouched — parity stays 12/12 with no `expected-deviations.json` entry. See `specs/rc_out.md` §4b "§ Settlement" and `src/reconcile/CONTEXT.md` § "Patio block aliases". | `src/reports/rc_out/index.ts` (patio filter, shares the settled-date filter pass), `src/reconcile/blockAliases.ts` (existing `isKnownPatioAlias`, now also gates the write path), test: `test/reports/rc_out-patio-write-skip.test.ts` (new) |

## Also binding (from the plan, restated for porters)
- All rounding through `norm.ts` (round-half-to-even); `Math.round` is lint-banned.
- Fetch queries, thresholds, natural keys: VERBATIM from the specs — no "improvements."
- Held-row philosophy: uncertain → held, never auto-written — **EXCEPT** a pattern-valid
  unknown `batch_code` (2026-07-11 policy above), which is no longer "uncertain": the code
  itself is machine-verifiable, so it auto-creates + writes. A pattern-INVALID code is still
  held exactly as before — the philosophy holds for everything that's genuinely ambiguous.

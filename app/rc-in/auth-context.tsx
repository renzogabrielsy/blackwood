// Re-export from shared location for backwards compatibility
export { AuthProvider, useAuth } from '@/components/providers/auth-context';
export type { UserRole, Permission } from '@/components/providers/auth-context';

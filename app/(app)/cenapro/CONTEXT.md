# Cenapro Module (Tenant #2 — CI / Cebu)

## Purpose
Top-level route `/cenapro` for the **second tenant** on the Blackwood platform: the CI / Cebu charcoal company ("Cenapro"). Four screens over the dedicated `cenapro` Postgres schema: an **editable** Production ledger (the app is now the maintaining file — operators add/edit/delete production rows here instead of in Excel), a Flec Inventory view whose **STARTING (opening) balances are now editable + append-only + backtrackable** (the rest of the inventory view — closing balances + movement ledger — stays computed/read-only), and the **QC pair** — an editable QC Ledger for CCC's partner lab results plus a read-only QC Breakdown. Fully decoupled from the ICTC / Davao tenant — shares zero tables, types, or code.

> **Tenant/Domain Module (Cenapro):** Cebu-specific.
> - **Production (`/cenapro/production`)** is an **editable Industrial-Spreadsheet ledger** — inline cell editing, dropdowns for categorical columns, add/delete rows, paste-from-Excel, dirty tracking + single Save/Discard. It writes through the auto-updatable `public.cenapro_production_events` VIEW (see "Production write path" below).
> - **Flec Inventory (`/cenapro/inventory`)** mirrors the Excel "PC WHSE" sheet: an **editable STARTING block** (opening flec count per grade × side, append-only, with full history) on top of computed read-only closing balances + the show-your-math movement ledger. Only the opening balances are written; everything below them is re-derived in SQL (see "Opening-balance write path" below).
> - **QC Ledger (`/cenapro/qc`)** is the **editable** entry grid for CCC's partner lab results (BD · ASH · GRIT · MC), one reading per source + warehouse per day, written through `cenapro_save_analysis_sample` with optimistic concurrency. **QC Breakdown (`/cenapro/qc/breakdown`)** is its read-only reading surface. See "CCC / QC Analysis — THE LIVE FEATURE" below.

## Files
| File | Role |
|------|------|
| `layout.tsx` | Module shell — a borderless `Card` frame (matches Production's look). No shared period provider; each screen owns its own controls. |
| `page.tsx` | Landing hub — four `hover-lift` card-links (Production / Flec Inventory / QC Ledger / QC Breakdown) + a "Tenant #2 · Cebu" note. |
| `loading.tsx` | Table skeleton (toolbar + header + 14 row stripes). |
| `error.tsx` | Error boundary — persistent message + **Copy button** (error HARD RULE) + a note that empty data is expected pre-exposure. |
| `types.ts` | Row types **derived from the generated `types/supabase.ts`** (`cenapro_production_events` view + `cenapro_flec_balance`/`cenapro_flec_ledger` function Returns) + flec-warehouse constants + `formatDisposition()` (still used by the Flec Inventory ledger's friendly "Crusher C1" label) + the **editable-ledger lookup constants** (`SHIFT_CODES`/`GRADE_CODES`/`PLANT_CODES`/`WAREHOUSE_CODES`/`SOURCE_LOCATION_CODES`/`DISPOSITION_KINDS`/`WHSE_SIDES` + `CRUSHER_CODES`/`KILN_CODES`) + the equipment-dependency helpers `partnerEquipmentOptions()` / `dispositionRequiresEquipment()` (still consumed by `bulk-paste-utils`'s remaining validation), **and the shared CCC/FLEC single-column helpers**: `CCC_FLEC_OPTIONS` (typeahead list `FLEC`/`C1`–`C4`/`RK1`–`RK4`), **`parseCccFlec()`** (forward: one cell string → `{disposition_kind, partner_equipment_code}`, forgiving on case/aliases, null on unrecognized) and **`formatCccFlec()`** (inverse: the two DB fields → the single Excel-parity string). These two are the canonical derive/format pair shared by BOTH the inline grid's save and the bulk modal (via `bulk-paste-utils`). See "Editable-ledger lookups" + "CCC/FLEC single column" + "Data path" below. |
| `production/page.tsx` | Server — **TWO orthogonal URL axes (a third, EDIT lock/unlock, is Phase 3):** **VIEW** `?view=ledger\|daily-w6\|daily-w7` × **SCOPE** `?scope=endless\|focus` (`endless` default + omits the param; a legacy `?focus=1` maps to `scope=focus`). Resolves the selected **period** from `?year=&batch=` (defaults to NEWEST when absent; stale/invalid → newest), then branches on BOTH axes into six no-dead-end combos: **(1) endless + ledger** → resolves the anchor (`{kind:'period'}` if the URL pair is valid, else `{kind:'latest'}`), **prefetches the FIRST keyset window server-side** (`fetchLedgerPage({mode:'anchor',anchor})`) so the initial paint is already anchored, and renders `<ProductionEndlessSheet key={anchorKey} … />` (keyed by anchor → clean remount on a dropdown jump). **(2) endless + daily-w6/w7** (Phase 2B) → resolves the SAME anchor, **prefetches the FIRST day-window server-side** (`fetchDailyPivotWindow({mode:'anchor',anchor,plant})`) so the initial paint is already anchored, and renders `<ProductionEndlessPivots key={plantView:anchorKey} … />` (keyed by plant+anchor → clean remount on a dropdown jump). The Phase-1 "coming in a later pass" fallback bar was REMOVED. **(3) focus + any view** → the month-scoped path `fetchProductionEvents(selectedPeriod)` → `<ProductionView>` (which itself switches ledger/W6/W7 on `?view=` and stays editable). Uses the pure `parseScope`/`parseViewMode`/`plantViewOf` from `ledger-url.ts`. **FILTER axis (2026-08-03):** also `parseLedgerFilters`es the six filter params SERVER-side and passes them into `fetchLedgerPage({…, filters})` so the endless prefetch is already filtered, plus down to `<ProductionEndlessSheet filters filtersActive>`; the sheet's React key becomes **`` `${anchorKey}|${filterKey}` ``** so a filter change forces the same clean remount a period jump does (fresh window + `firstItemIndex` reset by construction — no scroll jump). The daily W6/W7 pivots deliberately IGNORE the filters (they own their own plant-source filtering); the params just ride along in the URL so switching back to the ledger restores them. `searchParams` is now typed `Record<string, string | string[] | undefined>` with a local `one()` helper for the scalar params. |
| `production/ledger-url.ts` | **Pure URL axis helpers (NO `'use client'`)** — the single source of truth for the two enum axes, shared by the server page + the client toolbar controls without a client/server boundary hazard. Exports **VIEW** (`VIEW_MODES`/`ViewMode`/`VIEW_MODE_LABELS`/`parseViewMode` (legacy `?view=daily`→`daily-w6`)/`plantViewOf` (→`'W6'\|'W7'\|null`)) and **SCOPE** (`SCOPES`/`Scope`/`parseScope` — `endless` default + omits param, only `focus` is written). Consumed by `page.tsx` (server branching), `production-ledger-grid.tsx`, and `ledger-controls.tsx`. **FILTER axis (2026-08-03) lives here too** — the module now owns all THREE axes. Exports: `FILTER_COLUMNS` (`shift`/`grade`/`plant`/`whse`/`source`/`ccc`), `FILTER_PARAMS` (→ `?shift=&grade=&plant=&whse=&src=&ccc=`), `FILTER_NULL` (`__NULL__` sentinel for a blank cell), `LedgerFilters` (per-column `readonly string[]`; **empty array = no filter = show all**, and the param is OMITTED), `EMPTY_LEDGER_FILTERS`, `parseLedgerFilters(get)` (takes a plain getter so the SERVER page passes `(k)=>searchParams[k]` and the CLIENT passes `(k)=>useSearchParams().get(k)` — no React, no Next import), `writeLedgerFilters(sp,f)` / `deleteLedgerFilters(sp)`, `countActiveFilterColumns` / `hasActiveFilters` / `ledgerFilterKey` (order-insensitive fingerprint — the endless sheet's React key) / `describeActiveFilters` (empty-state copy), the **canonical option catalog** `FILTER_SPECS` (built from the `../types.ts` lookup constants — NOT from loaded rows) + `mergeDiscoveredOptions` / `mergeDiscoveredGroups` (append any present-but-unmapped value so nothing is unreachable) + `collectFilterPresence(rows)` (drives DIMMING of options with no rows in view), the client matcher `matchesLedgerFilters(row, filters)` over the structural `FilterableEventFields`, and **`cccFlecPredicateParts()` — the ONE place the CCC/FLEC one-column ⇄ two-DB-field split is expressed** (see "CCC/FLEC filtering" below). `actions.ts` (a `'use server'` module) imports the filter types + `cccFlecPredicateParts` from here, which is why this file must stay pure. |
| `production/ledger-controls.tsx` | Client — the shared toolbar **axis controls**: **`ViewModeSwitcher`** (segmented `?view=` control, moved here from the grid) + **`ScopeToggle`** (NEW — Endless/Focus segmented `?scope=` control, Infinity/Crosshair icons). Each writes ONE param while PRESERVING the others (view/scope/year/batch) via `useSearchParams` spread, so a scope switch keeps your view + period and a view switch keeps your scope + period; `ScopeToggle` also clears the legacy `?focus` param on any change. Rendered in BOTH the endless-sheet toolbar and the editable-grid toolbar so the axis framework reads identically across scopes. `useTransition` pending spinner. **Both accept `disabled`/`disabledHint`** (Phase 3b dirty-nav guard) — when an editor has unsaved changes the switchers dim + block navigation with a hint, matching the period picker's existing pattern. **`useLedgerFilters()` (2026-08-03)** — the client entry point to the FILTER axis, used by BOTH the focus grid and the endless sheet. Reads the six filter params, writes them with `router.replace` preserving every other param, and returns `{filters, activeCount, isPending, setColumn, clearAll}`. It keeps an **optimistic local mirror** of the pending selection (a URL write is a server round-trip; a checkbox that only ticks 300ms after the click reads as broken) — so `filters` is what the MENUS render, while the endless sheet's ROWS come from the server-applied `filters` PROP and `isPending` drives the spinner between them. Any URL change (including Back/Forward) drops the mirror, so the URL always wins. |
| `production/column-filter-menu.tsx` | Client — **the ONE multi-select filter menu** every filtered column renders, in every surface (focus grid header, endless frozen header, mobile filter sheet) so the surfaces can never drift. Built on `Popover` + `Command` (cmdk — already a dep; no new packages). **`ColumnFilterMenu`**: checkbox rows, `All`/`Clear` header actions, an optional typeahead (`searchable` — on Source + CCC/FLEC), a hover-/focus-revealed **`only`** button per row (the Excel/Sheets single-value idiom, kept as an EXPLICIT control so the primary row click stays a plain toggle), one-click **group toggles** for grouped columns ("All crushers" ⇄ "Clear Crushers"), options with no rows in the loaded window rendered **dimmed + "none here"** (never hidden, never disabled — in endless scope the value may exist further back and the query-side filter WILL find it), unmapped discovered values badged `new`, and a footer stating the current effect. Writes back in the canonical option ORDER so the URL is stable regardless of click sequence. Trigger = the same tiny chevron the old single-select used + a count chip, so the `h-8` header row does NOT grow (`showLabel={false}` on mobile, where the row already shows the column name). Also exports **`FilterSummaryChip`** (toolbar "Filters · N ✕") and **`FilteredEmptyState`** (names WHICH filters are responsible + Clear). |
| `production/actions.ts` | `'use server'` — **`fetchCenaproPeriods()`** (DISTINCT `(batch_year, batch)` from the view, deduped + sorted NEWEST-FIRST — year desc, then calendar-month desc; exports `CenaproPeriod`); **`fetchProductionEvents(period?)`** (reads `public.cenapro_production_events` for ONE period via `.eq('batch_year').eq('batch')`, newest-first by recv_date; returns `[]` when no valid period); **`fetchLedgerPage(input)`** — the **endless-sheet keyset pager** (canonical order `recv_date ASC, id ASC`; input is either an anchor (`{kind:'latest'}` DESC-then-reverse, or `{kind:'period'}` resolve first row + load forward inclusive + probe hasOlder) or a `{cursor,direction}` (`older`/`newer`) composite-keyset page via `.or('recv_date.<op>.D,and(recv_date.eq.D,id.<op>.I)')`, page size 100, returns `{rows, hasOlder, hasNewer, notice?, error?}`; reuses the EXACT single-string-literal `.select(...)` as `fetchProductionEvents`; exports `LedgerAnchor`/`LedgerCursor`/`LedgerPageInput`/`LedgerPage`). **Both input modes now carry an optional `filters: LedgerFilters`, applied by the module-local `applyLedgerFilters()` to EVERY branch — anchor-latest, the period anchor's first-row resolution, the period forward load, the `hasOlder` count probe, cursor-older and cursor-newer** — see "Endless filtering is a QUERY, not a view" below); **`fetchDailyPivotWindow(input)`** — the **endless W6/W7 DAY-windowed keyset pager** (Phase 2B). Paginates by WHOLE `prod_date`s (never a partial day — a page = the next `DAILY_WINDOW_DAYS`=12 COMPLETE days). Input is an anchor (`{kind:'latest'}` newest N days, or `{kind:'period'}` = the period's first plant-source prod_date then forward + probe hasOlder) or a `{cursor:prod_date, direction:'older'|'newer'}` day-cursor. **Filters the plant source set (`SOURCE_SETS[plant]`, FLEC/DVO excluded) SERVER-SIDE** so a window never ships useless rows AND every returned distinct `prod_date` yields exactly one day-block (day counting for the client `firstItemIndex` stays exact). Two-step: distinct prod_dates (dedupe an ordered single-column read) → all events for those dates (`.in('prod_date', dates)`, oldest-first `prod_date/recv_date/id`). Returns `{events, hasOlder, hasNewer, notice?, error?}`; exports `DailyPivotWindowInput`/`DailyPivotWindow`; imports `SOURCE_SETS`/`PlantView` from `production-sources.ts`; **and `saveProductionEvents(dirtyRows, deletedIds)`** (upsert + delete through the auto-updatable view; coerces strings→number/date, strips empties→null, NEVER sends `unique_tag`; `revalidatePath('/cenapro/production')` — re-fetches the current URL period). Exports `ProductionEventDirtyRow` — which now carries an **OPTIONAL `batch_year?: string`**: normally omitted (the base trigger derives `batch_year` from `YEAR(recv_date)`), but the endless pivots send it explicitly per new pull / re-tag (= the pull's own prod_date year, or the original event's year on a re-tag) so a cross-year pull lands in its OWN period; the coercion sends it only when parseable. |
| `production/production-endless-sheet.tsx` | Client — **the Endless Sheet (Phase 1, READ-ONLY)**: ONE continuous, virtualized view of the ENTIRE `cenapro_production_events` history, **oldest-first** (`recv_date ASC, id ASC`), lazy-loaded bidirectionally. Built on **`react-virtuoso`'s `TableVirtuoso`** (new dependency) — chosen for its **`firstItemIndex`** prepend-anchor mechanism (the hook decrements it by the prepended count IN THE SAME state batch as the row prepend → viewport stays pinned, no scroll jump, while older history streams in above). `startReached`→`fetchOlder`, `endReached`→`fetchNewer`; `initialTopMostItemIndex = rows.length-1` for `latest` (opens at the BOTTOM = newest), `0` for a period (opens at its first row). **Columns/widths/formatting MIRROR the ledger** (same colgroup `36/96/96/120/64/80/84/108/84/104/112/72/72`, min-width 1228, 12 data cols `# · recv · prod · batch · shift · grade · plant · whse · source · weight · CCC/FLEC · flec · side`) by REUSING the ledger's **exported** display helpers (`toGridRow`/`rowDirection`/`rowDirectionTint`/`rowDirectionFrozenTint`/`cccFlecBadgeClass`/`plantBadgeClass`/`BADGE_BASE`/`formatKg`) + `formatDateShort` from `@/components/shared/grid` — read-only cells, NO edit/selection. **Frozen 4 identity columns** (`.frozen-col`/`.frozen-corner`/`.frozen-edge`, cumulative left `0/36/132/228`) + **opaque frozen header** via `fixedHeaderContent` (custom `TableHead` = `frozen-row bg-muted`, z-index bumped to 20 so it out-ranks the z-10 frozen body cols). Custom `Scroller` adds `overflow-x:auto` (never-crush). **Month separators** are 1:1 with data rows (NO separator items → zero `firstItemIndex` drift): a boundary is derived inside `itemContent` by comparing a row to its predecessor, then rendered AS PART of that row — a `border-t-2 border-t-primary/40` across the row + a slim month label stacked in the always-visible frozen Recv cell. **NO sort/filter controls** (locked decision — those stay in Focus scope). Thin absolute "Loading earlier/newer entries…" affordances at the edges (NO row animations). **Toolbar = `CenaproPeriodPicker` + the shared `ViewModeSwitcher` (mode="ledger") + the shared `ScopeToggle` (scope="endless")** from `ledger-controls.tsx` (the old local `LedgerModeToggle` was removed) — so a View or Scope switch here `router.replace`s the URL and the server re-routes to the matching combo. loadError → persistent copyable banner (HARD RULE). Mobile: desktop-first — horizontal-scrolls (never-crush min-width) on phones, doesn't crash; the full phone card experience stays in Focus scope's `MobileCardList`. **DRAFT ENTRY ZONE (Phase 2A):** a toolbar **lock/unlock toggle** (UI-only state, NOT URL — "Add rows"/"Unlocked"); locked (default) = the read-only sheet bottoms out at the newest committed row, unlocked = a maintained pool of IN-LIST BLANK draft rows is appended below the last committed row IN THE SAME virtuoso list (you scroll DOWN into an effectively-infinite supply). **Blank-pool growth = TWO orthogonal mechanisms (refined 2026-07-22):** (1) **minimum-buffer maintenance** (`ensureBlankBuffer`, on the draftRows effect + on paste) keeps ~25 trailing blanks past the last non-blank draft (so there are always blanks before any scroll, and an Excel paste taller than the pool auto-extends) — idempotent, no-op once full; (2) **scroll-growth** — every genuine bottom-reach appends a fresh `BLANK_GROW_BATCH` (25) of blanks UNCONDITIONALLY (decoupled from typed content), so scrolling down ALWAYS reveals more rows with no visible cap. Scroll-growth is driven by **`atBottomStateChange`, NOT `endReached`** (the old `endReached`+buffer logic hit a hard floor at ~25 rows because trailing already ≥ trigger → never grew; and with a bottom overscan `endReached` re-fires as appended blanks settle → would runaway). **Anti-runaway guard:** a `growGuardRef` set on append, cleared only when the operator scrolls back UP off the bottom (`atBottom → false`); since `atBottomStateChange` fires only on a true↔false TRANSITION and one append (25 rows ≈ taller than the overscan) shoves the end far below the viewport → `atBottom` flips false → guard resets, one bottom-reach = exactly one append, and the next batch needs a fresh scroll-down (no append → re-render → re-append loop). `endReached` now only pages newer COMMITTED history via `fetchNewer` (a no-op at the true-latest drafting edge). **Save/Discard controls (relocated 2026-07-22):** live IN THE TOOLBAR beside the "Add rows"/"Unlocked" toggle — a primary **"Save N rows"** + a ghost **"Discard all"** (→ `AlertDialog` confirm, the ONE destructive action) — shown only when `unlocked && draftCount > 0`. The old floating glass Save bar at the bottom was REMOVED (the toolbar is now the single home for these controls). **Chrome-only motion polish (2026-07-22, CLAUDE.md hard rule — ZERO row/draft-row animation):** the Save/Discard group `animate-scale-in`s on unlock; the Save button's draft count `animate-badge-pop`s when it changes (keyed `<span>`); an unlocked "N ready"/"Type or paste below" hint `animate-fade-in`s; a post-save "Saved N rows" toolbar cue `animate-fade-up`s then auto-clears (2.5s); the toggle + buttons carry `transition-colors duration-150` hover/state polish. All motion is on toolbar chrome — the virtualized committed rows AND the draft rows keep only the pre-existing `transition-colors` hover (allowed) and get no entrance/stagger/fade (they recycle). Draft rows live HERE (state) and are **mirrored to `localStorage`** (`cenapro-ledger-drafts:v1:<user-id>`, debounced 300ms, restored on mount, cleared ONLY on confirmed Save/Discard) so nothing can destroy them (the modal lost ~20 rows repeatedly — BUG-012). **Single "Add rows" button (one action):** clicking it — if NOT at the true bottom (`hasNewer === true`, e.g. you jumped to an old month) first `reset({kind:'latest'})`s + scrolls to bottom, THEN reveals blanks; if already at the bottom just reveals blanks — either way you end unlocked at the true append edge (drafts NEVER append to a mid-history window). No separate "jump to latest" affordance. Draft data lives in the parent-owned `draftRows` array (keyed by position) rendered through virtuoso `itemContent` — recycling-safe; `firstItemIndex` (top prepend) and the bottom blank-append are orthogonal. Save validates via `mapBulkRowToDirty` (persistent copyable `errorToast` + red rails on error), commits via the EXISTING `saveProductionEvents(dirtyRows, [])`, then calls the hook's **`refreshNewest()`** so saved rows appear at the bottom without a full reload + `scrollToIndex('LAST')`. A one-shot `?add=1` URL intent (from the focus toolbar's "Add rows in the sheet →" affordance) auto-unlocks on arrival then strips itself. **RESTORE IS EXPLICIT + CONSENTED (2026-07-22, no silent auto-unlock):** on reload with ≥1 non-blank draft in localStorage the sheet loads in its NORMAL LOCKED read-only state (drafts are restored into `draftRows` state but stay DORMANT — blanks/drafts only render when unlocked), and a small non-blocking inline **Resume/Discard prompt** (`resumePrompt` state, an `animate-fade-up` `bg-card` banner under the toolbar — NOT a modal, NOT a toast) shows **"You have N unsaved draft row(s) from a previous session."** (N = non-blank restored count). **Resume** → `setUnlocked(true)` + scroll to the append edge (via `pendingScrollBottomRef`) + dismiss, dropping the operator back into editing with drafts intact; **Discard** → clear drafts (`setDraftRows([])` + explicit `clearDraftMirror()` → the mirror else-branch also `removeItem`s the key) + stay locked + dismiss; an **✕** = "later" (keep drafts, stay locked). The `?add=1` intent is REMEMBERED on the prompt (`wantAdd`): `?add=1` + NO drafts → unlock directly (no prompt); `?add=1` + drafts → still show the prompt, and there Discard clears the stale drafts AND unlocks fresh for adding (so an explicit add-intent never dead-ends). `useAuth()` supplies the user-id key. **INLINE EDIT OF COMMITTED ROWS (Phase 3a):** under the SAME unlock, committed rows become inline-editable — the endless sheet is now ONE unified editing surface (drafts + committed) over ONE coordinate space (`totalRows = committed.length + draftRows.length`; committed rows occupy coords `[0..C-1]`, drafts `C+draftIndex`, which equals the row's data-index into virtuoso's `items`). The shared grid hooks (`useCellSelection`/`useGridKeyboardNav`/`useGridEditSession` + a custom region-routing paste) address BOTH regions; `getCellValue`/`updateRow`/`removeRow`/`commitDateCell` route by the region boundary `C`. **Recycle-/prepend-safe pending state (non-negotiable):** modified committed cells live in a PARENT-OWNED `editedRows: Map<eventId, BulkRow>` (the FULL merged row per id, so an edit is self-contained + saveable even for an off-window / not-loaded row) and deleted committed rows in `deletedIds: Set<eventId>` — BOTH keyed by the stable event `id`, NEVER in a recycled virtual row's local state, so a row edited → scrolled off → back rehydrates its pending value from the map by id (prepending older pages shifts array indices but not ids). Committed rows render editable via `CommittedRowCells` (draft-entry-zone) reusing the SAME `EditableDataCells` the drafts use, so unlocked committed + draft rows are visually/behaviorally identical (a flat, non-frozen spreadsheet); LOCKED committed rows stay byte-for-byte the read-only frozen `renderCommittedCells` (dormant restored edits do NOT paint until unlocked). A per-row **Delete/Restore** control lives in the committed row's # cell (hover-revealed trash → strike-through + rose rail; flips to Restore) — chosen over a right-click menu to match the drafts' hover-× and avoid wiring a context menu into the virtualized list. **Unified dirty model + ONE Save bar:** counts span new + modified + deleted (`totalDirty`, compact "3 new · 2 mod · 1 del" label); Save builds ONE payload — drafts (no id → INSERT) + modified committed (with id → UPDATE, built from the `editedRows` map directly) + `deletedIds` — through the EXISTING `saveProductionEvents(dirtyRows, deletedIds)` (validation via `mapBulkRowToDirty` for both; error keys `d:<draftIndex>` / `c:<id>` → red rails + persistent copyable `errorToast`); Discard clears all three (+ mirror). **Post-save refresh:** drafts always land at the newest end → `win.reset(anchor)` + scroll to the bottom entry edge; a committed-ONLY change → the new **`win.refreshWindow()`** refetches the loaded window in place + re-anchors the viewport near the pre-save top row id (a `recv_date` edit may relocate the row in canonical order — accepted; the fresh server read never crashes on a relocation). **localStorage mirror is now v2** (`cenapro-ledger-drafts:v2:<user-id>`, shape `{ drafts, edits, deletes }`) — covers pending edits + deletes too; the Resume/Discard reload prompt counts new + modified + deleted. |
| `production/use-ledger-window.ts` | Client hook — **`useLedgerWindow(initialPage, filters?)`**: the endless sheet's self-contained bidirectional keyset pager (NO TanStack Query). Seeds from the server-prefetched `initialPage`; holds the flat `rows` window + `firstItemIndex` (OWNED here so the prepend + index decrement batch together), `hasOlder`/`hasNewer`, `loadingOlder`/`loadingNewer`, `notice`; exposes `fetchOlder()` (prepends the page before the window's first row + decrements `firstItemIndex`), `fetchNewer()` (appends after the last row), and `reset(anchor)` (drops + re-seeds). Cursors derive from the first/last row of the current window; per-direction guards (refs) ignore concurrent/exhausted calls; de-dups by `id` defensively. Calls the `fetchLedgerPage` server action directly; errors → `errorToast`. **The active `filters` are held in a ref and passed to EVERY `fetchLedgerPage` call** (older / newer / reset / refreshNewest / refreshWindow) — miss one and the keyset walk silently drifts back to unfiltered history. **`refreshNewest()` (Phase 2A):** walks forward from the current tail (local cursor, past the `hasNewer` guard, bounded loop, de-duped) appending every row newer than the window — called AFTER a draft Save so freshly-committed rows land at the bottom without a full reload (`revalidatePath` refreshes only the server tree, not this in-memory pager); re-seeds from `latest` if the window is empty. **`refreshWindow()` (Phase 3a):** re-pulls the CURRENTLY-loaded window fresh from the server after a Save that touched COMMITTED rows (inline edits / deletes can live on ANY loaded page, and a `recv_date` edit can relocate a row in canonical order, so append-only `refreshNewest` isn't enough) — re-seeds from the current top row's period anchor (falls back to `latest`), then re-paginates forward (bounded loop) to restore the prior loaded depth, keeping the viewport near where the operator was editing. Crash-safe: a fresh server read, never an in-memory merge of stale overlays, so a relocated/vanished row just lands in its new spot (or falls outside the window). |
| `production/period-picker.tsx` | Client — **local** Year + Batch picker (`CenaproPeriodPicker`) for the ledger toolbar, mirroring the ICTC production picker but URL-driven (no shared cross-tab context — Cenapro is a single page). Changing either select `router.replace`s `?year=&batch=` inside a `useTransition` (shows a spinner) → server re-fetch of that period. **Preserves the other axis params (`view`/`scope`)** so a period jump keeps the current view + scope (jump-to anchor in endless, clamp selector in focus — the same control). Year change snaps to that year's newest batch if the current batch is absent. **No "All periods" option** (that's the slow path this removes). Disabled (with a hint title) while the grid has unsaved edits. |
| `production/production-view.tsx` | Client — thin wrapper: `router.refresh()` on save success, and keys `<ProductionLedgerGrid>` by a fingerprint that folds in the **selected period** + the server-row ids, so a successful save OR a period switch remounts against the scoped/saved truth (fresh ids + computed `unique_tag`/`batch_year`, dirty state cleared). |
| `production/production-ledger-grid.tsx` | Client — the **editable** dense Excel-style ledger (one VIEW row = one grid row, keyed by stable `id`), scoped to one period. **12 data columns** (Excel order): `# · recv · prod · batch · shift · grade · plant · whse · source · weight · CCC/FLEC · flec · side`. Inline editing via shared `GridCell` + cell hooks (`useCellSelection`/`useClipboardCopy`/`useCellDelete`); `DatePickerCell` for recv/prod dates; **`SelectCell`** (a DropdownMenu cell) for the categorical columns (shift/grade/plant/whse/source/side); numeric `<Input>` for weight/flec; and the **single `CCC/FLEC` typeahead cell** — a paste-friendly `GridCell` + `<Input list=ledger-ccc-flec-suggestions>` (NOT a dropdown) holding the raw Excel value (`FLEC`/`Cn`/`RKn`). That merged cell is stored as a raw-text `ccc_flec` field on the grid row (seeded from the two DB fields via `formatCccFlec` on load, edited directly so typing isn't fought by a derive) and **parsed back into `disposition_kind` + `partner_equipment_code` at SAVE via the shared `parseCccFlec`** — an unrecognized value is rejected with a persistent `errorToast` (HARD RULE) naming the row(s); a single typed cell can't make an inconsistent pair, so the old equipment-dependency dance / separate Disposition+Equipment columns are gone. **Rows are rendered via a `React.memo`'d `ProductionRow`** fed PRIMITIVE slices of the global active/selection state + stable `useCallback` handlers, so editing/selecting one cell re-renders only the affected rows. Dirty tracking (`existing`/`new`/`modified`/`deleted` with amber/blue left borders), **NO trailing always-empty new row** (adds happen via the right-click Insert Above/Below context menu, or via the endless sheet's draft entry zone — the retired Bulk Add modal's replacement; paste-into-the-grid may still auto-extend rows), paste-from-Excel (TSV — `ccc_flec` lands as raw text), right-click context menu (Insert Above/Below, Duplicate, Delete/Restore), single Save/Discard. **The 4 leftmost identity columns (`# · Recv · Prod · Batch`) are FROZEN (Excel freeze panes)** — sticky-left with cumulative px offsets (left: 0 / 36 / 132 / 228 from the colgroup widths 36/96/96/120), so they stay pinned when scrolling right through the wide grid; the last frozen column (Batch) carries the canonical `.frozen-edge` anti-seam right divider. Frozen surfaces follow the canonical Frozen Panes pattern (CLAUDE.md + globals.css) — **fully OPAQUE**, never glass: header/corner `bg-muted` solid (`.frozen-corner`), body `bg-background` base + the row's opaque IN/OUT tint (`.frozen-col`), so scrolling content can't bleed THROUGH the pinned cells; z-scale stacks frozen header corners (z-30) > scrolling header row (z-20) > frozen body col (z-10) > normal cells. The empty state shows when the selected period has **zero data rows**. **Header filters = Shift / Grade / Plant / Warehouse / Source** (options distinct-in-data; Disposition filter removed). **Both the Recv and Prod date headers are clickable to sort** (`DateSortHeader` — click a date header to make it the sort key, click again to flip asc/desc; the up/down chevron marks the active key, an idle `ChevronsUpDown` the other) — in-memory, default newest-first by recv_date; the old toolbar "Date" button is gone. Toolbar hosts the **`CenaproPeriodPicker`** (left) + an **"Add rows in the sheet →" affordance** (`goAddInSheet`) that replaced the old "Bulk Add" button — it `router.push`es to endless+ledger dropping `year`/`batch` (→ `latest` anchor) with a one-shot `?add=1` so the endless sheet opens UNLOCKED at the true-latest append edge with the loss-proof draft zone ready; disabled while the grid `isDirty`. `SelectCell` + `DatePickerCell` are now IMPORTED from `@/components/shared/grid` (Phase 2 of the Blackwood Table consolidation — the local copies were promoted to the shared barrel in Phase 0 and deleted here; byte-identical, no drift). Still-local: `ColumnFilterMenu` + `DateSortHeader` + `ProductionRow`. **Cell selection / inline edit / keyboard nav / paste** run on the shared Blackwood Table hooks — `useGridKeyboardNav` (coordinate resolver wrapped to clamp ArrowLeft→col 1 and to gate inline editing to typable columns via a custom `isEditable`; Home/End handled in a container wrapper), `useGridEditSession` (with a CUSTOM revert that un-dirties a reverted modified-existing row), and the existing `useCellSelection`/`useClipboardCopy`/`useCellDelete` wired into the hook's `range` slot; `enableEnterAnchor: false` (plain Enter drops down). The grid keeps its OWN `handleSmartPaste` (paste must flip pasted existing rows to `modified`). The right-click row context menu now uses the shared `useGridContextMenu` hook + declarative `GridContextMenu` component (Phase 4 of the Blackwood Table consolidation — viewport edge-flip + close-on-outside/Esc + menu styling all lifted into the shared primitives; see `components/shared/grid/CONTEXT.md`). Items are a `GridMenuItem<number>[]` keyed on `rowIdx` (Insert Above/Below, Duplicate, separator, Delete/Restore); Delete (destructive) and Restore (muted) are TWO items gated by `hidden` on the row's deleted state, since the shared `variant` is static. **Visual styling (display-only):** the whole ledger is **bold** (`font-bold` on headers, data cells, and the edit `<Input>` so weight doesn't shift on F2). Each row carries a **bold row tint** keyed on the WAREHOUSE first, then the CCC/FLEC disposition — **priority: UNPLACED > WHSE 3 (DVO) > disposition** (via `rowDirection(row)`, which reads `warehouse_code` + the shared `parseCccFlec` — same source as save, so it re-tints live as you place/type): UNPLACED (warehouse blank) → no tint (an unplaced bagging row is no longer green until placed); WHSE 3 (DVO) → blue (`bg-blue-50 dark:bg-blue-950/40`) regardless of disposition; a real placed warehouse (WHSE 1/2/5/7) → bagged-IN (`flec_bagging`) → `bg-emerald-50 dark:bg-emerald-950/40`, any withdrawal (`partner_crusher`/`partner_kiln`) → `bg-rose-50 dark:bg-rose-950/40`; unrecognized at a real warehouse → no tint. (Two tint helpers: `rowDirectionTint` = the translucent scrolling-`<tr>` flavour above; `rowDirectionFrozenTint` = the OPAQUE flavour for the sticky frozen identity cells — `dark:` bumps to `-950/60`, blue included, so the pinned cells aren't see-through.) The tint is applied FIRST in the `<tr>` class so the dirty-state left borders (new=blue / modified=amber / deleted=strikethrough) + hover + cell-selection highlight all read on top. **Two columns render colored badges in display mode** (helpers `cccFlecBadgeClass` / `plantBadgeClass` + shared `BADGE_BASE`; the edit input stays a plain `<Input>` so typing/paste is unaffected) — punchier than before (`/20`–`/25` fill, `/40` border, bold `text-{c}-700 dark:text-{c}-300`): **CCC/FLEC** — `FLEC`→emerald, `C1`–`C4` (crushers)→amber, `RK1`–`RK4` (kilns)→rose, unrecognized→neutral; **PLANT** — `W6`→blue, `W7`→teal, `W6/W7`→indigo, `DVO`→slate, empty→plain (rendered via a `SelectCell` `renderTrigger` prop that styles ONLY the closed trigger; dropdown menu items stay plain text). Every badge color has a `dark:` text variant. No row animations (CLAUDE.md hard rule). **Axis toolbar:** the toolbar carries the shared **`ViewModeSwitcher`** (3-mode `?view=` segmented control) + the shared **`ScopeToggle`** (Endless/Focus `?scope=` control) — BOTH imported from `ledger-controls.tsx` (the grid's local `ViewModeSwitcher` was moved there in the Phase-1 reframe); the `ViewMode`/`parseViewMode`/`plantViewOf` helpers are imported from the pure `ledger-url.ts`. The grid reads its own `currentScope` from the URL (`?focus=1`→focus, else `parseScope('scope')`) and passes it to `ScopeToggle` so the toggle mirrors the real scope even in the endless+daily fallback. `?view=` legacy `daily`→`daily-w6`; `?view` defaults to `ledger`. When a daily mode is active: the period picker + switcher STAY visible but the editable-only controls (row count, Clear-filters, "Add rows in the sheet", Discard, Save) are HIDDEN and `<ProductionDailyBlock rows={initialRows} plantView={…} />` renders in place of the editable `<table>` (plus its context menu / datalist); the `loadError` banner still shows. The grid's hooks always run (only the JSX branches), and Ledger mode behavior is unchanged. **Mobile (Archetype C):** in Ledger mode the desktop `<table>` container is `hidden sm:block` and a `sm:hidden` **`<CenaproLedgerCardsMobile>`** renders instead on phones, fed a `mobileRows` memo (`rows` minus deleted/empty-new/filter-hidden — single source of truth) + the shared filter state/options/setters; the display-only badge helpers (`cccFlecBadgeClass`/`plantBadgeClass`/`BADGE_BASE`/`formatKg`) and the `GridRow` type are now **`export`ed** for that card file to reuse. The **toolbar is `flex-wrap`** so the period picker + view switcher + controls don't overflow at 375px (helps every view mode). See `production-ledger-cards-mobile.tsx`. |
| `production/production-ledger-cards-mobile.tsx` | Client — the **phone read layer for the LEDGER** (`?view=ledger` only; the daily-w6/w7 matrix is a later phase). Built on the platform **`MobileCardList<GridRow>`** primitive (**Archetype C** — dense desktop table → phone card list; see `components/shared/mobile/CONTEXT.md`). **ADDITIVE + read-only** — the whole editable layer (inline edit, Bulk Add, paste, context menu) stays desktop-only (click-to-select cells degrade on touch). The grid renders the desktop `<table>` `hidden sm:block` and this card list `sm:hidden`, fed the **SAME sorted/filtered rows** (`mobileRows` = the ledger's `rows` minus deleted/empty-new/filter-hidden — single source of truth, no refetch). **Card headline (≤6):** `recv · batch · shift/grade · source · weight` + a **CCC/FLEC disposition badge** and a **Plant chip** — status meaning is preserved via the SAME desktop badge helpers (`cccFlecBadgeClass` / `plantBadgeClass` over the shared `BADGE_BASE`, all now **exported** from `production-ledger-grid.tsx`, plus `formatKg` + the `GridRow` type — reused, not duplicated, mirroring how RC IN's `delivery-cards-mobile` imports from its sibling desktop table), NOT a bare row tint. **Detail bottom-Sheet:** every remaining field (prod date, batch_year, warehouse, side, flec count, weight, CCC/FLEC). **Mobile Filters Sheet** (2026-08-03: now MULTI-select — it renders the shared `ColumnFilterMenu` per column, driven by the SAME URL filter axis, with a per-row selection summary; props collapsed from 17 single-select fields to `filters`/`presence`/`activeFilterCount`/`setFilterColumn`/`clearFilters`) formerly reused the desktop single-select column filters (Shift/Grade/Plant/Whse/Source, `'ALL'`=off) via the SAME state + options + setters threaded from the grid (so mobile ⇄ desktop filter sets never diverge). **"View full table" escape hatch** mounts a read-only mirror of the ledger columns in a horizontally-scrolling Sheet (NOT the interactive grid). No ₱ anywhere in Cenapro → zero price gating. |
| `production/production-daily-block.tsx` | Client — the **EDITABLE Daily Block** view (Phase 1 + 2), a visually-styled rebuild of the boss's "PROD 2026" spreadsheet, rendered as **two plant variants** (`plantView: 'W6' \| 'W7'` prop — Daily W6 / Daily W7 view modes). **PROD-ONLY** — always groups by `prod_date` (the recv/prod toggle was removed; this is a production view by definition). Consumes the SAME period rows the ledger holds (`ProductionEventRow[]`). **Source filter (production OUTPUT only):** rows are filtered by `source_location_code` against `SOURCE_SETS[plantView]` before pivoting — **W6 = `{TNK 1, TNK 2, TNK 3, TNK 4, W6}`, W7 = `{W7}`**; **FLEC and DVO are EXCLUDED from BOTH** (inventory withdrawals, not factory output). `buildDateGroups(rows, plantView)` (pure) pivots into **PROD_DATE → SHIFT (M/E/N) → GRADE (3X50/2X6/3.5/4X8) → SOURCE → RECV_DATE** — **a LEAF row = one (source, recv_date) pull** (all events matching that 5-tuple aggregated into the equipment columns). The **SOURCE cell `rowSpan`s its recv-date rows**, so a source appears ONCE with its recv dates stacked **chronologically** beside it in a new **Recv Date** column (you can see how many pulls a tank had that prod date). Columns `Date · Shift · Grade · Source · Recv Date · C1–C4 · RK1–RK4 · Bagging · Sub-Total · Total`: equipment col = SUM(`weight_kg`) where `partner_equipment_code` = that code; **Bagging KEPT** = SUM where `disposition_kind='flec_bagging'`; **Sub-Total** = Σ partner; **Total** = Sub-Total + Bagging. **Only rollup = the per-DATE "Daily total" footer** (the grade subtotal row + all subtotal logic were REMOVED; a grade just lists its source→recv rows). **FIXED equipment columns (not adaptive):** all 8 (C1–C4, RK1–RK4) always render; blank cells blank (`0 → ''`). **Padded + outlined day blocks:** each day is padded with blank FILLER rows up to `MIN_DAY_ROWS` (=6, **tunable**) — filler rows sit after the last real row, before the footer; their numeric cells get a faint `bg-muted/30` so the area reads as a reserved/empty SLOT (not missing data), still on the uniform grid + inside the day box. **Excel-matched widths + density:** colgroup px (Date 76 / Shift 48 / Grade 100 / Source 140 / **Recv Date 80** / each crusher+kiln 56 / Bagging 66 / Sub-Total 70 / Total 84), table `width: max-content`, compact `px-1.5 py-0.5` / `text-[11px]`. **Complete 2-tier header (one solid bordered band edge-to-edge):** the **5 IDENTITY headers `rowSpan={2}`** so each fills the FULL header height (no empty white top-left), vertically centered, neutral muted bg, `.frozen-corner` (sticky top AND left, highest z, opaque) — labels are `Date · SHFT · Grade · Source · Recv Date` (the Shift header reads **`SHFT`**, not `Sh`; the real-row merged shift cell stays the big colored letter); the numeric side has the tinted group banner (Crushers/Kilns/Bagging/Totals) over the per-column code cells. **Layout — REAL merged cells via `rowSpan`:** DATE `rowSpan`s the day's REAL + FILLER rows (**NOT** the footer); SHIFT `rowSpan`s its shift's rows; GRADE `rowSpan`s its grade's leaf rows (`= leafCount`); SOURCE `rowSpan`s its recv-date rows. `dateRowSpan = dayBody + fillerCount`; the day has `dayBody + fillerCount + 1` `<tr>`s (the +1 footer is a separate full-width row) — **rectangular**. Merged identity content is **align-top** (label sits atop its block). Merged cells sticky-left (frozen) + OPAQUE. **THREE border weights, one token each (used everywhere — header, body, filler, footer):** **GRID** = uniform 1px `border-b border-r border-border` (solid, crisp) on EVERY cell, both axes (merged cells skip internal horizontal lines = intended merge look); **GROUP** = a 2px `border-l-2 border-l-border` divider between the column groups (Crushers | Kilns | Bagging | Totals), carried through header + body + filler + footer; **BOX** = the 2px day-block outline on the day's edge cells (`border-t-2` first row, `border-b-2` footer, `border-l-2` Date col, `border-r-2` Total col → survives horizontal scroll). **SOFT + NEUTRAL** — `border-border` (the SAME color as the gridlines, just 2px thick); the day box stands out by WEIGHT, not color (NOT `border-foreground`/black, which read as a harsh black outline on the perimeter cells). The whole table's outer perimeter is closed with a `border border-border` on the `<table>`. Clean card background, **no day tint**. **BOLD identity columns:** Date/Shift/Grade/Source/Recv Date cells render bold; numeric cells stay normal weight (the daily-total row is bold). **Frozen panes** — **5 identity columns** (Date/Shift/Grade/Source/**Recv Date**) + the 2-tier header, cumulative `left` offsets, `.frozen-edge` on **Recv Date**. **Full-width day footer:** a TRUE edge-to-edge totals row — a "Daily total" label cell `colSpan={5}` over ALL FIVE identity columns (sticky at `left:0`), then the equipment/bag/sub/total cells; ONE continuous solid `bg-muted` band (opaque across frozen cols), bold numbers, the box top + bottom edge + the group dividers. **Visual flair:** **shift = a BOLD colored letter** M amber / E violet / N indigo (NOT a filled pill — cannot overflow the 48px column); per-grade **chips** (3X50 sky / 2X6 teal / 3.5 amber / 4X8 rose); the group banner tints (Crushers amber | Kilns rose | Bagging emerald | Totals neutral) sit on the GROUP cells only. **No data-bars** (numbers render plain). Single container `animate-fade-in`, **NO per-row animation**. Empty state per variant ("No W6/W7 production output for this period"). **EDITABLE (Phase 1) — equipment/bagging NUMBERS on real pull rows, round-trip via the EXISTING `saveProductionEvents(dirtyRows, deletedIds)` (NO new backend):** the 8 equipment cols (C1–C4, RK1–RK4) + Bagging on REAL leaf rows are editable via the **canonical Blackwood Table click-to-select / type-to-edit model** (NOT always-on inputs anymore — see KEYBOARD NAVIGATION below): each renders a focusable STATIC cell by default and mounts the shared `EditInput` only while editing; **commit is EXPLICIT (registry-driven), NOT blur-driven** (see EXPLICIT-COMMIT below), Esc reverts; real-row identity cells + the footer stay read-only `<span>`s. Three ops: **EDIT** a value → UPDATE (dirty row WITH the cell's underlying event `id`, amber ring); **CLEAR** a value → DELETE (id → `deletedIds`, guarded by an `AlertDialog` confirm); **TYPE into a blank cell** → a **COLUMN-AWARE `Popover`** → stage an INSERT (dirty row WITHOUT id, blue tint). The popover always shows read-only **CONTEXT chips** (auto Plant W6/W7, prod date, shift, grade, target column e.g. "C3 · Crusher" / "Bagging") and branches on the column: **Crushers/Kilns (C1–C4, RK1–RK4)** → the only field is **Source** (a selector over `SOURCE_SETS[plantView]`, pre-filled to the row's source) — warehouse/side/flec are dropped (noise here, left null); **Bagging** → **Source + Warehouse** (`WAREHOUSE_CODES`, "— Unplaced"=null) + **Side** (`LS`/`RS`, nullable) + **Flec count** (optional). **Cell→event mapping:** the pivot records each cell's contributing event `id`(s) (`CellSlot.eventIds`) so UPDATE/DELETE target the exact event; the **COLUMN is the disposition** via `columnDisposition()` (C1–C4→`partner_crusher`+code; RK1–RK4→`partner_kiln`+code; Bagging→`flec_bagging`+null — same map as `parseCccFlec`/`partnerEquipmentOptions`, CHECK structurally satisfied). **INSERT dims** come from the leaf (recv_date/prod_date/shift/grade) + the popover's **Source** (defaults to the row's source) + `selectedPeriod.batch`, and **`plant_code` is AUTO-STAMPED = `plantView` (W6/W7)** so a new entry never shows a blank Plant in the Ledger; `batch_year` is NOT sent (the base trigger derives it from recv_date — correct for same-year periods; `ProductionEventDirtyRow` has no `batch_year` field, so the contract's cross-year override isn't expressible Phase 1 and is deferred). **COLLISION LOCK:** a cell whose 9-col pivot key maps to >1 event renders read-only with a 🔒 + "Multiple entries — edit in Ledger" (exactly 1 such cell in live W6/W7 data: MAY-2026 / TNK 2 / 3X50 bagging). **State + save:** `modified` (eventId→weight), `staged` (cellKey→INSERT dirty row), `deleted` (event ids); a Save/Discard bar (with a "N unsaved" count) appears in the toolbar when `hasChanges`; **Save** calls `saveProductionEvents(dirtyRows, deletedIds)` then `onSaveSuccess()` (→ the production-view `dataKey` remount / router.refresh); errors via `errorToast` (HARD RULE). Props `selectedPeriod` + `onSaveSuccess` are threaded from `production-ledger-grid.tsx`'s daily-block render. **EDITABLE (Phase 2) — NEW PULLS + NEW DAYS (same staged-INSERT → `saveProductionEvents` path):** the blank FILLER rows are **"new pull" input slots** (rendered FLAT, not merged): the identity cells are **Excel-style live typeahead** (`IdentitySuggestInput`, NOT a `<Select>` and NOT a native `<datalist>`) — **Shift** (`SHIFT_CODES`), **Grade** (`GRADE_CODES`), **Source** (`SOURCE_SETS[plantView]`). **Why a custom dropdown, not `<datalist>` (Issue 2a):** native `<datalist>` is unreliable for live typeahead — most notably it shows NOTHING for a single-char exact match (typing "M" into Shift is already a complete option) and its drop behavior is inconsistent across browsers (verified in the harness). `IdentitySuggestInput` renders the bare `EDIT_INPUT`-metric input (with `data-navid` so keyboard nav still finds it) PLUS a compact glass dropdown (`bg-popover/95 backdrop-blur-lg`, `animate-fade-in`) anchored under the cell: as you type it prefix-filters the valid set case-insensitively (space-insensitive, e.g. `tnk1`); **↓/↑** move the highlight (consumed — the grid does NOT navigate cells); **Enter/Tab WITH a genuine prefix match on a NON-EMPTY query (or an explicit arrow pick)** accept the highlighted option (`flushSync(onChange)` so the explicit commit reads the accepted value) then BUBBLE so the grid commits + moves (canonical Excel flow); an **UNMATCHED query (e.g. "XY")** shows NO dropdown and Tab/Enter commits the RAW text (it is NEVER silently snapped to a valid value); **click** an item accepts + commits; **Esc** closes the dropdown first (value kept), a second Esc bubbles → grid reverts to the snapshot. **CLEAR-TO-EMPTY (regression fix):** the auto-accept gate is `hasMatch = q !== '' && matches.length > 0` — it must be FALSE on an EMPTY query, otherwise `options.filter(o => o.startsWith(''))` = ALL options would auto-snap a cleared field back to the first option on Tab/Enter, so the operator could NEVER clear a Grade/Source/Shift identity cell. Emptying a cell + Tab/Enter (or selecting it + Delete/Backspace then committing) now commits EMPTY → `normalizeIdentity('')='' `→ `upsertDraft(slotKey, { shift|grade|source: '' })` clears that draft field. All three identity cells clear consistently (verified in a harness, W6 + W7). On commit the value is normalized against the allowed set (`normalizeIdentity`, case-insensitive, tank aliases `t1`/`tnk1`→`TNK 1`). **Recognized/tagged confirmation (Issue 2b):** the STATIC identity cell renders a committed value via `renderRecognizedIdentity(col, value, plantView)` — a value that IS a valid member of its set gets the SAME canonical treatment the REAL rows use (Shift → the colored `SHIFT_LETTER` letter + `SHIFT_LABEL` title; Grade → the `GRADE_CHIP` pill; Source → mono-bold), so the operator can SEE it tagged as the DB value; a non-empty value that is NOT recognized renders in a clear **unrecognized amber** style (`text-amber-600 dark:text-amber-400`, `title="Not a recognized value"`). `CellEditor` gained two optional props that power this — `renderEdit` (custom edit-mode element = the typeahead) and `renderValue` (custom static render = the recognized styling) — while still owning the static↔edit toggle + value-accessor registration. `identityOptions()`/`isRecognizedIdentity()` are the shared valid-set helpers. **Recv Date** is typed (normalized via `normalizeTypedDate` on commit) and starts **EMPTY** (placeholder only — NO prod_date prefill). Then editable equipment/Bagging weights. Committing a weight **validates the identity is complete** (shift + grade + source + recv) — if not, a gentle `toast.warning` and the value is kept but NOT staged. A complete-identity filler stages **one INSERT per non-empty weight column** (so C1 + C2 on one filler row = 2 events sharing the pull dims), with `prod_date` = the day, dims from the filler, `plant_code` = `plantView` (auto), batch from `selectedPeriod`, disposition/equipment from the column. **Crushers/Kilns** stage directly; **Bagging** opens a small warehouse/side/flec `BaggingMetaPopover`. Drafts live in a `drafts` Map keyed by a **STABLE, PARENT-OWNED, DETERMINISTIC slot id** (`${prodDate}#slot-{i}`, positional per day) — NOT a child `React.useId`. **This is the Issue-1 (duplicate-row) fix:** previously each `FillerRow` self-generated its draft id via `React.useId()` AND the parent rendered filled drafts FIRST (reordered ahead of the empty slots), so committing a blank slot made React MOVE the typed instance to a new index while ALSO mounting a fresh instance at the prepended draft index → two live rows shared one `slotKey` → duplicate `data-navid` → the typed value appeared in TWO rows with BOTH highlighted. Now `DateSection` assigns every slot (empty OR filled) a persistent positional id, passes it DOWN to `FillerRow` as the `slotKey` prop, and renders rows in **slot-id ORDER (not filled-first)** — `[...slotIds.map(sid → draftByKey.get(sid)), ...orphanDrafts]` — so a slot that goes empty→filled keeps its id AND its render position → React preserves the ONE instance (no second mount, no navid collision, no state bleed into a neighbouring slot). `slotCount = max(MIN_DAY_ROWS−dayBody, maxOccupiedDraftSlot+2, #filledDrafts+1) + drawer extras`, guaranteeing ≥1 trailing empty slot once a slot fills; a `FillerRow` effect clears its local identity state when its `draft` prop drops to null (Discard, which doesn't remount). Verified in a public harness: typing into a blank slot + Tab yields exactly ONE row with the value, ONE active ring, and ZERO duplicate `data-navid` (querySelectorAll count = 1), for both W6 and W7. `draftToDirtyRows()` converts a complete draft → its INSERT rows, merged into the same save payload. **Add-row drawer (hover-reveal):** the `MIN_DAY_ROWS` rectangle of empty slots stays as-is, but the previous **auto-grow-on-fill is REMOVED** — a small "+ Add row" pill at the top edge of the day footer's label cell (hidden until the day is hovered, `group-hover/day`, opacity-only, no layout shift) appends one input row to THAT day via per-day `extraRows` state (`slotCount = padSlots + extraRows`). **"Add day"** — a toolbar `AddDayButton` popover takes a typed date (normalized; validated as a real date, not already shown, within `selectedPeriod`'s month when resolvable) → spawns a NEW empty outlined day block (`addedDays`, sorted chronologically; empty added days save nothing). **Draft rows render flat** until save+refresh, when they become real merged rows. The **Date `rowSpan` covers real + draft + filler-slot rows** (footer separate) so the grid stays rectangular. `hasChanges`/"N unsaved" include draft-derived inserts; **Discard** clears all edit + draft + added-day state. **Excel-fidelity cell behavior (Phase 2.1):** (a) **identity typeahead** (custom `IdentitySuggestInput` dropdown, above); (b) **recv blank = empty** (no prefill); (c) **placeholder** is visible when empty+unfocused but **vanishes the instant the cell is focused** (placeholder attr blanked on focus, restored on empty blur) on every editable identity/recv/weight input; (d) **uniform row height** — a single `ROW_H` (`h-7`) on EVERY `<tr>` (real data, filler/input, AND the daily-total footer — its label dropped `py-1`→`py-0.5`), all cells `align-middle`; (e) **no height expansion on edit** — the STATIC cell render AND the shared inline `EditInput` both use `EDIT_INPUT` (`text-[11px]` / `px-1.5` / `leading-none` / `bg-transparent` / no border/ring/outline / `box-border` / `h-full` / `[min-height:0]` / `appearance-none`), so switching to edit shows only the caret and never grows the row. **CELL-HOST HEIGHT (regression fix):** because `EDIT_INPUT` uses `h-full` (height:100%) and a percentage height only resolves against a parent with a *definite* height, every `<td>` that hosts a `CellEditor` (the real-row + filler weight cells' `baseClass`, the filler identity `cellBase`, and the filler Recv cell) carries an explicit **`h-7`** — otherwise the `StaticCell` `<div>` collapses (0px when empty, ~11px when filled) inside a content-height `<td>`, leaving the cell visually present but **un-clickable / un-selectable** (a real click lands in the dead band on the bare `<td>`, which has no handler — the exact symptom of the "single-click does nothing" bug). The explicit `h-7` makes `h-full` resolve to the full cell height (the div fills the cell, fully clickable) **without** growing the row past 28px. **KEYBOARD NAVIGATION + CLICK-TO-SELECT (shared Blackwood Table primitives — Phase 3 retrofit):** the Daily Block now uses the SAME canonical interaction model as every other grid via the shared hooks `useGridKeyboardNav` + `useGridEditSession` + the shared `EditInput` (`@/components/shared/grid`, `@/lib/hooks/use-grid-keyboard-nav`, `@/lib/hooks/use-grid-edit-session`). **Two states per cell:** `activeNavId` = the SELECTED cell (active ring), `editingNavId` = the cell currently in EDIT mode. **Every editable cell renders TWO ways:** by default a focusable **STATIC** element (`StaticCell` — a `<div tabIndex=0 role=gridcell>` matching the `EDIT_INPUT` metrics, carrying `data-navid="${rowKey}|${colKey}"`); it mounts the shared `EditInput` ONLY when `editingNavId === navid`. The `CellEditor` wrapper owns the static↔edit toggle and registers each cell's thin **get / set / commit** accessors with the grid (for snapshot/seed/revert AND explicit commit) — each cell still owns its OWN value state + commit logic, so the entire save path (onEditWeight / onStageInsert / draft upserts / InsertPopover / BaggingMetaPopover) is untouched. **EXPLICIT-COMMIT (regression fix — the Save bar never appeared):** the cell's real commit is run EXPLICITLY through the registry (`commitCell(navid)`), NOT via the editing input's `onBlur`. In React 19 / Next 16 the focus/blur (`focusout`) handler is dropped when the editing input UNMOUNTS in the same render batch — and the grid unmounts the input the instant it moves focus (Tab/Enter) or switches the active cell (click-away), so the onBlur-commit silently never fired (no draft pushed, no `modified`/`deleted` set → `dirtyCount` stayed 0 → no Save/Discard bar, for EVERY edit kind: new filler pull, existing-weight edit, and clear-to-delete). Now `edit.commit` (Tab/Enter) and `onActivate` (click-away to another cell) BOTH call `commitCell(editingNavId)` before clearing edit mode; `editingNavIdRef` (a ref mirror of `editingNavId`) is read in those callbacks so the commit isn't a side effect inside a state updater (StrictMode-safe). The input's `onBlur` is kept only as a harmless idempotent fallback. **CANONICAL gestures:** **CLICK** (mousedown) on a static cell = SELECT only (ring, NO edit) + focus; **TYPE a printable char / F2 / DOUBLE-CLICK** = begin editing (type-over seeds the char); **ESC** = revert to the pre-edit snapshot (the EditInput's `escapedRef` suppresses the blur-commit), stay selected; **ENTER / TAB** = commit (the cell's real commit fires EXPLICITLY via `commitCell` in `edit.commit` — NOT via onBlur, which is dropped on unmount; see EXPLICIT-COMMIT) + move; **ARROWS / TAB / ENTER** (not editing) = navigate. Nav uses `createDomOrderNavResolver` (`containerRef`=scroll container, `navColOrder`=`NAV_COL_ORDER`, `parseId`=`parseNavId`, `findColInAdjacentRow`=a thin adapter reproducing the same-lane-preference / nearest-by-`NAV_COL_ORDER` fallback against the shared `DomNavCell[]` signature). The resolver reads **`[data-navid]`** (NOT `input[data-navid]`) so it finds the STATIC cells too — this is what makes click-to-select navigate. `onAfterMove` focuses the resolved `[data-navid]` element via `CSS.escape` so keyboard nav follows visually. **Enter-anchor** is provided by the shared hook (`enableEnterAnchor: true`, `laneOf` = colKey): the first Tab of a run records the colKey and a later plain Enter returns to it on the next row. **Navigation SKIPS read-only/locked cells** automatically — collision-locked weight cells (🔒) and real-row identity cells render as non-input `<span>`s with **no `data-navid`** → absent from the resolver's cell list (and `resolver.isEditable` is false for any element bearing `data-locked`); the footer/headers are never navigable. Click + keyboard share ONE active-cell model (`activeNavId`, set by a static cell's mousedown/focus → `onActivate`, which also exits any OTHER cell's edit). **ACTIVE-CELL HIGHLIGHT (matches the ledger):** the cell whose `data-navid === activeNavId` gets `ACTIVE_RING` = `ring-2 ring-primary ring-inset` (the SAME token the ledger's selected cell uses in `production-ledger-grid.tsx`) + `z-20`. An INSET ring is a box-shadow → **NO layout space**, so the highlighted cell stays the exact same height as every other (honors the no-expand rule). **Static — no transition** (CLAUDE.md cell-selection rule). It sits ON TOP of any dirty tint — the later `ring-*` wins ring color/width while the dirty `bg-*/10` fill remains, so both read together (e.g. an active amber-modified cell = amber fill + primary ring). `z-20` lifts the active cell ABOVE the z-10 frozen columns (`.frozen-col`/`.frozen-edge`) so the ring is never clipped behind a sticky neighbor on horizontal scroll; it follows clicks AND keyboard nav. Applied to all editable cells (weight on real+filler, filler identity/recv); locked/read-only cells never become active so never highlight. **Bagging + popover during nav:** Tab/Enter into a bagging weight commits; a positive value opens the warehouse/side/flec popover (Radix moves focus into it) — the popover briefly interrupts the Tab flow, and after confirm/cancel the user clicks/Tabs to resume (focus isn't trapped, the active cell isn't lost). **Known gaps (boss-sheet parity):** NO "RECV CCC" column (no CCC bucket — only C1–C4 / RK1–RK4 / FLEC); NO tank-composition string (Source is its own row per location, and Recv Date is now its own pivot column). Cenapro has no audit-log trigger (writes don't produce `audit_logs`); app rows can't be tagged with a distinct `provenance` through the view (needs a migration). Do not fabricate either. **MOBILE (Archetype E phone-summary):** the frozen matrix can't shrink to a phone (5 frozen identity columns alone = 476px > screen), so the `<div ref={gridScrollRef}>` matrix is `hidden … sm:block` (desktop-only, editing stays desktop by decree) and a `sm:hidden` **`CenaproDailyBlockSummaryMobile`** (local to `production-daily-block.tsx`) renders instead: a **per-day "Daily total" strip** (horizontally-scrollable chips reusing `group.daily.total`) + a **stacked per-row list** — one card per day showing `daily.total`/`daily.subTotal`/`daily.bagging` (the load-bearing footer values) then the pivot leaves flattened (`shift · grade · source · recvDate · recvRow.total`, styled with the same `SHIFT_LETTER`/`GRADE_CHIP`/`pillBase` tokens). Built entirely from the existing `groups` (`buildDateGroups`) pivot — **no number is recomputed**; there is no ₱ anywhere in Cenapro. **EXPORTS the pivot engine for reuse (Phase 2B mechanical extraction):** `buildDateGroups` + `SOURCE_SETS` (re-exported from `production-sources.ts`) + the pivot types (`DateGroup`/`ShiftBlock`/`GradeBlock`/`SourceBlock`/`RecvRow`/`DailyTotals`/`CellMap`/`CellSlot`/`EditColumn`/`EquipmentCode`) + `EQUIPMENT_CODES`/`BAGGING`/`ACTIVE_EQUIP`/`CRUSHER_COUNT` + the format helpers (`fmt`/`formatDayLabel`/`formatRecvLabel`) + the column geometry (`W_*`/`LEFT_*`/`IDENTITY_WIDTH`) + the visual tokens (`GRID`/`GROUP`/`BOX`/`ROW_H`/`CELL_PAD`/`SHIFT_LETTER`/`SHIFT_LABEL`/`GRADE_CHIP`/`pillBase`) + **`MIN_DAY_ROWS`** (the shared per-day filler floor, so the focus + endless views pad to the SAME minimum) — the endless read-only pivot renderer (`production-endless-pivots.tsx`) imports these so both variants share ONE pivot + geometry + token source (a width/token change propagates to both). `PlantView`/`SOURCE_SETS` themselves now live in the pure `production-sources.ts` (imported here). **Phase 3b** also EXPORTS its editing primitives for the endless pivot to reuse (`IdentitySuggestInput`, `InsertPopover`, `BaggingMetaPopover`, `renderRecognizedIdentity`, `normalizeIdentity`, `columnDisposition`, `parseWeight`, `identityOptions`, `isRecognizedIdentity`, `draftIdentityComplete`/`draftHasWeights`, `EDIT_COLUMNS`, `NAV_COL_ORDER`/`navColIndex`, `MONTHS`, types `FillerDraft`/`BaggingMeta`/`StagedInsert`/`NavColKey`), and takes an **`onDirtyChange?`** prop that lifts its dirty signal to `production-ledger-grid.tsx` so the focus-scope axis controls guard a switch that would silently discard the block's edits (the audit's severe finding). |
| `production/production-sources.ts` | **Pure module (NO `'use client'`, no server tag)** — the single source of truth for the plant SOURCE filter: `PlantView` (`'W6'\|'W7'`) + `SOURCE_SETS` (`W6 = {TNK 1..4, W6}`, `W7 = {W7}`; FLEC/DVO excluded from both). Lives in its own pure module precisely so the **server** action `fetchDailyPivotWindow` can filter server-side WITHOUT importing the `'use client'` daily-block component across the boundary. Imported by `production-daily-block.tsx` (re-exports `PlantView`), `actions.ts`, and `production-endless-pivots.tsx`. |
| `production/use-daily-pivot-window.ts` | Client hook — **`useDailyPivotWindow(initialWindow, plant)`**: the DAY-granular sibling of `useLedgerWindow` for the endless W6/W7 pivots. Accumulates a flat, oldest-first EVENT window (the renderer pivots it with `buildDateGroups` into day-blocks) and OWNS react-virtuoso's `firstItemIndex` in **DAY-BLOCK units** (a backward fetch decrements it by the number of prepended DISTINCT prod_dates — exact because the server pre-filters the plant source set, so every distinct day = one block; prepend + decrement batch together → no scroll jump). Seeded from the server-prefetched `initialWindow`; holds `events`/`firstItemIndex`/`hasOlder`/`hasNewer`/`loadingOlder`/`loadingNewer`/`notice`; exposes `fetchOlder()` (day-cursor = the oldest loaded `prod_date`, prepends), `fetchNewer()` (day-cursor = the newest loaded `prod_date`, appends), `reset(anchor)` (re-seed). Per-direction ref guards ignore concurrent/exhausted calls; de-dups by `id`; calls `fetchDailyPivotWindow` directly; errors → `errorToast`. **Phase 3b adds `refreshWindow()`** — after a Save, re-reads the loaded day-window fresh (period-anchored on the current top event, restores prior day-depth), so weight edits / deletes / pull re-tags / new pulls land + re-bucket without a full reload. |
| `production/production-endless-pivots.tsx` | Client — **the Endless W6/W7 Daily Pivots (Phase 2B, READ-ONLY)**: the pivot analog of the endless ledger. ONE continuous, virtualized, oldest-first view of the ENTIRE production history, but paginated by WHOLE prod-days and rendered as VIRTUALIZED DAY-BLOCKS scrolled in both directions across month boundaries. Built on `react-virtuoso`'s `TableVirtuoso`: **each day-block is ONE virtualized item** — the item's `<tr>` holds a single full-`colSpan` `<td>` containing a **nested read-only `<table>`** whose internal `rowSpan`s (the frozen 5-col identity Date/Shift/Grade/Source/Recv) render inside it; virtuoso measures each block's variable height. **ONE shared, opaque, frozen 2-tier header** (`fixedHeaderContent` = `PivotHeaderRows`, `TableHead` = `frozen-row bg-muted` z-20) sits over all blocks; the outer table's colgroup and every nested block's colgroup are IDENTICAL (`PivotColGroup`, `width:max-content`+`minWidth`) so columns line up under horizontal scroll, and the frozen identity cells (`.frozen-col`/`.frozen-corner`/`.frozen-edge`, cumulative `left` offsets) stick to the shared `Scroller` (custom, `overflow-x:auto`). Pivots the hook's accumulated events with **`buildDateGroups` REUSED VERBATIM** from the daily block; the `DayBlock` renderer mirrors `DateSection`'s REAL-row render (box outline via `BOX` borders, group dividers via `GROUP`, shift letters / grade chips) but STATIC (no edit machinery, no editable draft rows — "first-in-group" is derived POSITIONALLY from map indices, not mutable render flags). **Consistent-height day blocks (filler padding):** each day-block is padded up to `MIN_DAY_ROWS` (imported from `production-daily-block.tsx` — the SAME shared floor the focus daily block uses, so the two views can't drift) with faint **READ-ONLY** filler rows after the last real leaf and before the footer: `fillerCount = max(0, MIN_DAY_ROWS − dayBodySpan)`, `dateRowSpan = dayBodySpan + fillerCount`. A filler row emits NO Date/merged-identity cell (the Date + Shift/Grade/Source `rowSpan`s already cover it → the day box stays a rectangle, footer separate), just opaque `bg-background` frozen identity slots (Shift/Grade/Source/Recv, frozen-pane rule) + faint `bg-muted/30` numeric cells so the area reads as reserved/empty SLOTS on the uniform grid. Purely visual — NO inputs (editing endless pivots is a later phase). A day with ≥ `MIN_DAY_ROWS` real rows gets 0 fillers (unchanged). Adding fillers changes a block's HEIGHT only (virtuoso measures dynamically), NOT the number of day-block items — the `firstItemIndex` day-count anchor math is unaffected. **Per-day "Daily total" footer only — NO cross-scroll grand total (locked decision #7).** `firstItemIndex` (from the hook, DAY-BLOCK units) → `itemContent` computes `pos = index - firstItemIndex` for the month-boundary check (slim `MonthSeparator` bar when `prod_date` crosses a month, 1:1 with blocks, no separator items). `startReached`→`fetchOlder`, `endReached`→`fetchNewer`; `initialTopMostItemIndex` = last block for `latest` (opens at the newest day = bottom), `0` for a period (opens at its first day). **Toolbar = `CenaproPeriodPicker` (jump anchor) + `ViewModeSwitcher(mode=view)` + `ScopeToggle(scope=endless)` + "N days loaded".** loadError → persistent copyable banner (HARD RULE); empty → Inbox + notice. **NO row/day-block entrance animation** (virtualized — they recycle; only the pre-existing `transition-colors` hover). **Mobile:** the frozen 5-col matrix can't shrink to a phone; it **horizontal-scrolls at every breakpoint without crashing** (Scroller `overflow-x:auto`) — no phone-specific summary this phase (a focus-daily-block-style summary can be added later). **PHASE 3b — FULLY EDITABLE (unlock to edit).** The hard rule from the virtualization audit: **NO row-local edit state anywhere** (virtuoso recycles day-blocks). ALL pending state is PARENT-OWNED in one serializable `PivotPending` object (`modified` by eventId, `deleted` set, `staged` real-blank inserts by cellKey, `drafts` new-pulls by `${prodDate}#slot-i`, `pullEdits` identity re-tags by leafKey, `addedDays`, `extraRows`); the SINGLE active in-progress `editValue` lives in the parent too (one cell edits at a time → a half-typed value scrolled off-screen and back rehydrates). **Keyboard nav is DATA-DERIVED** (`buildNavModel` builds the ordered nav-cell list + per-row lanes from the pivoted `groups`+draft slots — NOT `querySelectorAll`, which stops at the virtualization boundary; a custom `NavResolver<string>` walks it; scroll-into-view-before-focus for off-screen targets via `virtuosoRef.scrollIntoView`). **Central, data-addressed commit** (no per-cell closure — scroll-safe). **Popovers (`InsertPopover`/`BaggingMetaPopover`) render at the PARENT level** anchored to captured cell coords (`PopoverAnchor`), so a recycled day-block never orphans them. Capabilities: weight edit / clear-to-delete (confirm) / type-into-blank→Insert popover; new pulls via filler rows (per-day `batch`/`batch_year` from the day's OWN prod_date, never a global period — sends explicit `batch_year`); **Edit-pull** row-level identity re-tag (right-click → dialog → re-buckets after save, the AG-Grid pattern; NOT inline on merged rowSpan cells); per-row context menu (Edit / Duplicate / Delete-or-Restore pull) via the shared `GridContextMenu`/`useGridContextMenu`; **Add day** (append-edge only, enabled when `!hasNewer`) + **Remove/Delete day** (day-footer menu; added-empty→✕, added-with-drafts→confirm, committed→confirm w/ event count → stages deletes); **undo/redo** over the whole pending buffer (⌘Z/⌘Y + toolbar, snapshot stacks); loss-proof localStorage mirror (`cenapro-pivot-drafts:v1:${plant}:${uid}`) + Resume/Discard prompt; collision cells (>1 event) stay locked with an "open in Ledger" deep-link; unified Save → `saveProductionEvents(dirtyRows, deletedIds)` then `win.refreshWindow()`. **Dirty-nav guard**: when `totalDirty>0`, the toolbar's `CenaproPeriodPicker`/`ViewModeSwitcher`/`ScopeToggle` are `disabled` so a switch can't silently discard edits. Locked mode is byte-for-byte the prior read-only renderer. |
| `production/draft-entry-zone.tsx` | Client — **`DraftRowCells`** + **`DraftDatalists`** (Phase 2A, IN-LIST model). The retired Bulk Add modal → a pinned draft section → now IN-LIST blank rows that are `TableVirtuoso` ITEMS in the SAME list as committed rows (the natural "Google Sheets" model). This module is **purely presentational**: `DraftRowCells` renders ONE draft row's `<td>` cells (row# + remove control + the 12 editable data cells via the shared `GridCell` + `<Input list=…>` typeahead) as a fragment — the endless sheet's virtuoso `TableRow` supplies the `<tr>`, so blanks align with committed rows exactly (same colgroup/widths). It holds **NO draft state** — `row` comes in as a prop and every edit calls up to `updateRow(draftIndex, field, value)`; the data lives in the endless sheet's PARENT-OWNED `draftRows` array (keyed by position), so virtuoso recycling an off-screen half-typed row never loses it (rehydrates from the array on remount) — the flat-list equivalent of `production-daily-block.tsx`'s parent-owned `drafts` Map keyed by a positional slot id, minus the monotonic-id indirection since this list never reorders. Draft cells are intentionally NOT frozen (editable + non-sticky; columns still align via the shared colgroup). `DraftDatalists` renders the 7 categorical `<datalist>`s once. Reuses `bulk-paste-utils.ts` types. **Phase 3a:** the 12 editable data cells (cols 1–12) are extracted into a shared **`EditableDataCells`** used by BOTH `DraftRowCells` (lead `+`/× cell) AND the new **`CommittedRowCells`** (lead row# + hover Delete/Restore toggle, amber "modified" rail / rose "deleted" strike) — so unlocked committed rows reuse the EXACT SAME cell editors as drafts (one consistent flat-spreadsheet editing surface). `DraftRowCells`'s coordinate prop is now the UNIFIED `rowIdx` (`committed.length + draftIndex`) instead of a bare draft index. |
| `production/bulk-paste-utils.ts` | The Bulk Add **paste + canonicalization layer** (pure, no React) — RC IN's `paste-utils.ts` analogue plus the categorical brain. Exports `BulkRow`/`BulkField`, `BULK_COLUMN_MAP` (**12** editable cols in the modal's left→right Excel order — `disposition_kind`+`partner_equipment_code` collapsed into a single `ccc_flec` cell), `createEmptyRow`, `isBlankRow`, `cleanBulkPasteValue` (dates→`parseExcelDate`, numerics strip `₱,"'%`/spaces; categoricals + `ccc_flec` kept raw so the cell shows what was pasted), per-column canonicalizers (`canonicalizeShift/Grade/Plant/Warehouse/Source/Side` — forgiving on case/aliases, e.g. `W7`/`whse7`/`7`→`WHSE 7`, `3x50`→`3X50`, `TNK1`→`TNK 1`, `left`→`LS`), and `mapBulkRowToDirty(row, defaultYear?)` which validates a row (weight > 0, categoricals map to real codes), runs both date cells through `normalizeTypedDate(value, defaultYear ?? currentYear)` as a **safety net** (so a typed shorthand that escaped commit-time normalization still lands canonical `yyyy-MM-dd`), and **resolves the single `ccc_flec` cell via the shared `parseCccFlec` from `types.ts`** (`FLEC`→`flec_bagging`; `C1`→`partner_crusher`+`C1`; `RK3`→`partner_kiln`+`RK3`) → `ProductionEventDirtyRow` or a list of human errors. The old separate `canonicalizeDisposition`/`canonicalizeEquipment` + the disposition⇄equipment CHECK dance are gone (the single column subsumes them — `parseCccFlec` returns null only for genuinely unrecognized input). `rowLabel(row, i)` for error messages. |
| `inventory/page.tsx` | Server — reads `?whse=&date=` URL params (defaults WHSE 7 / 2026-03-10), fetches `fetchFlecInventory()` + `fetchOpeningBalances()` + `fetchOpeningBalanceHistory()` in parallel → `<FlecInventoryClient />`. The START date is the "as of" for both the openings seed and the ledger seed; history is warehouse-scoped (date-independent). |
| `inventory/actions.ts` | `'use server'` — `fetchFlecInventory(warehouse, startDate)` (`cenapro_flec_balance` + `cenapro_flec_ledger` in parallel); `fetchOpeningBalances(warehouse, asOf)` (`cenapro_opening_balances` — the effective opening per grade×side, seeds the editable STARTING block); `fetchOpeningBalanceHistory(warehouse)` (`cenapro_opening_balance_history` — the full append-only trail, newest-first); **`saveOpeningBalances(changes[])`** loops `cenapro_set_opening_balance` once per CHANGED cell (append-only INSERT, effective date = page START date), stops + reports the first failing cell, `revalidatePath('/cenapro/inventory')` on success. See "Opening-balance write path". |
| `inventory/flec-inventory-client.tsx` | Client — warehouse `<Select>` + native start-date input (both push to URL params via `router.replace` in a transition). **The selection persists to `localStorage` (`cenapro_flec_prefs` = `{ whse, date }`):** `applyParams` writes it on every change, and a once-on-mount effect restores it when the URL carries NO `whse`/`date` param (a clean visit — nav menu / landing card), so the operator's last warehouse + start date permeate across refresh, navigation, and sessions. An explicit URL param ALWAYS wins (shared links keep working); localStorage is only the fallback, then the hardcoded default. SSR-guarded; the restore runs once (no replace loop); a stale `whse` not in `FLEC_WAREHOUSES` or a non-ISO date is ignored. **Editable STARTING block** (`StartingBlock`): a `table-fixed` grid, rows = `GRADE_CODES`, columns = `RS`/`LS` each split into **Starting** (editable digit-only `<input>`, seeded from `cenapro_opening_balances`) + **Now** (read-only closing from `cenapro_flec_balance`, "—" when absent per the empty-period caveat). Local edit buffer with amber dirty-border per cell, "N unsaved" badge, Discard + "Save starting balances" (writes only changed cells → `saveOpeningBalances` → success toast / `errorToast`; `router.refresh()` re-seeds). Per-cell `CellHistoryPopover` (History icon) + a collapsible whole-warehouse `OpeningHistoryPanel` grouped by grade×side (newest-first, "current" badge on the top entry) — the backtracking surface. Then the existing balance cards + show-your-math movement ledger. Local `StartingBlock`/`StartingCell`/`CellHistoryPopover`/`OpeningHistoryPanel`/`BalanceCard`/`LedgerDirection`. |

## Data
All reached through **read-only accessors in the already-served `public` schema** — the standard Supabase client hits them directly (no `.schema('cenapro')`, no cast). See "Data path" below for why. These accessors are thin wrappers over the underlying `cenapro` schema objects.

- **`public.cenapro_production_events`** (VIEW, ~755 rows across 9 (batch_year, batch) periods; recv_date 2025-12-01 → 2026-06-01). 16 columns: `id, recv_date, prod_date, batch, batch_year, shift_code, grade_code, plant_code, warehouse_code (nullable — "unplaced" rows), source_location_code, weight_kg, disposition_kind, partner_equipment_code, flec_count, whse_side, unique_tag`. **The ledger loads ONE period at a time** (filtered `.eq('batch_year').eq('batch')`), not all rows — `batch` = a month name (JANUARY…DECEMBER), `batch_year` = an int. Heaviest single period ≈ 162 rows; newest (the default) can be small (e.g. JUNE 2026 = 2 rows). Loading everything (the old path, ~755 rows × ~14 interactive cells with no virtualization) was the perf bottleneck.
  - PostgREST types all VIEW columns as nullable (`T | null`) — the client consumers guard accordingly (e.g. the sort comparator coalesces `recv_date`/`id` to `''`).
  - `disposition_kind` ∈ `flec_bagging | partner_crusher | partner_kiln`. The **production grid + bulk modal** render the single Excel `CCC/FLEC` value (`FLEC` / `C1` / `RK3`) via `formatCccFlec`; the **Flec Inventory ledger** still renders the friendly label (Bag / Crusher C1 / Kiln RK3) via `formatDisposition`.
  - Verified distinct values: shift `M`; grades `2X6/3.5/3X50`; warehouses `WHSE 1/5/7`; equipment `C1/C2/RK1/RK2/RK3/RK4`. (Filter options are derived from data at runtime, not hardcoded.)
- **`public.cenapro_flec_balance(p_warehouse_code text, p_start_date date)`** → `(warehouse_code, grade_code, side, current_flec, opening_seed, as_of)`. Closing count per (grade, side). NOTE: a (grade,side) with an opening but no events ≥ start date does NOT appear (empty-period caveat, schema §6.2).
- **`public.cenapro_flec_ledger(p_warehouse_code text, p_start_date date)`** → `(…, recv_date, grade_code, side, disposition_kind, partner_equipment_code, kg_moved, flec_in, flec_out, opening_seed, flec_in_to_date, flec_out_to_date, running_balance)`. The show-your-math movement detail (function Returns are non-null).
- **`public.cenapro_opening_balances(p_warehouse_code text, p_as_of_date date)`** → `(warehouse_code, grade_code, side, period_start_date, opening_flec_count, created_at)`. The **current effective** opening per (grade, side) as of the date = greatest `period_start_date ≤ as_of`, tie-broken by greatest `created_at`. Seeds the editable STARTING block. Unlike `cenapro_flec_balance`, it returns a (grade, side) even with no events forward.
- **`public.cenapro_opening_balance_history(p_warehouse_code text)`** → `(id, warehouse_code, grade_code, side, period_start_date, opening_flec_count, created_at)`. **ALL** append-only opening entries, newest-first per (grade, side) — the backtracking data.
- **`public.cenapro_set_opening_balance(p_warehouse_code, p_grade_code, p_side, p_effective_date date, p_count int)`** → INSERTS a new append-only entry (never overwrites), RETURNS the row. SECURITY INVOKER, granted to `authenticated` + `service_role` (NOT `anon`) — written only by `saveOpeningBalances` (server action, runs as the logged-in user). See "Opening-balance write path".
- Flec warehouses = `WHSE 1/2/5/7` (flec-count). **WHSE 3 is kg/DVO — DEFERRED, never surfaced.**
- Opening balances seeded for **WHSE 7** (3X50 RS=53, 2X6 LS=26 @ 2026-03-10) → that is the inventory page's default. With the seeded ledger, WHSE 7 closing as of 2026-03-10 is 3X50/RS = 56, 2X6/LS = 0; the other 6 grade×side cells have no opening AND no movement, so they appear in neither `cenapro_opening_balances` nor `cenapro_flec_balance` (the STARTING grid renders them blank → "—", ready to type into).

## Key Behaviors
- **Production = EDITABLE ledger; Flec Inventory = read-only.** The app is now the maintaining file for production rows.
- **Production screen = TWO orthogonal URL axes (Phase 1 reframe, 2026-07-21; a third, EDIT lock/unlock, is Phase 3).** **VIEW** (`?view=ledger\|daily-w6\|daily-w7`) × **SCOPE** (`?scope=endless\|focus`) — every combination is valid and reachable (six no-dead-end combos); the View switcher + Scope toggle + period dropdowns render in ALL of them. `endless` is the SCOPE default (omits the param); a legacy `?focus=1` maps to `scope=focus`. **ENDLESS + LEDGER = the Endless Sheet (Phase 1, read-only):** one virtualized, oldest-first (`recv_date ASC, id ASC`) view of the ENTIRE history, keyset-paginated bidirectionally (`fetchLedgerPage` + `useLedgerWindow` + `react-virtuoso`'s `TableVirtuoso`). The `?year=&batch=` dropdowns are a **JUMP-TO anchor** — the FIRST DB query is already anchored (period → its first row loading forward; no params → `latest`, opening at the BOTTOM = newest rows), never load-from-the-beginning-then-teleport; scrolling up prepends older rows with the viewport pinned (`firstItemIndex` decremented in the same batch), scrolling down streams newer months; **column FILTERS shipped 2026-08-03 and are pushed into the keyset QUERY** (sort controls are still focus-only); a dropdown jump `router.replace`s the URL → server re-prefetch → anchor-keyed remount. **FOCUS (any view) = the existing month-scoped EDITABLE grid, unchanged** (sort toggles + inline editing + right-click Insert intact) — `scope=focus` is a PURE month-clamp with no exclusive features; the SAME dropdowns are the clamp selector there. **ENDLESS + DAILY-W6/W7 = a Phase-1 fallback** (option (b): transparently render the month-scoped daily block + a small "Endless {W6/W7} coming" hint); Phase 2 replaces it with the true day-windowed endless pivot. The axis controls are the shared `ViewModeSwitcher` + `ScopeToggle` (`ledger-controls.tsx`), the pure helpers `parseScope`/`parseViewMode`/`plantViewOf` (`ledger-url.ts`); the old `LedgerModeToggle` + `?focus` silo are retired. Phase 2A DELIVERED the draft entry zone (retiring the Bulk Add modal); Phase 3 adds inline edit of committed rows on the sheet — see `.agents/prompts/cenapro-ledger-endless-sheet-spec.md`.
- **Production ledger:** scoped to **one (year, batch) period** via the toolbar `CenaproPeriodPicker` (URL params `?year=&batch=`); only that period's rows load (server-side `.eq` filter). **Default = the NEWEST period** (`fetchCenaproPeriods` returns periods newest-first; the page picks `periods[0]` when the URL has no param) so the page opens fast on recent data — NOT all ~755 rows. An explicit URL period wins; a stale one falls through to newest. There is intentionally **no "All periods" option** (a future "All" behind virtualization is out of scope). WITHIN the loaded period, filters + date-sort happen client-side (the per-period set is small). **Header filters = Shift / Grade / Plant / Whse / Source / CCC/FLEC** (2026-08-03: **MULTI-select**, canonical option domain from `types.ts` — not distinct-in-data — driven by the URL filter axis; they HIDE rows, not sort; a dirty row is EXEMPT and never hidden). See "Column filtering — the FILTER axis" below. **Both date headers sort:** clicking the Recv or Prod header (`DateSortHeader`) makes that date the in-memory sort key; clicking the active one flips asc/desc; default = newest-first by recv_date (the chevron marks the active key, an idle `ChevronsUpDown` the other). Sticky glass header (`bg-muted/90 backdrop-blur-sm`). One VIEW row = one grid row keyed by stable `id`. **12 data columns**, Excel order: `# · recv · prod · batch · shift · grade · plant · whse · source · weight · CCC/FLEC · flec · side`. Inline edit (click-to-activate, F2/type-over, keyboard nav, paste-from-Excel), `SelectCell` dropdowns for the categorical columns (shift/grade/plant/whse/source/side), the single `CCC/FLEC` typeahead `<Input list>` cell, `DatePickerCell` for recv/prod, numeric `<Input>` for weight/flec. **Rows are a `React.memo`'d `ProductionRow`** fed primitive active/selection slices + stable callbacks, so editing one cell re-renders only the touched row(s) — not the whole grid (a cheap complementary win on top of the period scoping). Dirty tracking (new=blue / modified=amber left border; deleted=strikethrough) + single Save/Discard; **NO trailing always-empty new row** — adds happen via Bulk Add (primary) or right-click Insert Above/Below (paste may still auto-extend). Empty state shows when the period has zero data rows. Switching period is blocked (picker disabled) while there are unsaved edits. No row animations (`transition-all duration-150` hover only).
  - **Frozen identity columns (Excel freeze panes — canonical pattern, see CLAUDE.md "Frozen Panes" + `globals.css`):** the 4 leftmost columns `# · Recv · Prod · Batch` are pinned with cumulative px offsets from the colgroup widths (36/96/96/120 → left: 0 / 36 / 132 / 228), so they stay visible when scrolling right. The table uses `borderCollapse: 'separate'` (required for sticky). Uses the shared `.frozen-col` (body) / `.frozen-corner` (header identity cells) / `.frozen-edge` (anti-seam right divider on the last frozen col, Batch) utilities. **All frozen surfaces are fully OPAQUE** — header/corner `bg-muted` solid, body `bg-background` solid + the row's opaque IN/OUT tint via `rowDirectionFrozenTint` + `group-hover:bg-muted/50` — so scrolling content can't bleed THROUGH the pinned cells (the previous translucent `bg-muted/90 backdrop-blur` thead was the bleed-through bug). Selection/edit/ring still render inside the frozen cells (GridCell/DatePickerCell paint their own selection feedback on top). **Z-scale (canonical):** frozen header corners (sticky top + left) `z-30` > scrolling header row `z-20` > frozen body col `z-10` > normal scrolling cells (auto). Mirrors the ICTC daily-ledger freeze-pane approach.
  - **Bold + direction tint + badges (display-only):** the ledger text is **bold** throughout. Each row gets a **bold row tint** keyed on the WAREHOUSE first, then the CCC/FLEC disposition — **priority: UNPLACED > WHSE 3 (DVO) > disposition** (via `rowDirection(row)`, which now takes the whole row so it can read `warehouse_code`): **UNPLACED** (`warehouse_code` blank) → **NO tint** (an unplaced bagging row is NOT highlighted until it's placed — this is the intentional behavior change; setting the Whse dropdown places it and re-tints it green); **WHSE 3** = the DVO warehouse → **BLUE** (`bg-blue-50 dark:bg-blue-950/40`) regardless of disposition (a DVO row is never counted as "taken out of a real warehouse"); a **real placed warehouse** (`WHSE 1/2/5/7`) → disposition decides: bagged-IN/`flec_bagging` → **GREEN** (`bg-emerald-50 dark:bg-emerald-950/40`), any withdrawal/`partner_crusher`+`partner_kiln` → **RED** (`bg-rose-50 dark:bg-rose-950/40`); unrecognized CCC/FLEC at a real warehouse → none. Frozen cells use the opaque variant (`-950/60` in dark, blue included). Tint is layered UNDER the dirty borders + selection highlight so those still read clearly. **CCC/FLEC** and **PLANT** render as compact colored badges in display mode, punchier now (`/20`–`/25` fill, `/40` border, bold `-700/-300` text): `FLEC` emerald / `Cn` amber / `RKn` rose; `W6` blue / `W7` teal / `W6/W7` indigo / `DVO` slate — each with a `dark:` variant; the edit `<Input>` stays plain so paste/typing is unaffected.
  - **CCC/FLEC single column:** disposition + equipment render as ONE Excel-parity cell (`FLEC`/`Cn`/`RKn`) between weight and flec. Edited as raw text, parsed to `disposition_kind` + `partner_equipment_code` on Save via the shared `parseCccFlec`; an unrecognized value is rejected with a persistent `errorToast`. A single typed cell can't make an inconsistent pair, so the prior partner-equipment dependency dance + the separate Disposition/Equipment columns are gone. See "CCC/FLEC single column" above.
  - **Unplaced-warehouse fix:** `warehouse_code` is nullable; a null row reads "unplaced" (subtle amber tint). Setting the Whse dropdown is how an operator places the unplaced bagging rows.
- **View modes (`?view=`):** the VIEW axis — the production screen shows a period's rows through multiple lenses, switched via the shared toolbar `ViewModeSwitcher` (now in `ledger-controls.tsx`, available in BOTH scopes) driving `?view=ledger|daily-w6|daily-w7` (preserves the other axis params — `scope`/`year`/`batch`; `useTransition` + `router.replace`; `ledger` is the default and omits the param; absent/invalid → `ledger`; **legacy `?view=daily` → `daily-w6`**). In focus scope the data does NOT re-fetch on a view change — every mode consumes the same `initialRows`. The typed `ViewMode` union + `parseViewMode()` + `plantViewOf()` now live in the pure `ledger-url.ts` (shared by the server page + the client controls), keeping it extensible (future: per-product, stats).
  - **Ledger mode (default):** the editable Industrial-Spreadsheet grid described above (unchanged — date auto-format, Enter-anchor nav, warehouse-first highlight all intact).
  - **Daily W6 / Daily W7 modes (EDITABLE — Phase 1 + 2):** the visually-styled boss "PROD 2026" pivot (`ProductionDailyBlock`, `plantView='W6'|'W7'`) — two variants showing **production OUTPUT** for each plant, **always grouped by `prod_date`** (PROD-ONLY; the recv/prod toggle was removed). The period picker + view switcher STAY visible; the editable-only toolbar controls HIDE; the `loadError` banner still shows. **Source filter:** rows are filtered by `source_location_code` before pivoting — **W6 keeps `{TNK 1, TNK 2, TNK 3, TNK 4, W6}`, W7 keeps `{W7}`; FLEC and DVO EXCLUDED from both** (inventory withdrawals, not output). **Bagging kept** (bagged tank/W6/W7 material is still output). **Hierarchy:** PROD_DATE → SHIFT (M/E/N) → GRADE (3X50/2X6/3.5/4X8) → SOURCE → **RECV_DATE** — a **LEAF = one (source, recv_date) pull**. The **SOURCE cell `rowSpan`s its recv-date rows**, so each source shows ONCE with its recv dates stacked **chronologically** in a dedicated **Recv Date** column (= how many pulls that tank had that prod date). **Columns:** `Date · Shift · Grade · Source · Recv Date · C1–C4 · RK1–RK4 · Bagging · Sub-Total · Total`. **Only rollup = the per-DATE "Daily total" footer** (the grade subtotal was removed). **Padded + outlined day blocks:** each day pads with blank FILLER rows up to `MIN_DAY_ROWS` (=6, tunable) — filler numeric cells get a faint `bg-muted/30` (reserved empty slot, not missing data). **Complete 2-tier header:** the **5 identity headers `rowSpan={2}`** (fill full header height, no empty white top-left, `.frozen-corner`, vertically centered) + the tinted group banner over the code cells — one solid bordered band edge-to-edge. **Three border tokens (one each, used everywhere):** **GRID** = uniform 1px `border-b border-r border-border` (solid, crisp) on every cell, both axes (merged cells skip internal horizontal lines); **GROUP** = 2px `border-l-2 border-l-border` divider between the column groups (Crushers|Kilns|Bagging|Totals), carried through header+body+filler+footer; **BOX** = 2px day outline (top row / footer / Date col / Total col) — SOFT NEUTRAL `border-border` (same color as gridlines, stands out by 2px weight not by color; NOT black). The `<table>` carries a `border border-border` perimeter (fully closed). Clean card bg, **no tint**. **Layout — REAL merged cells (`rowSpan`):** DATE spans the day's REAL + FILLER rows (**NOT** the footer); SHIFT spans its shift; GRADE its grade's leaf rows (`= leafCount`); SOURCE its recv-date rows; `dateRowSpan = dayBody + filler`, day has `dayBody + filler + 1` `<tr>`s (footer separate) — **rectangular**. Merged content **align-top**; merged cells sticky-left + opaque. **BOLD identity columns** (Date/Shift/Grade/Source/Recv Date); numeric cells normal weight (daily total bold). **No data-bars** (removed). **FIXED equipment columns** — all 8 always render; blank when 0. **Frozen panes** on **5 identity cols** (Date/Shift/Grade/Source/**Recv Date**) + the 2-tier header (`.frozen-edge` on Recv Date). **Full-width day footer:** a TRUE edge-to-edge totals row — a "Daily total" label `colSpan={5}` over ALL FIVE identity cols (sticky at `left:0`), then the numeric totals; one continuous solid `bg-muted` band, bold numbers, group dividers carried. **Excel-matched widths** (Date 76 / Shift 48 / Grade 100 / Source 140 / Recv 80 / equip 56 / Bag 66 / Sub 70 / Total 84), `width:max-content`, `px-1.5 py-0.5` / `text-[11px]`. **Visual treatment:** **shift = a BOLD colored letter** (M amber / E violet / N indigo — not a pill, can't overflow) + per-grade **chips**; group banner tints (Crushers amber / Kilns rose / Bagging emerald / Totals) on the group cells only; toolbar carries a `{plantView}` pill. Section-level fade only, NO per-row animation. Per-variant empty state. **EDITABLE (Phase 1):** equipment/bagging NUMBER cells on REAL leaf rows are click-to-edit (Enter/Tab/blur commit, Esc cancel) and round-trip through the EXISTING `saveProductionEvents(dirtyRows, deletedIds)` (no new backend): **edit** → UPDATE (dirty row WITH the cell's event `id`, amber ring); **clear** → DELETE (id → `deletedIds`, `AlertDialog` confirm); **type into a blank cell** → a **column-aware `Popover`** → stage an INSERT (no id, blue tint). The popover shows read-only **context chips** (auto Plant, prod date, shift, grade, target column) and branches: **crushers/kilns** → **Source only** (selector over `SOURCE_SETS[plantView]`, pre-filled to the row's source; no warehouse/side/flec); **Bagging** → **Source + Warehouse + Side + Flec count**. The pivot records each cell's event id(s); the COLUMN is the disposition (`columnDisposition()`: Cn→`partner_crusher`, RKn→`partner_kiln`, Bagging→`flec_bagging`+null). INSERT dims = the leaf + the popover's Source + `selectedPeriod.batch`, with **`plant_code` AUTO-STAMPED = `plantView`** (W6/W7 — no blank Plant in the Ledger); batch_year trigger-derived, not sent. A cell mapping to >1 event is **collision-LOCKED** (🔒, edit in Ledger; 1 such cell exists). A toolbar Save/Discard bar (+"N unsaved") appears on `hasChanges`; Save → action → `onSaveSuccess()` remount; errors via `errorToast`. **EDITABLE (Phase 2):** the blank FILLER rows are **"new pull" input slots** — identity cells are **Excel-style input + `<datalist>` typeahead** (not `<Select>`): Shift/Grade/Source filter native suggestions, normalized + validated on commit (`normalizeIdentity`); **Recv Date** is typed (normalized) and starts EMPTY (no prefill). Then editable equipment/Bagging weights. A weight commit **validates identity is complete** (shift+grade+source+recv, else a gentle toast) before staging; a complete filler stages **one INSERT per filled column** (C1+C2 = 2 events sharing the pull dims) with `plant_code` auto = `plantView`, dims from the row, batch from the period (crushers/kilns direct; Bagging via a small warehouse/side/flec popover). **Add-row drawer:** the `MIN_DAY_ROWS` rectangle stays; auto-grow-on-fill was removed — a hover-revealed "+ Add row" pill (at the footer label's top edge, `group-hover/day`, opacity-only) appends a row to that day via `extraRows`. A toolbar **"Add day"** popover (validated date in-period) spawns a NEW empty fillable day block (`addedDays`, sorted; empty added days save nothing). Drafts render FLAT until save+refresh; the Date `rowSpan` covers real+draft+slot rows so the grid stays rectangular; draft inserts feed the SAME `saveProductionEvents` payload + `hasChanges`/Discard. **Excel-fidelity cells:** **uniform row height** (`ROW_H` `h-7` on every `<tr>` incl. the footer, `align-middle`); **no height expansion on edit** (the inline `EditInput` matches the static cell metrics exactly — `EDIT_INPUT`: `text-[11px]`/`px-1.5`/`leading-none`/`bg-transparent`/no border/ring/`h-full`/`appearance-none`, caret-only change); **placeholder vanishes on focus**, restores on empty blur. **Keyboard nav (spreadsheet-style):** every editable cell is an always-rendered `EditInput` with `data-navid="${rowKey}|${colKey}"`; a grid-level `onKeyDown` reads the ordered cell list from the DOM and moves the active cell — **Tab/Shift+Tab** right/left (wrap at row ends), **Enter** down with the **Enter-anchor** (returns to the Tab-run's start column, mirroring the bulk-add modal; cleared by arrows/click), **arrows** within row/column, **Esc** cancels + stays. Skips read-only/locked cells (no `data-navid`); click + keyboard share one `activeNavId`. The active cell gets a **highlight matching the ledger** — `ACTIVE_RING` = `ring-2 ring-primary ring-inset` (+`z-20` so it's above the z-10 frozen cols, never clipped); an inset ring takes no layout space (no height change) and is static (no transition), sitting over any dirty `bg-*` tint so both read. Bagging Tab/Enter opens the warehouse/side/flec popover (focus briefly moves there; resume by click/Tab). Props `selectedPeriod`+`onSaveSuccess` threaded from `production-ledger-grid.tsx`. No cenapro audit-log trigger + no per-row `provenance` tag (gaps, deferred).
  - **Known gaps (boss-sheet parity, do NOT fabricate):** there is **no "RECV CCC" column** — the data model has no CCC bucket (only crushers C1–C4 / kilns RK1–RK4 / FLEC bagging); and there is **no tank-composition string** (the boss's "10+10+10+3 FLEC" source breakdown isn't stored, so Source shows the distinct `source_location_code` values joined instead). Future view-mode work should treat these as missing source data, not as a rendering TODO.
- **Draft entry zone (fast multi-row entry — Phase 2A, replaced the Bulk Add modal):** the editable ledger toolbar's **"Add rows in the sheet →"** affordance switches to the endless Ledger (`?scope=endless`, `?add=1`), which UNLOCKS a pool of **IN-LIST blank draft rows** appended below the last committed row in the SAME virtuoso list (the "Google Sheets" model — scroll DOWN into an effectively-infinite supply, topped up to ~25 trailing blanks). Blanks render through the same `itemContent` as committed rows (`DraftRowCells`), so columns align exactly. **Paste from Excel/Google Sheets fills the grid** (`.split(/\r\n|\n|\r/)` → rows, `.split('\t')` → columns, filling from the active cell, **auto-extending** rows when the paste is taller). "Add row" appends; hovering a row reveals an X to remove it. Column order (left→right, **12 data cols**): `recv_date · prod_date · batch · shift · grade · plant · warehouse · source · weight_kg · CCC/FLEC · flec_count · whse_side`.
  - **Loss-proof (the whole reason this exists — BUG-012):** draft rows live in the endless sheet's state and are **mirrored to `localStorage`** (debounced, versioned, keyed to user id), restored on mount, cleared ONLY on a confirmed Save/Discard. NOTHING destroys drafts — Escape only reverts the current cell (no Dialog to close), click-out/lock keep them, a reload restores them. This is the opposite of the retired modal, which had no close-guard and reset its rows to 8 blanks on every reopen.
  - **Typed-date auto-transcription (Excel parity):** unchanged — both date cells normalize a typed shorthand → `yyyy-MM-dd` on commit via `normalizeTypedDate(raw, yr)` (`yr` = `selectedPeriod?.batch_year`); `mapBulkRowToDirty` re-applies it at save as a safety net.
  - **Categoricals stay paste-friendly:** every categorical cell is a typeable `<Input list=…>` backed by a `<datalist>` — NOT a strict dropdown. Canonicalization happens **on Save** in `bulk-paste-utils.ts` (forgiving aliases — `W7`/`7`→`WHSE 7`, `3x50`→`3X50`, `left`→`LS`); the single `CCC/FLEC` cell resolves to both DB fields via the shared `parseCccFlec`.
  - **Save (INSERT-only):** the parent (endless sheet) drops blank rows, validates the rest via `mapBulkRowToDirty` (weight > 0; categoricals resolve to real codes; `CCC/FLEC` resolves via `parseCccFlec`) — invalid rows block the save with **one persistent, copyable `errorToast`** naming them + a red rail on each offending row (HARD RULE) — then INSERTs via the **existing** `saveProductionEvents(dirtyRows, [])`. On success: clear drafts + mirror, reseed a fresh sheet, `refreshNewest()` so the rows land at the bottom, scroll to LAST. On throw: drafts stay (state + mirror) for retry.
- **Flec Inventory:** warehouse + start-date drive state via **URL search params** (`?whse=&date=`); changing either `router.replace`s in a `useTransition` → server re-fetch. **The selection also persists to `localStorage` (`cenapro_flec_prefs`) and permeates a fresh visit:** `applyParams` writes `{ whse, date }` on every change, and a once-on-mount effect restores it whenever the URL has NO `whse`/`date` param (clean `/cenapro/inventory` from the nav menu or landing card) — so the operator's chosen warehouse + start date survive refresh, navigation, AND sessions (a critical detail that previously fell back to the WHSE 7 / 2026-03-10 default). **An explicit URL param always wins** (shared links unaffected); localStorage is only the fallback, then the hardcoded default. SSR-guarded; restore runs once (no replace loop). Start-date semantics made explicit ("Balances as of `<date>` forward"). Layout top→bottom mirrors the Excel PC WHSE sheet: **editable STARTING block** → current-balance cards → collapsible opening-balance history → show-your-math movement ledger ("opening + ins − outs = balance").
  - **Editable STARTING block:** rows = the 4 flec grades, columns = `RS`/`LS`, each cell = an editable **Starting** input (seeded from `cenapro_opening_balances(warehouse, startDate)`) + a read-only **Now** closing (from `cenapro_flec_balance`). Edits are local (amber dirty-border, "N unsaved" badge) until **Save starting balances** writes only the changed cells. The **START date is the effective date** of every saved opening — the modular part: change the date and/or values to set a fresh starting point for a period, and old values are kept (append-only). After save → success toast + `router.refresh()` re-seeds the grid and re-derives the ledger.
  - **Backtracking:** every saved opening is preserved. Surfaced two ways — a per-cell `CellHistoryPopover` (History icon next to each input) and a collapsible whole-warehouse `OpeningHistoryPanel` (grouped by grade×side, newest-first, top entry tagged "current"), both reading `cenapro_opening_balance_history(warehouse)`.
- **Error handling (HARD RULE):** every inline error banner is persistent + has a **Copy** button. `error.tsx` boundary likewise. Banners show generic retry/copy guidance (the old "schema not exposed" copy was removed once the public `cenapro_*` accessors went live).
- **Excel Standard:** `table-fixed`, explicit px widths, `px-2 py-1`, `h-8` rows, `text-xs`, `font-mono` right-aligned numerics, `tabular-nums`.
- **Navbar:** registered in `getBreadcrumb()` (`/cenapro`, `/cenapro/production`, `/cenapro/inventory`) + a **separate "Cenapro · Cebu" section** in the Modules dropdown (distinct from "ICTC · Davao").

## Editable-ledger lookups (hardcoded, FK-safe)
The categorical production-event columns are FK-constrained to the seeded `cenapro` lookup tables, which are **not exposed** over PostgREST. So the canonical option lists are **hardcoded constants in `types.ts`** — each string MUST match a seeded `code` value EXACTLY or the FK insert fails. Keep them in lockstep with the cenapro seed migration.

| Column | Options (constant) | Nullable |
|---|---|---|
| `shift_code` | `M` `E` `N` (`SHIFT_CODES`) | no |
| `grade_code` | `3X50` `2X6` `3.5` `4X8` (`GRADE_CODES`) | no |
| `plant_code` | `W6` `W7` `W6/W7` `DVO` (`PLANT_CODES`) | **yes** |
| `warehouse_code` | `WHSE 1/2/3/5/7` (`WAREHOUSE_CODES`) | **yes** (null = unplaced) |
| `source_location_code` | `TNK 1–4` `W6` `W7` `FLEC` `DVO` (`SOURCE_LOCATION_CODES`) | no |
| `disposition_kind` | `flec_bagging` `partner_crusher` `partner_kiln` (`DISPOSITION_KINDS`) | no |
| `partner_equipment_code` | crushers `C1–C4` / kilns `RK1–RK4` / none — **depends on disposition** (`partnerEquipmentOptions()`) | depends |
| `whse_side` | `LS` `RS` or blank (`WHSE_SIDES`) | **yes** |

> **UI note:** `disposition_kind` + `partner_equipment_code` are NOT two separate on-screen columns. The grid + bulk modal render a SINGLE `CCC/FLEC` column (Excel parity) whose options are `CCC_FLEC_OPTIONS` = `FLEC`/`C1`–`C4`/`RK1`–`RK4`. The DB schema stays normalized — the two fields are derived from / formatted to that one cell via the shared `parseCccFlec` / `formatCccFlec` helpers. See "CCC/FLEC single column" below.

## CCC/FLEC single column (one UI column ⇄ two normalized DB fields)
Renzo's Excel has ONE `CCC / FLEC` column; the DB normalizes it into `disposition_kind` + `partner_equipment_code`. Splitting it into two on-screen columns was redundant AND broke copy-paste alignment, so the UI collapses them back into a single `CCC/FLEC` column (between WEIGHT and the FLEC count, matching the Excel column order `… WHSE · SRC · WT · CCC/FLEC · FLEC AMT · WHSE SIDE`). **The DB schema is unchanged** — only the UI merges; the two fields are derived on save.

- **Shared helpers (`types.ts`, pure):** `parseCccFlec(raw)` → `{disposition_kind, partner_equipment_code} | null` (forward; forgiving case/alias; null on unrecognized) and `formatCccFlec(disposition, equipment)` → the single Excel string (inverse). `CCC_FLEC_OPTIONS` is the typeahead/datalist list. These are the canonical pair used by BOTH the inline grid's `saveProductionEvents` payload build AND the bulk modal (via `bulk-paste-utils.mapBulkRowToDirty`).
- **Mapping:** `FLEC` ⇄ `flec_bagging` + null equipment; `C1`–`C4` ⇄ `partner_crusher` + that code; `RK1`–`RK4` ⇄ `partner_kiln` + that code.
- **Inline grid:** the cell is a raw-text `ccc_flec` field (seeded via `formatCccFlec` on load, edited directly, parsed via `parseCccFlec` on Save). A paste-friendly `<Input list>` (datalist), not a dropdown — so a pasted `FLEC`/`C1`/`RK3` lands directly and copy round-trips the raw value.
- **Bulk modal:** `ccc_flec` is a `BulkRow` field (raw on paste, canonicalized on Save via `parseCccFlec`).
- **Validation:** a single typed cell can't produce an inconsistent (disposition, equipment) pair, so the old cross-field CHECK dance is removed; the only failure is an unrecognized value, surfaced as a persistent copyable `errorToast` (HARD RULE) naming the offending row(s).
- **Round-trip proven (2026-06-02):** `C1`→`partner_crusher`+`C1`; `FLEC`→`flec_bagging`+null; `RK3`→`partner_kiln`+`RK3`; and DB→display→parse→DB is identity for all 5 disposition/equipment combos (18/18 assertions). Paste alignment fix = the grid + bulk column maps now both have 12 data columns in Excel order (`ccc_flec` at index 10, between `weight_kg` and `flec_count`), so a pasted tab-block maps positionally.

## Column filtering — the FILTER axis (`?shift=&grade=&plant=&whse=&src=&ccc=`, 2026-08-03)

Per-column **MULTI-SELECT** filtering on the ledger's six categorical columns — **Shift · Grade · Plant · Whse · Source · CCC/FLEC**. It replaced the old single-select `DropdownMenuRadioGroup` (`'ALL'` = no filter) that lived only in the focus grid and covered only five columns (CCC/FLEC, the column Renzo actually asked about, had no filter at all).

### The contract
- **Empty selection = no filter = show all**, and the param is OMITTED (clean URLs). There is deliberately no "show nothing" state, so a filter can never strand the operator on an empty grid with no way back.
- **Columns AND together; values within a column OR together.**
- `__NULL__` is the sentinel for a blank cell (Plant "— None", Whse "— Unplaced") — the same sentinel the pre-multi-select grid used.
- Values are the raw DB codes, so a URL reads plainly: `?whse=WHSE+1,__NULL__&ccc=FLEC,C1`. Unknown values are preserved verbatim rather than dropped.
- State lives ONLY in the URL (CLAUDE.md rule) → shareable, reload-safe, back-button-correct, and readable by the server page. Owned by `ledger-url.ts`; written by `useLedgerFilters()`.

### CCC/FLEC filtering — ONE column, TWO DB fields, an OR across them
Because the on-screen `CCC/FLEC` column stands in for `disposition_kind` + `partner_equipment_code`, a selection like `{FLEC, C1}` is **an OR spanning two columns**:

- `FLEC` ⇒ `disposition_kind = 'flec_bagging'`
- `C1`–`C4` / `RK1`–`RK4` ⇒ `partner_equipment_code IN (…)`

`cccFlecPredicateParts()` in `ledger-url.ts` is the ONE place that split is expressed. `actions.ts` turns it into PostgREST — `.or('disposition_kind.eq.flec_bagging,partner_equipment_code.in.("C1")')` when both sides are picked, and a plain single-column `.in()` when only one side is (no `or` needed). The client matcher compares against the merged `formatCccFlec()` cell. Same rule, two renderings. The menu groups the values **Bagging / Crushers / Kilns** with a one-click group toggle, because picking C1–C4 individually was the exact tedium being complained about.

### Endless filtering is a QUERY, not a view — non-negotiable
The endless sheet is a virtualized keyset pager. A client-side hide there would filter only the LOADED window while scrolling kept fetching unfiltered pages, and `hasOlder`/`hasNewer` would describe the UNFILTERED history — "show only C1" could look nearly empty while thousands of C1 rows sat further back. So in **endless scope the predicates go into the SQL**: `applyLedgerFilters()` is applied to every `fetchLedgerPage` branch (anchor-latest, the period anchor's first-row resolution, the period forward load, the `hasOlder` count probe, cursor-older, cursor-newer), and `useLedgerWindow` carries the filters into every page it pulls. The keyset walk itself is filtered, a page = the next 100 MATCHING rows however far back they are, and the edge flags stay truthful.

- **Composition trap (verified, don't re-litigate):** PostgREST ANDs repeated top-level params **including repeated `or=`**, which is what lets a filter `.or(...)` coexist with the composite-keyset `.or(...)` already on these queries. Measured against live data: keyset-only 752 rows, ccc-only 892, both applied **580** — strictly narrower than either, and byte-identical to the nested-`and(or(…),or(…))` spelling. Values with spaces/slashes (`WHSE 1`, `W6/W7`) are double-quoted inside `in.(…)`.
- **A filter change RESETS the window via the React key** (`` `${anchorKey}|${filterKey}` `` in `page.tsx`), so the server re-prefetches the filtered first page and `firstItemIndex` resets by construction — the pager's prepend-anchor invariant is never violated and there is no scroll jump.
- **A period with ZERO matches does not dead-end.** `fetchLedgerPage`'s period branch falls back to the newest matching rows anywhere in history and says so in `notice`, instead of returning an empty window with both edge flags dead (nothing to scroll toward).
- **Focus scope stays client-side** — it loads one bounded period, so hiding is complete and instant. Both scopes read the SAME `LedgerFilters` from the SAME URL params and the same `matchesLedgerFilters` semantics; only the mechanism differs.

### Options come from the canonical domain, not the loaded window
Deriving options from loaded rows (the old behavior) made the menu mutate while scrolling and made a value that exists only further back unreachable. `FILTER_SPECS` sources them from the seeded `cenapro` lookup constants in `types.ts` instead, plus `mergeDiscoveredOptions()` appends any present-but-unmapped value (badged `new`). Options with no rows in the current window are **dimmed + "none here"** — never hidden, never disabled, because in endless scope the query WILL still find them.

### UNSAVED WORK IS NEVER HIDDEN — the rule, per scope
A dirty row silently disappearing behind a filter destroys work the operator cannot see. The two scopes protect it differently because the mechanisms differ, and BOTH state it in the UI:

- **Focus (client-side hide):** a `new` / `modified` / `deleted` row is **EXEMPT from every filter** and stays in place with its dirty rail. The toolbar reads e.g. `10 of 60 rows · 1 unsaved · 1 unsaved kept visible` (`exemptVisibleCount`). Nothing refetches, so keeping the row costs nothing. *Verified:* with `?whse=WHSE+3` active, editing a visible row's warehouse to `WHSE 1` keeps it on screen and bumps the "unsaved kept visible" counter.
- **Endless (server-side predicate):** changing a filter REFETCHES the window, and a pending edit whose row falls outside the new window would be counted as unsaved while being invisible. So all six filter menus **and** the chip's clear button are **disabled while `totalDirty > 0`**, with the hint *"Save or discard your unsaved changes before filtering — filtering reloads the window."* — the same dirty-guard idiom the period picker / view switcher / scope toggle already use. Draft rows are appended client-side and are never subject to a filter at all. After a Save with filters active, rows the refetched window legitimately can't bring back are counted and surfaced as *"N saved rows hidden by filters"* with a one-click **Show**.

### Affordances
Toolbar `FilterSummaryChip` (`Filters · N ✕`), per-column count chip + primary-tinted chevron on the header, the existing "Clear filters" button kept working, a `filtered` marker in the endless row counter, and a `FilteredEmptyState` that NAMES the responsible filters (`Shift M · CCC/FLEC C3, C4`) rather than a bare "nothing here". Header cells stay `h-8` — the trigger is the same tiny chevron as before.

## Production write path (`saveProductionEvents` → auto-updatable VIEW)
`public.cenapro_production_events` is an **auto-updatable VIEW** with INSERT/UPDATE/DELETE granted to **`authenticated`** (SELECT also to `anon`; `service_role` gets SELECT only — so service-role/anon writes are correctly denied). The server action runs as the logged-in user (`authenticated`), so:
- **Upsert:** `supabase.from('cenapro_production_events').upsert(rows)` — rows WITH `id` UPDATE, rows WITHOUT INSERT.
- **Delete:** `supabase.from('cenapro_production_events').delete().in('id', ids)`.
- The base trigger computes `unique_tag` + `batch_year` — both are **READ-ONLY, never sent**. (`saveProductionEvents` deletes first, then upserts; coerces strings→number/date and strips empties→null via the view's generated `Insert` type; surfaces DB errors as a single string → `errorToast`.)
- The DB enforces CHECK `production_event_partner_equipment_presence` (bagging ⇒ equipment NULL; non-bagging ⇒ equipment NOT NULL); the grid guards both halves client-side.
- **Write path proven (2026-06-01)** by running INSERT/UPDATE/DELETE as the `authenticated` role directly in Postgres: an INSERT with no `id`/`unique_tag`/`batch_year` returned `batch_year=2026` and a computed `unique_tag`; UPDATE + DELETE through the view both succeeded; CHECK confirmed. Throwaway proof cleaned up (0 leftover rows). `npx tsc --noEmit` = 0 errors.

## Opening-balance write path (`saveOpeningBalances` → append-only RPC)
The Flec Inventory STARTING block is written through a **dedicated append-only RPC**, NOT view DML (the `warehouse_opening_balance` table intentionally has no UNIQUE — every "set" is a new row so history is preserved):
- **`public.cenapro_set_opening_balance(p_warehouse_code, p_grade_code, p_side, p_effective_date date, p_count int)`** — SECURITY INVOKER; **INSERTS** a new entry (never UPDATE/DELETE) and RETURNS it. Granted to `authenticated` + `service_role` only (NOT `anon`).
- **`saveOpeningBalances(changes[])`** loops the RPC **once per CHANGED cell** (the grid diffs draft vs seed and sends only edited cells). `effectiveDate` = the page's START date for all cells. Stops at the first failing cell and returns `"Failed to save <grade>/<side> (= <count>, as of <date>): <msg>"` → `errorToast` (persistent + Copy); earlier cells already committed (each RPC is its own INSERT). On success: `revalidatePath('/cenapro/inventory')`, the client toasts + `router.refresh()` so the grid re-seeds and the ledger re-derives from the new opening.
- **Effective semantics:** the opening effective as of date D = greatest `period_start_date ≤ D`, tie-broken by greatest `created_at`. So saving the same (grade, side) at the same START date again simply supersedes (a new later-`created_at` row wins) while the prior value stays in history. Changing the START date sets a fresh opening for a new period.
- **Write path proven (2026-06-01)** from Node via the SDK: `cenapro_set_opening_balance` as the **`anon`** key → `permission denied for table warehouse_opening_balance` (correct — the server action runs as `authenticated`); as **`service_role`** → INSERT returned the row, `cenapro_opening_balances` read it back as the effective opening, and `cenapro_opening_balance_history` listed it. Throwaway sentinel row (WHSE 2 / 4X8 / LS @ 2000-01-01) hard-deleted afterward (0 leftover). `npx tsc --noEmit` = 0 errors; `npm run build` clean.

## Data path (public `cenapro_*` accessors — NOT `.schema('cenapro')`)
The cenapro data is reached through **accessors that live in the `public` schema**, which PostgREST already exposes:
- a VIEW `public.cenapro_production_events` (16 columns) — read by `fetchProductionEvents`, **written** by `saveProductionEvents` (auto-updatable; see "Production write path").
- two set-returning functions `public.cenapro_flec_balance(...)` / `public.cenapro_flec_ledger(...)` (read-only).
- three opening-balance accessors `public.cenapro_opening_balances(...)` / `public.cenapro_opening_balance_history(...)` (read-only) + **`public.cenapro_set_opening_balance(...)`** (append-only write RPC, `authenticated`/`service_role` only) — see "Opening-balance write path".
- **CCC analysis (2026-08-01):** a writable VIEW `public.cenapro_analysis_samples`, three read-only VIEWs `public.cenapro_ccc_sample_groups` / `_ccc_analysis_daily` / `_ccc_analysis_monthly`, and two RPCs `public.cenapro_save_analysis_sample(...)` (authenticated) / `public.cenapro_import_analysis_samples(jsonb)` (service_role only). **These four views grant SELECT to `authenticated` ONLY — `anon` is revoked**, unlike the older accessors above. See "CCC Analysis — LIVE BACKEND".

Reads are granted to `authenticated` + `anon` (writes to `authenticated`), so the **standard Supabase client reaches them directly** — `supabase.from('cenapro_production_events')` and `supabase.rpc('cenapro_flec_balance'|'cenapro_flec_ledger'|'cenapro_opening_balances'|'cenapro_opening_balance_history'|'cenapro_set_opening_balance', …)`. No `.schema('cenapro')`, no structural casts.

**Why this path (and not exposing the `cenapro` schema):** flipping PostgREST's "Exposed schemas" toggle to add `cenapro` would surface the *entire* schema's API surface and is a project-wide setting. Wrapping just the objects the UI needs as `public.cenapro_*` accessors keeps the blast radius minimal while still giving fully-typed, first-class client access.

**Types:** `types.ts` derives all row shapes from the generated `types/supabase.ts` (no hand-written interfaces), so they auto-update on every `supabase gen types typescript --linked`:
- `ProductionEventRow = Database['public']['Views']['cenapro_production_events']['Row']`
- `FlecBalanceRow = Database['public']['Functions']['cenapro_flec_balance']['Returns'][number]`
- `FlecLedgerRow = Database['public']['Functions']['cenapro_flec_ledger']['Returns'][number]`
- `OpeningBalanceRow = Database['public']['Functions']['cenapro_opening_balances']['Returns'][number]`
- `OpeningBalanceHistoryRow = Database['public']['Functions']['cenapro_opening_balance_history']['Returns'][number]`
- `OpeningBalanceCellChange` — hand-authored payload interface (one changed STARTING cell: `warehouse`/`grade`/`side`/`effectiveDate`/`count`) for `saveOpeningBalances`; the only non-derived type, since it's a UI→action contract, not a DB row.

Notes:
- The production `.select(...)` column list MUST be a **single string literal** (not `+`-concatenated) — the typed PostgREST client parses it at the type level to infer the row shape; concatenation defeats inference and falls back to an error type.
- The upsert payload is typed against `Database['public']['Views']['cenapro_production_events']['Insert']` (the auto-updatable view's generated Insert block; identical to Update). New rows omit `id` entirely so the base default generates it.
- `formatDisposition(disposition: string | null, …)` is typed loosely (string) to consume the generated row types directly; at runtime it's always one of the three known kinds, unknown values render as-is.
- The old `ProductionDailyRow` stopgap interface (for `view_production_daily`) was **removed** — it had no consumer and no matching `public.cenapro_*` accessor in the generated types. Re-derive it from generated types if/when that view is surfaced and exposed.
- **Live data confirmed from Node** via the anon key (2026-06-01): `cenapro_production_events` count = 752; `cenapro_flec_balance(WHSE 7, 2026-03-10)` 3X50/RS `current_flec` = 56; `cenapro_flec_ledger(WHSE 7, 2026-03-10)` = 100 rows, all 16 columns; `cenapro_opening_balances(WHSE 7, 2026-03-10)` = 3X50/RS 53 + 2X6/LS 26 (the seeds). The browser pages are auth-gated (a `GET /cenapro` returns 307 → login when unauthenticated), so this Node check is the runtime proof short of a logged-in session.

## Dependencies
- `@/lib/supabase/server` — `createClient()` (server Supabase client; reads the public `cenapro_*` view/RPCs via `.from()` / `.rpc()`, **writes** the auto-updatable production view via `.upsert()` / `.delete()`, and writes opening balances via the append-only `cenapro_set_opening_balance` RPC).
- `@/types/supabase` — generated `Database` type; `types.ts` derives the cenapro row shapes from it; the production upsert payload uses the view's `Insert` block; the five flec/opening RPC row shapes derive from the function `Returns`.
- `@/components/shared/grid/{GridCell,SelectCell,DatePickerCell,EditInput,EDIT_INPUT}` — the shared Excel-style cells: `GridCell` (display/edit modes, selection feedback), `SelectCell` (categorical dropdown cell) and `DatePickerCell` (native date cell + `formatDateShort`) which the production ledger IMPORTS from the shared barrel (local copies promoted to `components/shared/grid/` in Phase 0, deleted from the ledger in Phase 2 — byte-identical, no drift), and **`EditInput` + `EDIT_INPUT`** which the Daily Block (`production-daily-block.tsx`) IMPORTS (its local `EditInput`/`EDIT_INPUT` were the Phase-0 promotion source and have been deleted; the Daily Block's `CellEditor` mounts the shared `EditInput` in edit mode). See `components/shared/grid/CONTEXT.md`.
- `@/lib/hooks/use-grid-keyboard-nav` (`useGridKeyboardNav` + `createDomOrderNavResolver` + `DomNavCell`) and `@/lib/hooks/use-grid-edit-session` (`useGridEditSession`) — the Daily Block's keyboard state machine + edit session (Phase 3). DOM-order resolver over `[data-navid]`; locked cells carry no `data-navid` so nav skips them.
- `@/lib/hooks/{use-cell-selection,use-clipboard-copy,use-cell-delete}` — cell range selection, Ctrl+C TSV copy, Backspace/Delete clear (shared with RC IN / RC OUT / ICTC daily ledger).
- `@/lib/hooks/{use-grid-keyboard-nav,use-grid-edit-session,use-grid-paste}` — the shared Blackwood Table grid primitives. The production ledger + the endless sheet's draft entry zone consume `useGridKeyboardNav` (keyboard state machine + `createCoordinateNavResolver`, wrapped per-grid to clamp ArrowLeft to col 1 and — ledger only — to gate inline editing to typable columns via a custom `isEditable`) and `useGridEditSession` (owns `isEditing` + pre-edit snapshot + start/revert/commit; the draft entry zone passes `onAfterCommit` = its typed-date normalizer; the ledger keeps a CUSTOM revert that also un-dirties a reverted modified-existing row). The draft entry zone also uses `useGridPaste`; the ledger keeps its OWN `handleSmartPaste` because its paste must mark pasted existing rows `modified` (the shared paste hook doesn't touch `_state`). Home/End (both grids) and single-cell concerns are handled in thin container wrappers. `enableEnterAnchor`: **true** for the draft entry zone (Tab-then-Enter returns to the run's lane — the old `enterAnchorColRef` behavior, now from the shared hook), **false** for the ledger (plain Enter drops straight down).
- `@/lib/paste-utils` — `parseExcelDate`, `trimCellValue` for paste-from-Excel cleaning, and **`normalizeTypedDate(input, defaultYear)`** for Excel-style commit-time auto-transcription of TYPED shorthand dates (`"6/2"`→`yyyy-MM-dd`, injecting `defaultYear` when the year is omitted; invalid/empty passed through unchanged) — used by the draft entry zone's date cells + `mapBulkRowToDirty` safety net.
- `@/lib/toast` — `errorToast()` (persistent + Copy; HARD RULE) for all save/validation errors.
- `@/components/ui/{table,select,card,dropdown-menu,button,input,popover,collapsible,badge,dialog}` — Shadcn primitives (`dropdown-menu` powers the production `SelectCell` + header filters; the Flec Inventory page uses `select` for the warehouse picker, `popover` for the per-cell STARTING history, and `collapsible` for the whole-warehouse history panel).
- `@/lib/utils` — `cn()`.
- `react-virtuoso` — `TableVirtuoso` (the endless sheet's virtualizer; chosen for its `firstItemIndex` prepend-anchor primitive — `@tanstack/react-virtual`, used elsewhere for full-dataset renders, has no equivalent). Consumed ONLY by `production-endless-sheet.tsx`.
- `date-fns` — `format`, `parseISO`, `isValid` (date display — `yyyy-MM-dd`).
- `sonner` — success/info toasts (paste, save, copy); errors go through `errorToast` per HARD RULE.
- `lucide-react` — icons.

## CCC / QC Analysis — THE LIVE FEATURE (`/cenapro/qc`, `/cenapro/qc/breakdown`)

> ✅ **Shipped 2026-08-01.** Renzo evaluated four candidate designs at the throwaway
> `/cenapro/analysis-drafts/{a,b,c,d}` routes, picked **A for entry** and **D for reading**,
> and the two were promoted here. **The draft tree, `lib/cenapro/ccc-analysis-draft.ts` and the
> five draft breadcrumb entries are DELETED** — do not resurrect them; the drafts' visual
> decisions (round-2 column widths, Month+Year dropdowns, recharts, spreadsheet keyboard nav,
> `Σ DAY TOTAL` rule-offs, the opaque frozen month footer, no stacked bar charts) all survive
> in the promoted routes. B and C were rejected and are gone entirely.

Two routes over one shared **`?m=YYYY-MM`** month axis, cross-linked both ways:

| Route | Role | Scope | Writes |
|---|---|---|---|
| `/cenapro/qc` — **QC Ledger** | ENTRY. The CCC-CI ANALYSIS sheet as a dense 12-column grid; the four metric columns are editable, everything left of them is reference. | **`scope='all'`** — the entry surface must show every receipt an operator can type against, DVO included. | Yes — `cenapro_save_analysis_sample`, one RPC per sample group. |
| `/cenapro/qc/breakdown` — **QC Breakdown** | READING. Monthly + daily weighted analytics, four metric trend panels. | **`scope='ex_dvo'`** — the sheet's headline framing. | No. Read-only by design. |

### Files
| File | Role |
|------|------|
| `qc/page.tsx` | Server — resolves `?m=` (via `resolveQcMonth`), awaits `loadQcLedgerData`, renders `QcLedgerClient`. `force-dynamic`. |
| `qc/data.ts` | **The ONE read path for BOTH routes.** Not a server action (no `'use server'`, no mutations) — plain async functions the two `page.tsx`s await. `loadQcMonthKeys()` (the month picker's axis, from the monthly view), `loadQcLedgerData(month, monthKeys)` (four parallel reads: sample groups + production events + `_analysis_daily scope='all'` + `_analysis_monthly scope='all'`, assembled into day blocks), `loadQcBreakdownData(month, monthKeys)` (monthly + daily at `scope='ex_dvo'`). Exports `QcDraw` / `QcGroup` / `QcLedgerDay` / `QcMonthRow` / `QcDayRow` / the two `*Data` shapes. **The view IS the grain:** groups are built from `cenapro_ccc_sample_groups` rows and the production-event draws are hung off them by `sampleGroupKey(date, canonToken(src), effectiveWhseKey(row))`; a draw whose group the view does not carry (impossible today — the view is built from those rows) synthesizes an orphan group rather than silently dropping tonnage. |
| `qc/actions.ts` | `'use server'` — **`saveQcSamples(inputs)`**. One `cenapro_save_analysis_sample` RPC **per sample group**, threading that group's own `sample_row_version` through unchanged (`null` → insert, integer → update, version checked in the same statement as the write). Returns a per-group `SaveQcSampleResult` (`key`, `ok`, `outcome`, `rowVersion`, `message`) so a conflict on one group never blocks the others. Never retries, never force-writes, never re-reads a version to "fix" a conflict. An all-null payload short-circuits to `no_metrics` without a round trip. `revalidatePath('/cenapro/qc')` + `revalidatePath('/cenapro/qc/breakdown')` when anything landed. Metric args are OMITTED when null (every metric param is `DEFAULT NULL`, so omitting clears on the UPDATE path exactly as an explicit null would, and the generated `Args` type declares them `number`). |
| `qc/qc-ledger-client.tsx` | Client — the entry grid. 12 columns (`Date · Prod · Sh · Grade · Plant · Whse · SRC · WT kg · BD · ASH · GRIT · MC`) at widths `62/62/40/62/60/62/62/88` + `124×4`, table min-width **994px**, so the four analysis columns hold ~50% of the table at EVERY viewport (a `table-fixed` table distributes surplus in proportion to declared widths). Three-tier structure: faint `bg-muted/25` day HEADER (date + sample status), the day's draw rows, a ruled-off **`Σ DAY TOTAL`** (`bg-muted`, `border-t-2 border-t-foreground/45`, bold tabular figures, amber `wtd of NN% sampled` caption under 100%), and a **frozen `Σ <MONTH>` footer** (`<tfoot>` + `.frozen-row-bottom` + `.frozen-edge-top`, solid `bg-muted` — never glass) inside the grid's own `min-h-0 flex-1 overflow-auto` scrollport, which is also the keyboard host. KPI tiles ×4 with DAILY sparklines and a signed (never colour-coded) delta vs the prior month. Save bar + `Discard`; the toolbar carries the Month/Year picker, the source filter, the logged/kg/coverage chips and the quiet **`QC Breakdown →`** link. Mobile: `hidden sm:flex` grid + `NarrowScreenNotice`. |
| `qc/breakdown/page.tsx` | Server — resolves `?m=`, awaits `loadQcBreakdownData`, folds the months into the whole-history line via `combineAggregates`, builds the monthly + daily metric series, finds the newest zero-sample month. `force-dynamic`. |
| `qc/breakdown/qc-breakdown-client.tsx` | Client — KPI tiles ×4 (Received ex-DVO · Wtd BD · Wtd MC · Sample coverage), then **both rollup tables side by side** in one `xl:grid-cols-[minmax(340px,0.85fr)_1.15fr] items-start` band (`RollupTable`: Mo · TTL WT · BD · ASH · GRIT · MC · Cov, zero-sample months render an amber "no samples yet — enter … analysis" cell spanning the five metric columns, plus a sticky `Σ All` footer that totals **weight only** and says so; `DailyRollupTable`: Day · DOW · TTL WT · 4 metrics · Cov, sticky header + sticky `Σ <MONTH>` footer, scrollport capped `max-h-[420px]`), then the four `MetricTrendPanel` small multiples with the `Daily | Monthly` `GrainToggle`. A quiet **`← QC Ledger (entry)`** link in the toolbar. **No `hidden sm:block`** — the breakdown degrades gracefully on a phone (tiles go 2-up, each table scrolls inside its own scrollport, the page itself never scrolls horizontally). |
| `qc/qc-chrome.tsx` | `Chip` / `Tile` / `Panel` (optional `actions` header slot) / `NarrowScreenNotice`. Presentational, no directive — a server page and a client component can both render them. |
| `qc/qc-metric-charts.tsx` | Client — the recharts chart chrome. `METRIC_COLOR` (one platform `--chart-N` token per metric: bd→1, ash→2, grit→3, mc→4 — tokens not hex, because a recharts `stroke` is a string prop and a Tailwind `dark:` class would silently do nothing), a themed `tooltipChrome()`, a padded y-domain helper, **`MetricSpark`** (the ledger tiles' `h-10` area spark, `connectNulls={false}`, dots auto-enabled at ≤14 samples) and **`MetricTrendPanel`** (the breakdown's small multiple — CartesianGrid, real axes, themed tooltip, `connectNulls={false}`, headline last-value in the metric's hue). **Layer rule:** the digest board's idiom is REPRODUCED here, never imported — `components/digest/**` is ICTC tenant code and Cenapro is Tenant #2. |
| `qc/month-year-picker.tsx` | `MonthYearPicker` — the shared `?m=YYYY-MM` control. Two shadcn `Select`s: **Month** always lists all twelve with the empty ones `disabled` + suffixed `· no data` (an absent month is information), **Year** lists only the years present, newest-first. A year change prefers the same calendar month in the new year, else that year's newest. `router.push` inside a `useTransition`, **preserving every other search param**. |
| `qc/load-error.tsx` | Inline load-error banner with a **Copy** button (error HARD RULE), client island. |
| `lib/cenapro/ccc-analysis.ts` | **The data contract.** Metric metadata (`METRICS`, `METRIC_LABEL`, `METRIC_SHORT`, `METRIC_DECIMALS`/`_WEIGHTED`, `METRIC_WEIGHT_COLUMN`), the `AnalysisScope` union, row types derived from `types/supabase.ts`, `canonToken()` / `effectiveWhseKey()` / `sampleGroupKey()`, and `SaveSampleOutcome` / `SaveSampleResult` / `SaveSampleArgs`. **Contains no aggregation math by design.** |
| `lib/cenapro/ccc-analysis-view.ts` | **The presentation layer over the SQL aggregates.** `QcAggregate` + `aggregateFromView(row)` (pure renaming of a daily/monthly view row; takes the structural `AggregateViewRow` so a caller may `.select()` a subset), `combineAggregates(parts)` (folds periods by summing each one's published NUMERATOR `wtd_X × wtd_X_kg` and DENOMINATOR `wtd_X_kg` — exact, and specifically NOT the mean-of-means the monthly footer refuses to print), `adjustAggregate(base, adjustments)` (see below), `MetricValues`/`hasAnyMetric`, `SeriesPoint`, the month helpers (`monthKey`/`monthLabel`/`formatMonthHeading`/`shiftMonth`/`monthBounds`/`resolveQcMonth`) and the display formatters (`formatKg`/`formatCompactKg`/`formatMetric`/`formatCoverage`/`formatDayHeading`/`formatShortDate`/`formatDayOfMonth`/`formatDow`). |
| `lib/cenapro/ccc-analysis-fixture.json` | **HISTORICAL PROVENANCE — not read at runtime by anything.** The 500 lab samples transcribed from Renzo's CCC-CI ANALYSIS sheet, keyed `"YYYY-MM-DD\|SRC\|WHSE"`; it was the INPUT to the live backfill (`scripts/cenapro/backfill-ccc-analysis-samples.mjs`, its only remaining reader) and all 500 rows now live in `cenapro.analysis_sample`. Kept in the repo as the record of what was transcribed. **Generated — never hand-edit.** |

### The one piece of arithmetic on the client, and why it is not a second implementation
CLAUDE.md forbids computing weighted averages in TypeScript, and neither page does. Totals, coverage, the four weighted averages and their per-metric denominators are all READ from `cenapro_ccc_analysis_daily` / `_monthly`.

`adjustAggregate()` does **not** re-aggregate. It takes a SQL aggregate and applies per-group DELTAS to the numerator/denominator pair SQL already published, so:

- with **no** adjustments it returns the SQL row's numbers untouched (an early return — not even a float round-trip), which is why the regression figures below render byte-for-byte from SQL;
- with adjustments it answers the one question the DB cannot answer yet: *"what would this total read if the number I am typing right now were saved?"*

Two callers, both on the ledger: an **unsaved edit** (swap the group's stored metrics for the typed ones) and the **source filter** (`dropWeight: true` — the group's tonnage leaves the period too). A save writes through the RPC, `revalidatePath` + `router.refresh()` land the DB's own answer, and the preview disappears. There is no path where a rendered number is a TypeScript re-derivation of stored state.

### Saving — one RPC per sample group, four outcomes surfaced
The Save button builds one payload per dirty group and calls `saveQcSamples`. Results are applied per group: succeeded groups lose their pending text and a normal auto-dismissing success toast fires; **failed groups KEEP what was typed** so nothing has to be retyped.

| Outcome | Surfaced as |
|---|---|
| `inserted` / `updated` | `toast.success('Saved N sample groups')` (2.5s) + `router.refresh()`. |
| `version_conflict` | Persistent inline banner + `errorToast`: *"Someone else changed this sample while you were editing. Reload to see their values."* A **Reload** button re-fetches. Never a silent retry or force-write. |
| `already_exists` | *"A sample was logged for this group while you were typing. Reload, then edit the value that is now stored."* + Reload. |
| `not_found` | *"That sample was deleted while you were editing. Reload the ledger."* + Reload. |
| `no_metrics` | **Prevented client-side.** A group whose four metrics all end up blank never leaves the browser: the save bar turns amber (*"1 group has every metric cleared — that would delete the reading"*), the offending cells get a rose ring, the inline banner names the group, and NOTHING is sent. (The server action re-checks anyway.) |
| `invalid_key` / RPC error | The raw message, in the banner + `errorToast`. |

The inline banner is the non-toast half of the error HARD RULE: it persists until dismissed and carries a **Copy** button that grabs every failure line at once.

### The sample grain
A **sample group** is `(recv_date, source_location_code, effective warehouse)` — one lab sample covers every draw from the same source + warehouse on a day. Partner receipts are the rows where `partner_equipment_code IS NOT NULL`. The effective warehouse is `coalesce(warehouse_code, plant_code)`: FLEC/DVO rows carry a real `warehouse_code` (`WHSE 1/3/5/7`), while TNK 1–4 / W7 draws have it NULL and the sheet's WHSE is the `plant_code` (`W6`/`W7`). That is why the sample table stores a resolved NOT NULL `whse_key` and why `effectiveWhseKey()` exists.

**The `〃` fan-out** is the data model made visible: the editable cells live on a group's FIRST draw, and its sibling draws render `〃` and inherit the value. Typing on the lead row covers the whole group's weight — verified live: setting BD=1 on the 2-draw 2026-05-01 FLEC/WHSE 7 group moves the day total to **0.9254**, the full-group-weight answer (40,437 kg), not the lead-draw-only figure.

### The four metrics that must appear on both pages
Renzo's requirement: **daily kg totals, monthly kg totals, daily weighted averages, monthly weighted averages** — visible on the ledger AND the breakdown.

| Metric | QC Ledger (entry) | QC Breakdown (reading) |
|---|---|---|
| Daily kg | `Σ DAY TOTAL` row under every day block | daily rollup table's `TTL WT` column |
| Monthly kg | frozen `Σ <MONTH>` footer + the toolbar chip | monthly rollup row + the daily table's `Σ` footer |
| Daily wtd avgs | the four analysis columns of `Σ DAY TOTAL` **and** the KPI tile sparklines | daily rollup table + the `Daily` grain of the metric panels |
| Monthly wtd avgs | KPI tile headline **and** the frozen footer | monthly rollup row + the `Monthly` grain of the metric panels |

**Verified reconciliation figure (re-verified in the browser 2026-08-01, now sourced from SQL):** May 2026 = **1,134,070 kg**, weighted **0.5602 / 2.80 / 0.73 / 11.63**. Rendered identically by the ledger's toolbar chip, the ledger's frozen `Σ MAY 2026` footer, the breakdown's monthly MAY row and the breakdown's daily `Σ MAY 2026` footer. May carries no DVO, so `all` and `ex_dvo` coincide there — June and July do have DVO, which is why the ledger's footer captions the included DVO tonnage (`incl. 720,958 kg DVO` in July) while the breakdown stays strictly ex-DVO.

**Coverage is never silent.** A weighted average whose denominator is short of the row's total weight always says so: the day rows caption `wtd of NN% sampled`, the frozen footer captions each metric `wtd · NN%` (amber under 100% — May's ASH reads `wtd · 98%` because 2026-05-08 FLEC/WHSE 5 genuinely has no ASH), the toolbar chip carries the month's coverage, and both breakdown tables carry a `Cov` column. July 2026 renders kg with em-dash metrics and 0% coverage everywhere — correct, and the reason the feature exists. In the charts the same honesty is `connectNulls={false}`.

### Default month
Both routes resolve `?m=` identically (`resolveQcMonth`): a valid URL month, else **the newest month with receipts** (July 2026 today). Deliberately the same on both — they share one `?m=` axis and cross-link, so different defaults would make the first click between them look like a navigation bug, and the newest month is where the outstanding entry work is. *(The evaluation draft A defaulted to the newest well-covered month instead, which was an artefact of wanting the demo to show filled cells. One line in `resolveQcMonth` if Renzo prefers that back.)*

### Keyboard model (the ledger)
Renzo: *"tabbing and entering should allow us to navigate it like how excel/google sheet would let us."* The analysis cells run the canonical **Blackwood Table** stack: `useGridKeyboardNav` + `createCoordinateNavResolver` (`lib/hooks/`) over the shared `GridCell` + `EditInput` (`components/shared/grid/`) — the same pair RC IN's bulk grid and the production schedule use.

- **The coordinate space is `entryGroups.length × 4` — the analysis columns only.** Row `n` is the nth sample group in visual order. Day-header rows, `〃` sibling rows, `Σ DAY TOTAL` rows and the frozen footer have **no coordinates at all**, which is *why* nav can never land on one — there is no skip logic to get wrong.
- Tab off MC wraps to the next row's BD; Shift+Tab off BD wraps back to the previous row's MC; arrows clamp at the edges.
- `enableEnterAnchor: true` — the first Tab of a run records the lane, and a later plain Enter drops one row and **returns to that lane** (BD→ASH→GRIT→MC→Enter lands on the next row's BD).
- Click SELECTS (ring only); a printable key types over; F2 / double-click edits in place keeping the value; Escape reverts to the pre-edit snapshot.
- The `min-h-0 flex-1 overflow-auto` scrollport is the keyboard host (`role="grid"`, `tabIndex={0}`); cells are `tabIndex={-1}`, so Tab is the grid's to interpret and can never walk out of the sheet mid-entry.
- **`setCell` prunes no-op edits.** A value equal to the STORED one deletes its entry instead of recording one, so Escape (which replays the pre-edit snapshot through `setValue`) leaves the group genuinely clean rather than inflating the dirty count.

**Driven and verified in a real browser (2026-08-01):** click on BD row 0 → selected `(0,0)`, not editing; `Tab` → `(0,1)`; `Tab×3` → wraps to `(1,0)` (skipping the `〃` sibling draw entirely); `Enter` → `(2,0)`, the anchored lane; `ArrowLeft` at col 0 → clamps at `(2,0)`; `ArrowUp` → `(1,0)`; pressing `9` → editor opens pre-filled `"9"`, focus on the input; `Escape` → back to `0.563` and the save bar reads "No unsaved changes".

## CCC Analysis — LIVE BACKEND (shipped 2026-08-01)

**Migrations:** `supabase/migrations/20260801073405_cenapro_analysis_sample.sql` (table + accessor), `..073646_cenapro_ccc_analysis_views.sql` (aggregates), `..073853_cenapro_analysis_write_path.sql` (RPCs). **Backfill:** `scripts/cenapro/backfill-ccc-analysis-samples.mjs` (re-runnable; `--dry-run` supported). **Frontend contract:** `lib/cenapro/ccc-analysis.ts`.

### The table — `cenapro.analysis_sample`
One row per `(sample_date, source_location_code, whse_key)`. `bd` / `ash` / `grit` / `mc` are **independently nullable** (2026-05-08 FLEC/WHSE 5 genuinely has no ASH). Provenance: `source` (`sheet_backfill` | `app`), `notes`, `row_version`, `created_at/by`, `updated_at/by` (FK→`profiles`, ON DELETE SET NULL). RLS on, single-org (`authenticated` = full CRUD, `anon` nothing).

**The NULL-warehouse uniqueness trap, and how it's solved.** The grain's third component is the *effective* warehouse = `coalesce(warehouse_code, plant_code)` — tank and W7 draws carry a NULL `warehouse_code`. A UNIQUE spanning a nullable column would NOT de-duplicate those rows (`NULL <> NULL` in a unique index), so every tank sample would be insertable an unlimited number of times. The table therefore stores the **resolved token in a NOT NULL `whse_key` column** and the unique constraint `cenapro_analysis_sample_natural_key` spans three NOT NULL columns. `cenapro.fn_canon_token(text)` (IMMUTABLE: trim / collapse whitespace / uppercase) is the ONE normalization definition, shared by the table's CHECKs, the views' join key and the write RPCs; `canonToken()` in `lib/cenapro/ccc-analysis.ts` is its JS twin. CHECK bounds are **unit sanity only** (bd 0–5, the three percentages 0–100) — deliberately far wider than the observed sheet ranges, because a tight "observed range" bound would reject the outlier an analyst most needs to see.

### The views (aggregation lives HERE, not in TS)
| Object (public accessor) | Grain | Provides |
|---|---|---|
| `cenapro_analysis_samples` | one lab sample | The raw ledger, **all 500 rows** including the 117 that match no event group yet. Auto-updatable. |
| `cenapro_ccc_sample_groups` | (date, src, effective whse) | `draw_count`, `total_kg`, the joined `bd/ash/grit/mc`, `is_sampled`, `is_complete`, `missing_metric_count`, `source_group`, `is_dvo`, and **`sample_row_version`** (feeds the save RPC — NULL means "unsampled, create it"). |
| `cenapro_ccc_analysis_daily` | `sample_date` × `scope` | `total_kg`, `sampled_kg`, `coverage`, group counts, the four `wtd_*` + their per-metric denominators `wtd_*_kg`. |
| `cenapro_ccc_analysis_monthly` | `month_start`/`month_key` × `scope` | Same, plus `day_count`. |

**Weighted average = `SUM(kg × metric) / SUM(kg)` over the groups carrying THAT metric, per metric independently** — a group missing ASH still contributes its full weight to BD/GRIT/MC, which is why each metric ships its own denominator. Proof in live data: 2026-05-08 has `wtd_bd_kg` = 55,207 but `wtd_ash_kg` = 35,233.

**Ex-DVO is a `scope` COLUMN, not a frontend filter.** The daily and monthly views emit **two rows per period**: `scope='all'` (entry ledger) and `scope='ex_dvo'` (the reading page's headline). Both come from one aggregation expression so they cannot disagree, and `all_kg` / `dvo_kg` / `ex_dvo_kg` are period-wide on *either* row — so a single `ex_dvo` row already carries the breakdown's whole day shape (ex-DVO total + DVO tonnage + ex-DVO weighted averages) without a second query. Always filter on `scope`.

**Regression, verified from SQL:** `month_key='2026-05'`, `scope='ex_dvo'` → `total_kg` **1,134,070**, wtd **0.5602 / 2.80 / 0.73 / 11.63** — identical to what the drafts render.

### The write path
- **`public.cenapro_save_analysis_sample(p_sample_date, p_source_location_code, p_whse_key, p_bd, p_ash, p_grit, p_mc, p_expected_row_version, p_notes) → jsonb`** — SECURITY INVOKER, `authenticated` + `service_role`. Upserts by the natural key, canonicalizes the key server-side, stamps `created_by`/`updated_by = auth.uid()` and flips `source` to `'app'`. **Optimistic concurrency:** pass `p_expected_row_version = NULL` to CREATE (a losing race returns `already_exists`, never a clobber) or the `sample_row_version` you read to UPDATE (a mismatch returns `version_conflict` **plus the current version**). The version is checked in the SAME statement as the write — the `fn_save_schedule_day` idiom, no read-then-write. `row_version` is bumped by a **trigger**, so a raw write through the accessor view advances it too and cannot silently defeat the lock. Outcomes: `inserted` / `updated` / `already_exists` / `version_conflict` / `not_found` / `no_metrics` / `invalid_key`.
- **`public.cenapro_import_analysis_samples(p_rows jsonb) → jsonb`** — **`service_role` ONLY.** The idempotent sheet import. `ON CONFLICT DO UPDATE` is gated on `source = 'sheet_backfill'`, so a re-run can **never** clobber a reading an operator typed in the app, and on the values differing, so an unchanged re-run honestly reports `updated: 0`.
- **No delete RPC on purpose** — `public.cenapro_analysis_samples` is auto-updatable with DELETE granted to `authenticated`, and a delete carries no provenance an RPC could add.

### Backfill outcome (2026-08-01)
**All 500 sheet samples imported** — the ~117 that place nowhere are real lab readings for pre-June DVO container vans whose weights the DB does not carry yet (DVO is deferred out of Cenapro v1); dropping them would mean re-transcribing the sheet the day DVO lands, and they cost nothing because the views join *from* event groups. **383 groups carry a sample** (382 exact + 1 remap), **July 2026 = 144 groups / 0 sampled** — the live gap the feature fills.

The one remap: `2025-12-02|FLEC|WHSE 5` → `…|WHSE 7`, because the only Dec-2 FLEC draw in the DB is under WHSE 7; the original sheet key is preserved in `notes`. **Deliberate divergence from the draft's matcher:** the draft's `date|src` fallback also lends an already-placed sample to a *second* group (2026-02-22 FLEC/WHSE 7's reading shown against FLEC/WHSE 5, 2,249 kg). The backfill refuses that — the sheet never sampled that draw, and guessing would inflate coverage and pull a weighted average toward a number nobody measured. February coverage therefore reads **98.99%** here vs 99.2% in the draft; its four weighted averages are unchanged, and no other month moves.

### Grants trap (applies to every future `cenapro` object)
The `cenapro` schema carries a DEFAULT ACL `{anon=r, authenticated=arwd, service_role=arwd}` from the original `create_cenapro_schema` migration. **Every new relation is born with `anon=SELECT` and `authenticated=INSERT/UPDATE/DELETE`, silently, whatever the CREATE says** — which contradicts the Phase-4 "anon has no data access" posture and makes a read-only reporting view look writable. The views migration REVOKEs both explicitly; do the same for anything new.

## See Also
- [Blackwood Table (shared grid primitives)](../../../components/shared/grid/CONTEXT.md) — the production ledger + the endless sheet's draft entry zone consume the shared keyboard-nav / edit-session / paste hooks and the shared `GridCell`/`SelectCell`/`DatePickerCell`; the **QC Ledger** (`qc/qc-ledger-client.tsx`) consumes `useGridKeyboardNav` (COORDINATE resolver) + `useGridEditSession` + `GridCell`/`EditInput` over its four analysis columns; the **Daily Block** (`production-daily-block.tsx`, Phase 3 retrofit) consumes `useGridKeyboardNav` (DOM-order resolver `createDomOrderNavResolver`) + `useGridEditSession` + the shared `EditInput`/`EDIT_INPUT` and now follows the canonical click-to-select / type-to-edit model. RC IN's `bulk-delivery-input.tsx` is the canonical reference migration.
- `/Users/renzosy/blackwood/cenapro/CENAPRO_SCHEMA.md` — the authoritative schema contract (tables, `flec_ledger`/`flec_balance`, `view_production_daily`, the 28 ported business rules, DVO deferral).
- [Navbar](../../../components/NAVBAR.md) — page titles / breadcrumbs / Modules dropdown.
- [Production module](../production/CONTEXT.md) — ICTC production; source of the dense-table + `ColumnFilterMenu` patterns reused here.

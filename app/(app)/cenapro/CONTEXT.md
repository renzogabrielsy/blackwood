# Cenapro Module (Tenant #2 — CI / Cebu)

## Purpose
Top-level route `/cenapro` for the **second tenant** on the Blackwood platform: the CI / Cebu charcoal company ("Cenapro"). Two screens over the dedicated `cenapro` Postgres schema: an **editable** Production ledger (the app is now the maintaining file — operators add/edit/delete production rows here instead of in Excel) and a Flec Inventory view whose **STARTING (opening) balances are now editable + append-only + backtrackable** (the rest of the inventory view — closing balances + movement ledger — stays computed/read-only). Fully decoupled from the ICTC / Davao tenant — shares zero tables, types, or code.

> **Tenant/Domain Module (Cenapro):** Cebu-specific.
> - **Production (`/cenapro/production`)** is an **editable Industrial-Spreadsheet ledger** — inline cell editing, dropdowns for categorical columns, add/delete rows, paste-from-Excel, dirty tracking + single Save/Discard. It writes through the auto-updatable `public.cenapro_production_events` VIEW (see "Production write path" below).
> - **Flec Inventory (`/cenapro/inventory`)** mirrors the Excel "PC WHSE" sheet: an **editable STARTING block** (opening flec count per grade × side, append-only, with full history) on top of computed read-only closing balances + the show-your-math movement ledger. Only the opening balances are written; everything below them is re-derived in SQL (see "Opening-balance write path" below).

## Files
| File | Role |
|------|------|
| `layout.tsx` | Module shell — a borderless `Card` frame (matches Production's look). No shared period provider; each screen owns its own controls. |
| `page.tsx` | Landing hub — two `hover-lift` card-links (Production / Flec Inventory) + a "Tenant #2 · Cebu" note. |
| `loading.tsx` | Table skeleton (toolbar + header + 14 row stripes). |
| `error.tsx` | Error boundary — persistent message + **Copy button** (error HARD RULE) + a note that empty data is expected pre-exposure. |
| `types.ts` | Row types **derived from the generated `types/supabase.ts`** (`cenapro_production_events` view + `cenapro_flec_balance`/`cenapro_flec_ledger` function Returns) + flec-warehouse constants + `formatDisposition()` (still used by the Flec Inventory ledger's friendly "Crusher C1" label) + the **editable-ledger lookup constants** (`SHIFT_CODES`/`GRADE_CODES`/`PLANT_CODES`/`WAREHOUSE_CODES`/`SOURCE_LOCATION_CODES`/`DISPOSITION_KINDS`/`WHSE_SIDES` + `CRUSHER_CODES`/`KILN_CODES`) + the equipment-dependency helpers `partnerEquipmentOptions()` / `dispositionRequiresEquipment()` (still consumed by `bulk-paste-utils`'s remaining validation), **and the shared CCC/FLEC single-column helpers**: `CCC_FLEC_OPTIONS` (typeahead list `FLEC`/`C1`–`C4`/`RK1`–`RK4`), **`parseCccFlec()`** (forward: one cell string → `{disposition_kind, partner_equipment_code}`, forgiving on case/aliases, null on unrecognized) and **`formatCccFlec()`** (inverse: the two DB fields → the single Excel-parity string). These two are the canonical derive/format pair shared by BOTH the inline grid's save and the bulk modal (via `bulk-paste-utils`). See "Editable-ledger lookups" + "CCC/FLEC single column" + "Data path" below. |
| `production/page.tsx` | Server — resolves the selected **period** from `?year=&batch=` URL params (defaults to the NEWEST period when absent), `fetchCenaproPeriods()` + `fetchProductionEvents(selectedPeriod)` → `<ProductionView periods selectedPeriod />`. Only the chosen period's rows load (the perf fix). A stale/invalid URL period falls through to the newest. |
| `production/actions.ts` | `'use server'` — **`fetchCenaproPeriods()`** (DISTINCT `(batch_year, batch)` from the view, deduped + sorted NEWEST-FIRST — year desc, then calendar-month desc; exports `CenaproPeriod`); **`fetchProductionEvents(period?)`** (reads `public.cenapro_production_events` for ONE period via `.eq('batch_year').eq('batch')`, newest-first by recv_date; returns `[]` when no valid period); **and `saveProductionEvents(dirtyRows, deletedIds)`** (upsert + delete through the auto-updatable view; coerces strings→number/date, strips empties→null, NEVER sends `unique_tag`/`batch_year`; `revalidatePath('/cenapro/production')` — re-fetches the current URL period). Exports `ProductionEventDirtyRow`. |
| `production/period-picker.tsx` | Client — **local** Year + Batch picker (`CenaproPeriodPicker`) for the ledger toolbar, mirroring the ICTC production picker but URL-driven (no shared cross-tab context — Cenapro is a single page). Changing either select `router.replace`s `?year=&batch=` inside a `useTransition` (shows a spinner) → server re-fetch of that period. Year change snaps to that year's newest batch if the current batch is absent. **No "All periods" option** (that's the slow path this removes). Disabled (with a hint title) while the grid has unsaved edits. |
| `production/production-view.tsx` | Client — thin wrapper: `router.refresh()` on save success, and keys `<ProductionLedgerGrid>` by a fingerprint that folds in the **selected period** + the server-row ids, so a successful save OR a period switch remounts against the scoped/saved truth (fresh ids + computed `unique_tag`/`batch_year`, dirty state cleared). |
| `production/production-ledger-grid.tsx` | Client — the **editable** dense Excel-style ledger (one VIEW row = one grid row, keyed by stable `id`), scoped to one period. **12 data columns** (Excel order): `# · recv · prod · batch · shift · grade · plant · whse · source · weight · CCC/FLEC · flec · side`. Inline editing via shared `GridCell` + cell hooks (`useCellSelection`/`useClipboardCopy`/`useCellDelete`); `DatePickerCell` for recv/prod dates; **`SelectCell`** (a DropdownMenu cell) for the categorical columns (shift/grade/plant/whse/source/side); numeric `<Input>` for weight/flec; and the **single `CCC/FLEC` typeahead cell** — a paste-friendly `GridCell` + `<Input list=ledger-ccc-flec-suggestions>` (NOT a dropdown) holding the raw Excel value (`FLEC`/`Cn`/`RKn`). That merged cell is stored as a raw-text `ccc_flec` field on the grid row (seeded from the two DB fields via `formatCccFlec` on load, edited directly so typing isn't fought by a derive) and **parsed back into `disposition_kind` + `partner_equipment_code` at SAVE via the shared `parseCccFlec`** — an unrecognized value is rejected with a persistent `errorToast` (HARD RULE) naming the row(s); a single typed cell can't make an inconsistent pair, so the old equipment-dependency dance / separate Disposition+Equipment columns are gone. **Rows are rendered via a `React.memo`'d `ProductionRow`** fed PRIMITIVE slices of the global active/selection state + stable `useCallback` handlers, so editing/selecting one cell re-renders only the affected rows. Dirty tracking (`existing`/`new`/`modified`/`deleted` with amber/blue left borders), **NO trailing always-empty new row** (adds happen via the Bulk Add modal — primary — or the right-click Insert Above/Below context menu; paste-into-the-grid may still auto-extend rows), paste-from-Excel (TSV — `ccc_flec` lands as raw text), right-click context menu (Insert Above/Below, Duplicate, Delete/Restore), single Save/Discard. **The 4 leftmost identity columns (`# · Recv · Prod · Batch`) are FROZEN (Excel freeze panes)** — sticky-left with cumulative px offsets (left: 0 / 36 / 132 / 228 from the colgroup widths 36/96/96/120), so they stay pinned when scrolling right through the wide grid; the last frozen column (Batch) carries the canonical `.frozen-edge` anti-seam right divider. Frozen surfaces follow the canonical Frozen Panes pattern (CLAUDE.md + globals.css) — **fully OPAQUE**, never glass: header/corner `bg-muted` solid (`.frozen-corner`), body `bg-background` base + the row's opaque IN/OUT tint (`.frozen-col`), so scrolling content can't bleed THROUGH the pinned cells; z-scale stacks frozen header corners (z-30) > scrolling header row (z-20) > frozen body col (z-10) > normal cells. The empty state shows when the selected period has **zero data rows**. **Header filters = Shift / Grade / Plant / Warehouse / Source** (options distinct-in-data; Disposition filter removed). **Both the Recv and Prod date headers are clickable to sort** (`DateSortHeader` — click a date header to make it the sort key, click again to flip asc/desc; the up/down chevron marks the active key, an idle `ChevronsUpDown` the other) — in-memory, default newest-first by recv_date; the old toolbar "Date" button is gone. Toolbar hosts the **`CenaproPeriodPicker`** (left) + the **"Bulk Add" button** that opens `<BulkAddModal>` (its `onInserted` = the grid's `onSaveSuccess` → `router.refresh()`, so bulk-inserted rows land in this grid). `SelectCell` + `DatePickerCell` are now IMPORTED from `@/components/shared/grid` (Phase 2 of the Blackwood Table consolidation — the local copies were promoted to the shared barrel in Phase 0 and deleted here; byte-identical, no drift). Still-local: `ColumnFilterMenu` + `DateSortHeader` + `ProductionRow`. **Cell selection / inline edit / keyboard nav / paste** run on the shared Blackwood Table hooks — `useGridKeyboardNav` (coordinate resolver wrapped to clamp ArrowLeft→col 1 and to gate inline editing to typable columns via a custom `isEditable`; Home/End handled in a container wrapper), `useGridEditSession` (with a CUSTOM revert that un-dirties a reverted modified-existing row), and the existing `useCellSelection`/`useClipboardCopy`/`useCellDelete` wired into the hook's `range` slot; `enableEnterAnchor: false` (plain Enter drops down). The grid keeps its OWN `handleSmartPaste` (paste must flip pasted existing rows to `modified`). The right-click row context menu now uses the shared `useGridContextMenu` hook + declarative `GridContextMenu` component (Phase 4 of the Blackwood Table consolidation — viewport edge-flip + close-on-outside/Esc + menu styling all lifted into the shared primitives; see `components/shared/grid/CONTEXT.md`). Items are a `GridMenuItem<number>[]` keyed on `rowIdx` (Insert Above/Below, Duplicate, separator, Delete/Restore); Delete (destructive) and Restore (muted) are TWO items gated by `hidden` on the row's deleted state, since the shared `variant` is static. **Visual styling (display-only):** the whole ledger is **bold** (`font-bold` on headers, data cells, and the edit `<Input>` so weight doesn't shift on F2). Each row carries a **bold row tint** keyed on the WAREHOUSE first, then the CCC/FLEC disposition — **priority: UNPLACED > WHSE 3 (DVO) > disposition** (via `rowDirection(row)`, which reads `warehouse_code` + the shared `parseCccFlec` — same source as save, so it re-tints live as you place/type): UNPLACED (warehouse blank) → no tint (an unplaced bagging row is no longer green until placed); WHSE 3 (DVO) → blue (`bg-blue-50 dark:bg-blue-950/40`) regardless of disposition; a real placed warehouse (WHSE 1/2/5/7) → bagged-IN (`flec_bagging`) → `bg-emerald-50 dark:bg-emerald-950/40`, any withdrawal (`partner_crusher`/`partner_kiln`) → `bg-rose-50 dark:bg-rose-950/40`; unrecognized at a real warehouse → no tint. (Two tint helpers: `rowDirectionTint` = the translucent scrolling-`<tr>` flavour above; `rowDirectionFrozenTint` = the OPAQUE flavour for the sticky frozen identity cells — `dark:` bumps to `-950/60`, blue included, so the pinned cells aren't see-through.) The tint is applied FIRST in the `<tr>` class so the dirty-state left borders (new=blue / modified=amber / deleted=strikethrough) + hover + cell-selection highlight all read on top. **Two columns render colored badges in display mode** (helpers `cccFlecBadgeClass` / `plantBadgeClass` + shared `BADGE_BASE`; the edit input stays a plain `<Input>` so typing/paste is unaffected) — punchier than before (`/20`–`/25` fill, `/40` border, bold `text-{c}-700 dark:text-{c}-300`): **CCC/FLEC** — `FLEC`→emerald, `C1`–`C4` (crushers)→amber, `RK1`–`RK4` (kilns)→rose, unrecognized→neutral; **PLANT** — `W6`→blue, `W7`→teal, `W6/W7`→indigo, `DVO`→slate, empty→plain (rendered via a `SelectCell` `renderTrigger` prop that styles ONLY the closed trigger; dropdown menu items stay plain text). Every badge color has a `dark:` text variant. No row animations (CLAUDE.md hard rule). **View-mode switcher:** the toolbar carries a 3-mode segmented control (`ViewModeSwitcher`, right after the `CenaproPeriodPicker`) driving the `?view=ledger\|daily-w6\|daily-w7` URL param (preserves `year`/`batch`; `useTransition` + `router.replace`; `ledger` omits the param to keep the URL clean; invalid/absent → `ledger`; **legacy `?view=daily` maps to `daily-w6`** for back-compat). A `ViewMode` union (`'ledger' \| 'daily-w6' \| 'daily-w7'`) + `parseViewMode()` + `plantViewOf()` (→ `'W6' \| 'W7' \| null`) keep it extensible. When a daily mode is active: the period picker + switcher STAY visible but the editable-only controls (row count, Clear-filters, Bulk Add, Discard, Save) are HIDDEN and `<ProductionDailyBlock rows={initialRows} plantView={…} />` renders in place of the editable `<table>` (plus its context menu / datalist / BulkAddModal); the `loadError` banner still shows. The grid's hooks always run (only the JSX branches), and Ledger mode behavior is unchanged. **Mobile (Archetype C):** in Ledger mode the desktop `<table>` container is `hidden sm:block` and a `sm:hidden` **`<CenaproLedgerCardsMobile>`** renders instead on phones, fed a `mobileRows` memo (`rows` minus deleted/empty-new/filter-hidden — single source of truth) + the shared filter state/options/setters; the display-only badge helpers (`cccFlecBadgeClass`/`plantBadgeClass`/`BADGE_BASE`/`formatKg`) and the `GridRow` type are now **`export`ed** for that card file to reuse. The **toolbar is `flex-wrap`** so the period picker + view switcher + controls don't overflow at 375px (helps every view mode). See `production-ledger-cards-mobile.tsx`. |
| `production/production-ledger-cards-mobile.tsx` | Client — the **phone read layer for the LEDGER** (`?view=ledger` only; the daily-w6/w7 matrix is a later phase). Built on the platform **`MobileCardList<GridRow>`** primitive (**Archetype C** — dense desktop table → phone card list; see `components/shared/mobile/CONTEXT.md`). **ADDITIVE + read-only** — the whole editable layer (inline edit, Bulk Add, paste, context menu) stays desktop-only (click-to-select cells degrade on touch). The grid renders the desktop `<table>` `hidden sm:block` and this card list `sm:hidden`, fed the **SAME sorted/filtered rows** (`mobileRows` = the ledger's `rows` minus deleted/empty-new/filter-hidden — single source of truth, no refetch). **Card headline (≤6):** `recv · batch · shift/grade · source · weight` + a **CCC/FLEC disposition badge** and a **Plant chip** — status meaning is preserved via the SAME desktop badge helpers (`cccFlecBadgeClass` / `plantBadgeClass` over the shared `BADGE_BASE`, all now **exported** from `production-ledger-grid.tsx`, plus `formatKg` + the `GridRow` type — reused, not duplicated, mirroring how RC IN's `delivery-cards-mobile` imports from its sibling desktop table), NOT a bare row tint. **Detail bottom-Sheet:** every remaining field (prod date, batch_year, warehouse, side, flec count, weight, CCC/FLEC). **Mobile Filters Sheet** reuses the desktop single-select column filters (Shift/Grade/Plant/Whse/Source, `'ALL'`=off) via the SAME state + options + setters threaded from the grid (so mobile ⇄ desktop filter sets never diverge). **"View full table" escape hatch** mounts a read-only mirror of the ledger columns in a horizontally-scrolling Sheet (NOT the interactive grid). No ₱ anywhere in Cenapro → zero price gating. |
| `production/production-daily-block.tsx` | Client — the **EDITABLE Daily Block** view (Phase 1 + 2), a visually-styled rebuild of the boss's "PROD 2026" spreadsheet, rendered as **two plant variants** (`plantView: 'W6' \| 'W7'` prop — Daily W6 / Daily W7 view modes). **PROD-ONLY** — always groups by `prod_date` (the recv/prod toggle was removed; this is a production view by definition). Consumes the SAME period rows the ledger holds (`ProductionEventRow[]`). **Source filter (production OUTPUT only):** rows are filtered by `source_location_code` against `SOURCE_SETS[plantView]` before pivoting — **W6 = `{TNK 1, TNK 2, TNK 3, TNK 4, W6}`, W7 = `{W7}`**; **FLEC and DVO are EXCLUDED from BOTH** (inventory withdrawals, not factory output). `buildDateGroups(rows, plantView)` (pure) pivots into **PROD_DATE → SHIFT (M/E/N) → GRADE (3X50/2X6/3.5/4X8) → SOURCE → RECV_DATE** — **a LEAF row = one (source, recv_date) pull** (all events matching that 5-tuple aggregated into the equipment columns). The **SOURCE cell `rowSpan`s its recv-date rows**, so a source appears ONCE with its recv dates stacked **chronologically** beside it in a new **Recv Date** column (you can see how many pulls a tank had that prod date). Columns `Date · Shift · Grade · Source · Recv Date · C1–C4 · RK1–RK4 · Bagging · Sub-Total · Total`: equipment col = SUM(`weight_kg`) where `partner_equipment_code` = that code; **Bagging KEPT** = SUM where `disposition_kind='flec_bagging'`; **Sub-Total** = Σ partner; **Total** = Sub-Total + Bagging. **Only rollup = the per-DATE "Daily total" footer** (the grade subtotal row + all subtotal logic were REMOVED; a grade just lists its source→recv rows). **FIXED equipment columns (not adaptive):** all 8 (C1–C4, RK1–RK4) always render; blank cells blank (`0 → ''`). **Padded + outlined day blocks:** each day is padded with blank FILLER rows up to `MIN_DAY_ROWS` (=6, **tunable**) — filler rows sit after the last real row, before the footer; their numeric cells get a faint `bg-muted/30` so the area reads as a reserved/empty SLOT (not missing data), still on the uniform grid + inside the day box. **Excel-matched widths + density:** colgroup px (Date 76 / Shift 48 / Grade 100 / Source 140 / **Recv Date 80** / each crusher+kiln 56 / Bagging 66 / Sub-Total 70 / Total 84), table `width: max-content`, compact `px-1.5 py-0.5` / `text-[11px]`. **Complete 2-tier header (one solid bordered band edge-to-edge):** the **5 IDENTITY headers `rowSpan={2}`** so each fills the FULL header height (no empty white top-left), vertically centered, neutral muted bg, `.frozen-corner` (sticky top AND left, highest z, opaque) — labels are `Date · SHFT · Grade · Source · Recv Date` (the Shift header reads **`SHFT`**, not `Sh`; the real-row merged shift cell stays the big colored letter); the numeric side has the tinted group banner (Crushers/Kilns/Bagging/Totals) over the per-column code cells. **Layout — REAL merged cells via `rowSpan`:** DATE `rowSpan`s the day's REAL + FILLER rows (**NOT** the footer); SHIFT `rowSpan`s its shift's rows; GRADE `rowSpan`s its grade's leaf rows (`= leafCount`); SOURCE `rowSpan`s its recv-date rows. `dateRowSpan = dayBody + fillerCount`; the day has `dayBody + fillerCount + 1` `<tr>`s (the +1 footer is a separate full-width row) — **rectangular**. Merged identity content is **align-top** (label sits atop its block). Merged cells sticky-left (frozen) + OPAQUE. **THREE border weights, one token each (used everywhere — header, body, filler, footer):** **GRID** = uniform 1px `border-b border-r border-border` (solid, crisp) on EVERY cell, both axes (merged cells skip internal horizontal lines = intended merge look); **GROUP** = a 2px `border-l-2 border-l-border` divider between the column groups (Crushers | Kilns | Bagging | Totals), carried through header + body + filler + footer; **BOX** = the 2px day-block outline on the day's edge cells (`border-t-2` first row, `border-b-2` footer, `border-l-2` Date col, `border-r-2` Total col → survives horizontal scroll). **SOFT + NEUTRAL** — `border-border` (the SAME color as the gridlines, just 2px thick); the day box stands out by WEIGHT, not color (NOT `border-foreground`/black, which read as a harsh black outline on the perimeter cells). The whole table's outer perimeter is closed with a `border border-border` on the `<table>`. Clean card background, **no day tint**. **BOLD identity columns:** Date/Shift/Grade/Source/Recv Date cells render bold; numeric cells stay normal weight (the daily-total row is bold). **Frozen panes** — **5 identity columns** (Date/Shift/Grade/Source/**Recv Date**) + the 2-tier header, cumulative `left` offsets, `.frozen-edge` on **Recv Date**. **Full-width day footer:** a TRUE edge-to-edge totals row — a "Daily total" label cell `colSpan={5}` over ALL FIVE identity columns (sticky at `left:0`), then the equipment/bag/sub/total cells; ONE continuous solid `bg-muted` band (opaque across frozen cols), bold numbers, the box top + bottom edge + the group dividers. **Visual flair:** **shift = a BOLD colored letter** M amber / E violet / N indigo (NOT a filled pill — cannot overflow the 48px column); per-grade **chips** (3X50 sky / 2X6 teal / 3.5 amber / 4X8 rose); the group banner tints (Crushers amber | Kilns rose | Bagging emerald | Totals neutral) sit on the GROUP cells only. **No data-bars** (numbers render plain). Single container `animate-fade-in`, **NO per-row animation**. Empty state per variant ("No W6/W7 production output for this period"). **EDITABLE (Phase 1) — equipment/bagging NUMBERS on real pull rows, round-trip via the EXISTING `saveProductionEvents(dirtyRows, deletedIds)` (NO new backend):** the 8 equipment cols (C1–C4, RK1–RK4) + Bagging on REAL leaf rows are editable via the **canonical Blackwood Table click-to-select / type-to-edit model** (NOT always-on inputs anymore — see KEYBOARD NAVIGATION below): each renders a focusable STATIC cell by default and mounts the shared `EditInput` only while editing; **commit is EXPLICIT (registry-driven), NOT blur-driven** (see EXPLICIT-COMMIT below), Esc reverts; real-row identity cells + the footer stay read-only `<span>`s. Three ops: **EDIT** a value → UPDATE (dirty row WITH the cell's underlying event `id`, amber ring); **CLEAR** a value → DELETE (id → `deletedIds`, guarded by an `AlertDialog` confirm); **TYPE into a blank cell** → a **COLUMN-AWARE `Popover`** → stage an INSERT (dirty row WITHOUT id, blue tint). The popover always shows read-only **CONTEXT chips** (auto Plant W6/W7, prod date, shift, grade, target column e.g. "C3 · Crusher" / "Bagging") and branches on the column: **Crushers/Kilns (C1–C4, RK1–RK4)** → the only field is **Source** (a selector over `SOURCE_SETS[plantView]`, pre-filled to the row's source) — warehouse/side/flec are dropped (noise here, left null); **Bagging** → **Source + Warehouse** (`WAREHOUSE_CODES`, "— Unplaced"=null) + **Side** (`LS`/`RS`, nullable) + **Flec count** (optional). **Cell→event mapping:** the pivot records each cell's contributing event `id`(s) (`CellSlot.eventIds`) so UPDATE/DELETE target the exact event; the **COLUMN is the disposition** via `columnDisposition()` (C1–C4→`partner_crusher`+code; RK1–RK4→`partner_kiln`+code; Bagging→`flec_bagging`+null — same map as `parseCccFlec`/`partnerEquipmentOptions`, CHECK structurally satisfied). **INSERT dims** come from the leaf (recv_date/prod_date/shift/grade) + the popover's **Source** (defaults to the row's source) + `selectedPeriod.batch`, and **`plant_code` is AUTO-STAMPED = `plantView` (W6/W7)** so a new entry never shows a blank Plant in the Ledger; `batch_year` is NOT sent (the base trigger derives it from recv_date — correct for same-year periods; `ProductionEventDirtyRow` has no `batch_year` field, so the contract's cross-year override isn't expressible Phase 1 and is deferred). **COLLISION LOCK:** a cell whose 9-col pivot key maps to >1 event renders read-only with a 🔒 + "Multiple entries — edit in Ledger" (exactly 1 such cell in live W6/W7 data: MAY-2026 / TNK 2 / 3X50 bagging). **State + save:** `modified` (eventId→weight), `staged` (cellKey→INSERT dirty row), `deleted` (event ids); a Save/Discard bar (with a "N unsaved" count) appears in the toolbar when `hasChanges`; **Save** calls `saveProductionEvents(dirtyRows, deletedIds)` then `onSaveSuccess()` (→ the production-view `dataKey` remount / router.refresh); errors via `errorToast` (HARD RULE). Props `selectedPeriod` + `onSaveSuccess` are threaded from `production-ledger-grid.tsx`'s daily-block render. **EDITABLE (Phase 2) — NEW PULLS + NEW DAYS (same staged-INSERT → `saveProductionEvents` path):** the blank FILLER rows are **"new pull" input slots** (rendered FLAT, not merged): the identity cells are **Excel-style live typeahead** (`IdentitySuggestInput`, NOT a `<Select>` and NOT a native `<datalist>`) — **Shift** (`SHIFT_CODES`), **Grade** (`GRADE_CODES`), **Source** (`SOURCE_SETS[plantView]`). **Why a custom dropdown, not `<datalist>` (Issue 2a):** native `<datalist>` is unreliable for live typeahead — most notably it shows NOTHING for a single-char exact match (typing "M" into Shift is already a complete option) and its drop behavior is inconsistent across browsers (verified in the harness). `IdentitySuggestInput` renders the bare `EDIT_INPUT`-metric input (with `data-navid` so keyboard nav still finds it) PLUS a compact glass dropdown (`bg-popover/95 backdrop-blur-lg`, `animate-fade-in`) anchored under the cell: as you type it prefix-filters the valid set case-insensitively (space-insensitive, e.g. `tnk1`); **↓/↑** move the highlight (consumed — the grid does NOT navigate cells); **Enter/Tab WITH a genuine prefix match on a NON-EMPTY query (or an explicit arrow pick)** accept the highlighted option (`flushSync(onChange)` so the explicit commit reads the accepted value) then BUBBLE so the grid commits + moves (canonical Excel flow); an **UNMATCHED query (e.g. "XY")** shows NO dropdown and Tab/Enter commits the RAW text (it is NEVER silently snapped to a valid value); **click** an item accepts + commits; **Esc** closes the dropdown first (value kept), a second Esc bubbles → grid reverts to the snapshot. **CLEAR-TO-EMPTY (regression fix):** the auto-accept gate is `hasMatch = q !== '' && matches.length > 0` — it must be FALSE on an EMPTY query, otherwise `options.filter(o => o.startsWith(''))` = ALL options would auto-snap a cleared field back to the first option on Tab/Enter, so the operator could NEVER clear a Grade/Source/Shift identity cell. Emptying a cell + Tab/Enter (or selecting it + Delete/Backspace then committing) now commits EMPTY → `normalizeIdentity('')='' `→ `upsertDraft(slotKey, { shift|grade|source: '' })` clears that draft field. All three identity cells clear consistently (verified in a harness, W6 + W7). On commit the value is normalized against the allowed set (`normalizeIdentity`, case-insensitive, tank aliases `t1`/`tnk1`→`TNK 1`). **Recognized/tagged confirmation (Issue 2b):** the STATIC identity cell renders a committed value via `renderRecognizedIdentity(col, value, plantView)` — a value that IS a valid member of its set gets the SAME canonical treatment the REAL rows use (Shift → the colored `SHIFT_LETTER` letter + `SHIFT_LABEL` title; Grade → the `GRADE_CHIP` pill; Source → mono-bold), so the operator can SEE it tagged as the DB value; a non-empty value that is NOT recognized renders in a clear **unrecognized amber** style (`text-amber-600 dark:text-amber-400`, `title="Not a recognized value"`). `CellEditor` gained two optional props that power this — `renderEdit` (custom edit-mode element = the typeahead) and `renderValue` (custom static render = the recognized styling) — while still owning the static↔edit toggle + value-accessor registration. `identityOptions()`/`isRecognizedIdentity()` are the shared valid-set helpers. **Recv Date** is typed (normalized via `normalizeTypedDate` on commit) and starts **EMPTY** (placeholder only — NO prod_date prefill). Then editable equipment/Bagging weights. Committing a weight **validates the identity is complete** (shift + grade + source + recv) — if not, a gentle `toast.warning` and the value is kept but NOT staged. A complete-identity filler stages **one INSERT per non-empty weight column** (so C1 + C2 on one filler row = 2 events sharing the pull dims), with `prod_date` = the day, dims from the filler, `plant_code` = `plantView` (auto), batch from `selectedPeriod`, disposition/equipment from the column. **Crushers/Kilns** stage directly; **Bagging** opens a small warehouse/side/flec `BaggingMetaPopover`. Drafts live in a `drafts` Map keyed by a **STABLE, PARENT-OWNED, DETERMINISTIC slot id** (`${prodDate}#slot-{i}`, positional per day) — NOT a child `React.useId`. **This is the Issue-1 (duplicate-row) fix:** previously each `FillerRow` self-generated its draft id via `React.useId()` AND the parent rendered filled drafts FIRST (reordered ahead of the empty slots), so committing a blank slot made React MOVE the typed instance to a new index while ALSO mounting a fresh instance at the prepended draft index → two live rows shared one `slotKey` → duplicate `data-navid` → the typed value appeared in TWO rows with BOTH highlighted. Now `DateSection` assigns every slot (empty OR filled) a persistent positional id, passes it DOWN to `FillerRow` as the `slotKey` prop, and renders rows in **slot-id ORDER (not filled-first)** — `[...slotIds.map(sid → draftByKey.get(sid)), ...orphanDrafts]` — so a slot that goes empty→filled keeps its id AND its render position → React preserves the ONE instance (no second mount, no navid collision, no state bleed into a neighbouring slot). `slotCount = max(MIN_DAY_ROWS−dayBody, maxOccupiedDraftSlot+2, #filledDrafts+1) + drawer extras`, guaranteeing ≥1 trailing empty slot once a slot fills; a `FillerRow` effect clears its local identity state when its `draft` prop drops to null (Discard, which doesn't remount). Verified in a public harness: typing into a blank slot + Tab yields exactly ONE row with the value, ONE active ring, and ZERO duplicate `data-navid` (querySelectorAll count = 1), for both W6 and W7. `draftToDirtyRows()` converts a complete draft → its INSERT rows, merged into the same save payload. **Add-row drawer (hover-reveal):** the `MIN_DAY_ROWS` rectangle of empty slots stays as-is, but the previous **auto-grow-on-fill is REMOVED** — a small "+ Add row" pill at the top edge of the day footer's label cell (hidden until the day is hovered, `group-hover/day`, opacity-only, no layout shift) appends one input row to THAT day via per-day `extraRows` state (`slotCount = padSlots + extraRows`). **"Add day"** — a toolbar `AddDayButton` popover takes a typed date (normalized; validated as a real date, not already shown, within `selectedPeriod`'s month when resolvable) → spawns a NEW empty outlined day block (`addedDays`, sorted chronologically; empty added days save nothing). **Draft rows render flat** until save+refresh, when they become real merged rows. The **Date `rowSpan` covers real + draft + filler-slot rows** (footer separate) so the grid stays rectangular. `hasChanges`/"N unsaved" include draft-derived inserts; **Discard** clears all edit + draft + added-day state. **Excel-fidelity cell behavior (Phase 2.1):** (a) **identity typeahead** (custom `IdentitySuggestInput` dropdown, above); (b) **recv blank = empty** (no prefill); (c) **placeholder** is visible when empty+unfocused but **vanishes the instant the cell is focused** (placeholder attr blanked on focus, restored on empty blur) on every editable identity/recv/weight input; (d) **uniform row height** — a single `ROW_H` (`h-7`) on EVERY `<tr>` (real data, filler/input, AND the daily-total footer — its label dropped `py-1`→`py-0.5`), all cells `align-middle`; (e) **no height expansion on edit** — the STATIC cell render AND the shared inline `EditInput` both use `EDIT_INPUT` (`text-[11px]` / `px-1.5` / `leading-none` / `bg-transparent` / no border/ring/outline / `box-border` / `h-full` / `[min-height:0]` / `appearance-none`), so switching to edit shows only the caret and never grows the row. **CELL-HOST HEIGHT (regression fix):** because `EDIT_INPUT` uses `h-full` (height:100%) and a percentage height only resolves against a parent with a *definite* height, every `<td>` that hosts a `CellEditor` (the real-row + filler weight cells' `baseClass`, the filler identity `cellBase`, and the filler Recv cell) carries an explicit **`h-7`** — otherwise the `StaticCell` `<div>` collapses (0px when empty, ~11px when filled) inside a content-height `<td>`, leaving the cell visually present but **un-clickable / un-selectable** (a real click lands in the dead band on the bare `<td>`, which has no handler — the exact symptom of the "single-click does nothing" bug). The explicit `h-7` makes `h-full` resolve to the full cell height (the div fills the cell, fully clickable) **without** growing the row past 28px. **KEYBOARD NAVIGATION + CLICK-TO-SELECT (shared Blackwood Table primitives — Phase 3 retrofit):** the Daily Block now uses the SAME canonical interaction model as every other grid via the shared hooks `useGridKeyboardNav` + `useGridEditSession` + the shared `EditInput` (`@/components/shared/grid`, `@/lib/hooks/use-grid-keyboard-nav`, `@/lib/hooks/use-grid-edit-session`). **Two states per cell:** `activeNavId` = the SELECTED cell (active ring), `editingNavId` = the cell currently in EDIT mode. **Every editable cell renders TWO ways:** by default a focusable **STATIC** element (`StaticCell` — a `<div tabIndex=0 role=gridcell>` matching the `EDIT_INPUT` metrics, carrying `data-navid="${rowKey}|${colKey}"`); it mounts the shared `EditInput` ONLY when `editingNavId === navid`. The `CellEditor` wrapper owns the static↔edit toggle and registers each cell's thin **get / set / commit** accessors with the grid (for snapshot/seed/revert AND explicit commit) — each cell still owns its OWN value state + commit logic, so the entire save path (onEditWeight / onStageInsert / draft upserts / InsertPopover / BaggingMetaPopover) is untouched. **EXPLICIT-COMMIT (regression fix — the Save bar never appeared):** the cell's real commit is run EXPLICITLY through the registry (`commitCell(navid)`), NOT via the editing input's `onBlur`. In React 19 / Next 16 the focus/blur (`focusout`) handler is dropped when the editing input UNMOUNTS in the same render batch — and the grid unmounts the input the instant it moves focus (Tab/Enter) or switches the active cell (click-away), so the onBlur-commit silently never fired (no draft pushed, no `modified`/`deleted` set → `dirtyCount` stayed 0 → no Save/Discard bar, for EVERY edit kind: new filler pull, existing-weight edit, and clear-to-delete). Now `edit.commit` (Tab/Enter) and `onActivate` (click-away to another cell) BOTH call `commitCell(editingNavId)` before clearing edit mode; `editingNavIdRef` (a ref mirror of `editingNavId`) is read in those callbacks so the commit isn't a side effect inside a state updater (StrictMode-safe). The input's `onBlur` is kept only as a harmless idempotent fallback. **CANONICAL gestures:** **CLICK** (mousedown) on a static cell = SELECT only (ring, NO edit) + focus; **TYPE a printable char / F2 / DOUBLE-CLICK** = begin editing (type-over seeds the char); **ESC** = revert to the pre-edit snapshot (the EditInput's `escapedRef` suppresses the blur-commit), stay selected; **ENTER / TAB** = commit (the cell's real commit fires EXPLICITLY via `commitCell` in `edit.commit` — NOT via onBlur, which is dropped on unmount; see EXPLICIT-COMMIT) + move; **ARROWS / TAB / ENTER** (not editing) = navigate. Nav uses `createDomOrderNavResolver` (`containerRef`=scroll container, `navColOrder`=`NAV_COL_ORDER`, `parseId`=`parseNavId`, `findColInAdjacentRow`=a thin adapter reproducing the same-lane-preference / nearest-by-`NAV_COL_ORDER` fallback against the shared `DomNavCell[]` signature). The resolver reads **`[data-navid]`** (NOT `input[data-navid]`) so it finds the STATIC cells too — this is what makes click-to-select navigate. `onAfterMove` focuses the resolved `[data-navid]` element via `CSS.escape` so keyboard nav follows visually. **Enter-anchor** is provided by the shared hook (`enableEnterAnchor: true`, `laneOf` = colKey): the first Tab of a run records the colKey and a later plain Enter returns to it on the next row. **Navigation SKIPS read-only/locked cells** automatically — collision-locked weight cells (🔒) and real-row identity cells render as non-input `<span>`s with **no `data-navid`** → absent from the resolver's cell list (and `resolver.isEditable` is false for any element bearing `data-locked`); the footer/headers are never navigable. Click + keyboard share ONE active-cell model (`activeNavId`, set by a static cell's mousedown/focus → `onActivate`, which also exits any OTHER cell's edit). **ACTIVE-CELL HIGHLIGHT (matches the ledger):** the cell whose `data-navid === activeNavId` gets `ACTIVE_RING` = `ring-2 ring-primary ring-inset` (the SAME token the ledger's selected cell uses in `production-ledger-grid.tsx`) + `z-20`. An INSET ring is a box-shadow → **NO layout space**, so the highlighted cell stays the exact same height as every other (honors the no-expand rule). **Static — no transition** (CLAUDE.md cell-selection rule). It sits ON TOP of any dirty tint — the later `ring-*` wins ring color/width while the dirty `bg-*/10` fill remains, so both read together (e.g. an active amber-modified cell = amber fill + primary ring). `z-20` lifts the active cell ABOVE the z-10 frozen columns (`.frozen-col`/`.frozen-edge`) so the ring is never clipped behind a sticky neighbor on horizontal scroll; it follows clicks AND keyboard nav. Applied to all editable cells (weight on real+filler, filler identity/recv); locked/read-only cells never become active so never highlight. **Bagging + popover during nav:** Tab/Enter into a bagging weight commits; a positive value opens the warehouse/side/flec popover (Radix moves focus into it) — the popover briefly interrupts the Tab flow, and after confirm/cancel the user clicks/Tabs to resume (focus isn't trapped, the active cell isn't lost). **Known gaps (boss-sheet parity):** NO "RECV CCC" column (no CCC bucket — only C1–C4 / RK1–RK4 / FLEC); NO tank-composition string (Source is its own row per location, and Recv Date is now its own pivot column). Cenapro has no audit-log trigger (writes don't produce `audit_logs`); app rows can't be tagged with a distinct `provenance` through the view (needs a migration). Do not fabricate either. |
| `production/bulk-add-modal.tsx` | Client — the **Bulk Add** `Dialog` (glass: `bg-background/95 backdrop-blur-xl supports-[backdrop-filter]:bg-background/80`). A dense Excel-style paste grid **preset with 8 blank rows** (`Array.from({ length: 8 }, createEmptyRow)`) for fast multi-row INSERT — modeled on RC IN's `bulk-delivery-input.tsx`. **12 data columns** (Excel order): `recv · prod · batch · shift · grade · plant · whse · source · weight · CCC/FLEC · flec · side` — disposition + equipment are now ONE `CCC/FLEC` cell (matching the inline grid). Reuses the shared `GridCell` + the same three cell hooks; faithfully mirrors RC IN's `handleSmartPaste`/`handleGridPaste` paste engine (split `\r\n|\n|\r` → rows, `\t` → cols, fill from the active cell, **auto-extend** rows when the paste is taller; per-`<Input>` `stopPropagation` on paste + `stopImmediatePropagation` on Escape so the Radix Dialog doesn't swallow them). **Categorical cells stay paste-friendly** — plain typeable `<Input list=…>` with `<datalist>` suggestions (ICTC daily-ledger pattern), NOT strict dropdowns; the CCC/FLEC cell uses the shared `CCC_FLEC_OPTIONS` datalist. "Add Row" appends; the X on row hover removes. Save drops blank rows, canonicalizes + validates each (via `bulk-paste-utils`), then INSERTs via `saveProductionEvents(rows, [])`; on success closes, toasts a count, and calls `onInserted` (page refresh). The inline ledger stays — this is the additional fast-entry path. **Typed-date auto-transcription:** both date cells (`recv_date`/`prod_date`) Excel-normalize a typed shorthand to the canonical `yyyy-MM-dd` the instant the cell is COMMITTED — `commitDateCell(rowIdx, field)` runs `normalizeTypedDate(raw, yr)` (from `lib/paste-utils`) and writes back only when it changes (no-op for already-canonical → no flicker). Wired on BOTH commit paths: Tab/Enter (via `commitActiveDateCell()` inside `handleGridKeyDown`'s `isEditing` branch, BEFORE `setIsEditing(false)` — the grid intercepts Tab/Enter so the input's own onBlur may not fire) AND click-away (the date `<Input>`'s `onBlur={() => onCommitDate(rowIdx, field)}`, which only touches the value — active/editing state stays owned by the grid-container onBlur). `"6/2"`→`"{yr}-06-02"` (bare M/D injects the year), `"6/2/26"`→`"2026-06-02"`, already-ISO is a no-op, invalid (`"13/40"`) / empty / non-date text is left UNCHANGED for the operator to fix. The year `yr = defaultYear ?? new Date().getFullYear()`; **`defaultYear` is a new prop threaded from `selectedPeriod?.batch_year`** in `production-ledger-grid.tsx`. **Excel-style "Enter after Tab" nav:** the column a Tab RUN begins from is remembered as an anchor; a later Enter drops DOWN a row and returns to that ANCHOR column — not the last cell tabbed to (e.g. Tab,Tab,Tab from col 0 then Enter lands at the next row's col 0). As of Phase 2 of the Blackwood Table consolidation this is provided by the shared `useGridKeyboardNav` with `enableEnterAnchor: true` (the hook owns the anchor lane ref; set on the first Tab, consumed on Enter, cleared by arrows) — the old local `enterAnchorColRef`/`moveActive` were deleted. **Cell selection / inline edit / paste / keyboard nav** all run on the shared hooks now (`useGridKeyboardNav` + `useGridEditSession` + `useGridPaste`); the coordinate resolver is wrapped to clamp ArrowLeft→col 1 (col 0 is the row#), and Home/End are intercepted in a thin container wrapper (they aren't NavMoves). The typed-date auto-transcription fires via the edit session's `onAfterCommit`. Mirrors the RC IN reference. |
| `production/bulk-paste-utils.ts` | The Bulk Add **paste + canonicalization layer** (pure, no React) — RC IN's `paste-utils.ts` analogue plus the categorical brain. Exports `BulkRow`/`BulkField`, `BULK_COLUMN_MAP` (**12** editable cols in the modal's left→right Excel order — `disposition_kind`+`partner_equipment_code` collapsed into a single `ccc_flec` cell), `createEmptyRow`, `isBlankRow`, `cleanBulkPasteValue` (dates→`parseExcelDate`, numerics strip `₱,"'%`/spaces; categoricals + `ccc_flec` kept raw so the cell shows what was pasted), per-column canonicalizers (`canonicalizeShift/Grade/Plant/Warehouse/Source/Side` — forgiving on case/aliases, e.g. `W7`/`whse7`/`7`→`WHSE 7`, `3x50`→`3X50`, `TNK1`→`TNK 1`, `left`→`LS`), and `mapBulkRowToDirty(row, defaultYear?)` which validates a row (weight > 0, categoricals map to real codes), runs both date cells through `normalizeTypedDate(value, defaultYear ?? currentYear)` as a **safety net** (so a typed shorthand that escaped commit-time normalization still lands canonical `yyyy-MM-dd`), and **resolves the single `ccc_flec` cell via the shared `parseCccFlec` from `types.ts`** (`FLEC`→`flec_bagging`; `C1`→`partner_crusher`+`C1`; `RK3`→`partner_kiln`+`RK3`) → `ProductionEventDirtyRow` or a list of human errors. The old separate `canonicalizeDisposition`/`canonicalizeEquipment` + the disposition⇄equipment CHECK dance are gone (the single column subsumes them — `parseCccFlec` returns null only for genuinely unrecognized input). `rowLabel(row, i)` for error messages. |
| `inventory/page.tsx` | Server — reads `?whse=&date=` URL params (defaults WHSE 7 / 2026-03-10), fetches `fetchFlecInventory()` + `fetchOpeningBalances()` + `fetchOpeningBalanceHistory()` in parallel → `<FlecInventoryClient />`. The START date is the "as of" for both the openings seed and the ledger seed; history is warehouse-scoped (date-independent). |
| `inventory/actions.ts` | `'use server'` — `fetchFlecInventory(warehouse, startDate)` (`cenapro_flec_balance` + `cenapro_flec_ledger` in parallel); `fetchOpeningBalances(warehouse, asOf)` (`cenapro_opening_balances` — the effective opening per grade×side, seeds the editable STARTING block); `fetchOpeningBalanceHistory(warehouse)` (`cenapro_opening_balance_history` — the full append-only trail, newest-first); **`saveOpeningBalances(changes[])`** loops `cenapro_set_opening_balance` once per CHANGED cell (append-only INSERT, effective date = page START date), stops + reports the first failing cell, `revalidatePath('/cenapro/inventory')` on success. See "Opening-balance write path". |
| `inventory/flec-inventory-client.tsx` | Client — warehouse `<Select>` + native start-date input (both push to URL params via `router.replace` in a transition). **The selection persists to `localStorage` (`cenapro_flec_prefs` = `{ whse, date }`):** `applyParams` writes it on every change, and a once-on-mount effect restores it when the URL carries NO `whse`/`date` param (a clean visit — nav menu / landing card), so the operator's last warehouse + start date permeate across refresh, navigation, and sessions. An explicit URL param ALWAYS wins (shared links keep working); localStorage is only the fallback, then the hardcoded default. SSR-guarded; the restore runs once (no replace loop); a stale `whse` not in `FLEC_WAREHOUSES` or a non-ISO date is ignored. **Editable STARTING block** (`StartingBlock`): a `table-fixed` grid, rows = `GRADE_CODES`, columns = `RS`/`LS` each split into **Starting** (editable digit-only `<input>`, seeded from `cenapro_opening_balances`) + **Now** (read-only closing from `cenapro_flec_balance`, "—" when absent per the empty-period caveat). Local edit buffer with amber dirty-border per cell, "N unsaved" badge, Discard + "Save starting balances" (writes only changed cells → `saveOpeningBalances` → success toast / `errorToast`; `router.refresh()` re-seeds). Per-cell `CellHistoryPopover` (History icon) + a collapsible whole-warehouse `OpeningHistoryPanel` grouped by grade×side (newest-first, "current" badge on the top entry) — the backtracking surface. Then the existing balance cards + show-your-math movement ledger. Local `StartingBlock`/`StartingCell`/`CellHistoryPopover`/`OpeningHistoryPanel`/`BalanceCard`/`LedgerDirection`. |

## Data
All reached through **read-only accessors in the already-served `public` schema** — the standard Supabase client hits them directly (no `.schema('cenapro')`, no cast). See "Data path" below for why. These accessors are thin wrappers over the underlying `cenapro` schema objects.

- **`public.cenapro_production_events`** (VIEW, ~755 rows across 9 (batch_year, batch) periods; recv_date 2025-12-01 → 2026-06-01). 16 columns: `id, recv_date, prod_date, batch, batch_year, shift_code, grade_code, plant_code, warehouse_code (nullable — "unplaced" rows), source_location_code, weight_kg, disposition_kind, partner_equipment_code, flec_count, whse_side, unique_tag`. **The ledger loads ONE period at a time** (filtered `.eq('batch_year').eq('batch')`), not all rows — `batch` = a month name (JANUARY…DECEMBER), `batch_year` = an int. Heaviest single period ≈ 162 rows; newest (the default) can be small (e.g. JUNE 2026 = 2 rows). Loading everything (the old path, ~755 rows × ~14 interactive cells with no virtualization) was the perf bottleneck.
  - PostgREST types all VIEW columns as nullable (`T | null`) — the client consumers guard accordingly (e.g. the sort comparator coalesces `recv_date`/`id` to `''`).
  - `disposition_kind` ∈ `flec_bagging | partner_crusher | partner_kiln`. The **production grid + bulk modal** render the single Excel `CCC/FLEC` value (`FLEC` / `C1` / `RK3`) via `formatCccFlec`; the **Flec Inventory ledger** still renders the friendly label (Bag / Crusher C1 / Kiln RK3) via `formatDisposition`.
  - Verified distinct values: shift `M`; grades `2X6/3.5/3X50`; warehouses `WHSE 1/5/7`; equipment `C1/C2/RK1/RK2/RK3/RK4`. (Filter options are derived from data at runtime, not hardcoded.)
- **`public.cenapro_flec_balance(p_warehouse_code text, p_start_date date)`** → `(warehouse_code, grade_code, side, current_flec, opening_seed, as_of)`. Closing count per (grade, side). NOTE: a (grade,side) with an opening but no events ≥ start date does NOT appear (empty-period caveat, schema §6.2).
- **`public.cenapro_flec_ledger(p_warehouse_code text, p_start_date date)`** → `(…, recv_date, grade_code, side, disposition_kind, partner_equipment_code, kg_moved, flec_in, flec_out, opening_seed, flec_in_to_date, flec_out_to_date, running_balance)`. The show-your-math movement detail (function Returns are non-null).
- **`public.cenapro_opening_balances(p_warehouse_code text, p_as_of_date date)`** → `(warehouse_code, grade_code, side, period_start_date, opening_flec_count, created_at)`. The **current effective** opening per (grade, side) as of the date = greatest `period_start_date ≤ as_of`, tie-broken by greatest `created_at`. Seeds the editable STARTING block. Unlike `cenapro_flec_balance`, it returns a (grade, side) even with no events forward.
- **`public.cenapro_opening_balance_history(p_warehouse_code text)`** → `(id, warehouse_code, grade_code, side, period_start_date, opening_flec_count, created_at)`. **ALL** append-only opening entries, newest-first per (grade, side) — the backtracking data.
- **`public.cenapro_set_opening_balance(p_warehouse_code, p_grade_code, p_side, p_effective_date date, p_count int)`** → INSERTS a new append-only entry (never overwrites), RETURNS the row. SECURITY INVOKER, granted to `authenticated` + `service_role` (NOT `anon`) — written only by `saveOpeningBalances` (server action, runs as the logged-in user). See "Opening-balance write path".
- Flec warehouses = `WHSE 1/2/5/7` (flec-count). **WHSE 3 is kg/DVO — DEFERRED, never surfaced.**
- Opening balances seeded for **WHSE 7** (3X50 RS=53, 2X6 LS=26 @ 2026-03-10) → that is the inventory page's default. With the seeded ledger, WHSE 7 closing as of 2026-03-10 is 3X50/RS = 56, 2X6/LS = 0; the other 6 grade×side cells have no opening AND no movement, so they appear in neither `cenapro_opening_balances` nor `cenapro_flec_balance` (the STARTING grid renders them blank → "—", ready to type into).

## Key Behaviors
- **Production = EDITABLE ledger; Flec Inventory = read-only.** The app is now the maintaining file for production rows.
- **Production ledger:** scoped to **one (year, batch) period** via the toolbar `CenaproPeriodPicker` (URL params `?year=&batch=`); only that period's rows load (server-side `.eq` filter). **Default = the NEWEST period** (`fetchCenaproPeriods` returns periods newest-first; the page picks `periods[0]` when the URL has no param) so the page opens fast on recent data — NOT all ~755 rows. An explicit URL period wins; a stale one falls through to newest. There is intentionally **no "All periods" option** (a future "All" behind virtualization is out of scope). WITHIN the loaded period, filters + date-sort happen client-side (the per-period set is small). **Header filters = Shift / Grade / Plant / Warehouse / Source** (single-select, options distinct-in-data; they HIDE rows, not sort). **Both date headers sort:** clicking the Recv or Prod header (`DateSortHeader`) makes that date the in-memory sort key; clicking the active one flips asc/desc; default = newest-first by recv_date (the chevron marks the active key, an idle `ChevronsUpDown` the other). Sticky glass header (`bg-muted/90 backdrop-blur-sm`). One VIEW row = one grid row keyed by stable `id`. **12 data columns**, Excel order: `# · recv · prod · batch · shift · grade · plant · whse · source · weight · CCC/FLEC · flec · side`. Inline edit (click-to-activate, F2/type-over, keyboard nav, paste-from-Excel), `SelectCell` dropdowns for the categorical columns (shift/grade/plant/whse/source/side), the single `CCC/FLEC` typeahead `<Input list>` cell, `DatePickerCell` for recv/prod, numeric `<Input>` for weight/flec. **Rows are a `React.memo`'d `ProductionRow`** fed primitive active/selection slices + stable callbacks, so editing one cell re-renders only the touched row(s) — not the whole grid (a cheap complementary win on top of the period scoping). Dirty tracking (new=blue / modified=amber left border; deleted=strikethrough) + single Save/Discard; **NO trailing always-empty new row** — adds happen via Bulk Add (primary) or right-click Insert Above/Below (paste may still auto-extend). Empty state shows when the period has zero data rows. Switching period is blocked (picker disabled) while there are unsaved edits. No row animations (`transition-all duration-150` hover only).
  - **Frozen identity columns (Excel freeze panes — canonical pattern, see CLAUDE.md "Frozen Panes" + `globals.css`):** the 4 leftmost columns `# · Recv · Prod · Batch` are pinned with cumulative px offsets from the colgroup widths (36/96/96/120 → left: 0 / 36 / 132 / 228), so they stay visible when scrolling right. The table uses `borderCollapse: 'separate'` (required for sticky). Uses the shared `.frozen-col` (body) / `.frozen-corner` (header identity cells) / `.frozen-edge` (anti-seam right divider on the last frozen col, Batch) utilities. **All frozen surfaces are fully OPAQUE** — header/corner `bg-muted` solid, body `bg-background` solid + the row's opaque IN/OUT tint via `rowDirectionFrozenTint` + `group-hover:bg-muted/50` — so scrolling content can't bleed THROUGH the pinned cells (the previous translucent `bg-muted/90 backdrop-blur` thead was the bleed-through bug). Selection/edit/ring still render inside the frozen cells (GridCell/DatePickerCell paint their own selection feedback on top). **Z-scale (canonical):** frozen header corners (sticky top + left) `z-30` > scrolling header row `z-20` > frozen body col `z-10` > normal scrolling cells (auto). Mirrors the ICTC daily-ledger freeze-pane approach.
  - **Bold + direction tint + badges (display-only):** the ledger text is **bold** throughout. Each row gets a **bold row tint** keyed on the WAREHOUSE first, then the CCC/FLEC disposition — **priority: UNPLACED > WHSE 3 (DVO) > disposition** (via `rowDirection(row)`, which now takes the whole row so it can read `warehouse_code`): **UNPLACED** (`warehouse_code` blank) → **NO tint** (an unplaced bagging row is NOT highlighted until it's placed — this is the intentional behavior change; setting the Whse dropdown places it and re-tints it green); **WHSE 3** = the DVO warehouse → **BLUE** (`bg-blue-50 dark:bg-blue-950/40`) regardless of disposition (a DVO row is never counted as "taken out of a real warehouse"); a **real placed warehouse** (`WHSE 1/2/5/7`) → disposition decides: bagged-IN/`flec_bagging` → **GREEN** (`bg-emerald-50 dark:bg-emerald-950/40`), any withdrawal/`partner_crusher`+`partner_kiln` → **RED** (`bg-rose-50 dark:bg-rose-950/40`); unrecognized CCC/FLEC at a real warehouse → none. Frozen cells use the opaque variant (`-950/60` in dark, blue included). Tint is layered UNDER the dirty borders + selection highlight so those still read clearly. **CCC/FLEC** and **PLANT** render as compact colored badges in display mode, punchier now (`/20`–`/25` fill, `/40` border, bold `-700/-300` text): `FLEC` emerald / `Cn` amber / `RKn` rose; `W6` blue / `W7` teal / `W6/W7` indigo / `DVO` slate — each with a `dark:` variant; the edit `<Input>` stays plain so paste/typing is unaffected.
  - **CCC/FLEC single column:** disposition + equipment render as ONE Excel-parity cell (`FLEC`/`Cn`/`RKn`) between weight and flec. Edited as raw text, parsed to `disposition_kind` + `partner_equipment_code` on Save via the shared `parseCccFlec`; an unrecognized value is rejected with a persistent `errorToast`. A single typed cell can't make an inconsistent pair, so the prior partner-equipment dependency dance + the separate Disposition/Equipment columns are gone. See "CCC/FLEC single column" above.
  - **Unplaced-warehouse fix:** `warehouse_code` is nullable; a null row reads "unplaced" (subtle amber tint). Setting the Whse dropdown is how an operator places the unplaced bagging rows.
- **View modes (`?view=`):** the production screen shows ONE period's rows through multiple lenses, switched via a toolbar segmented control (`ViewModeSwitcher`, right of the period picker) driving the `?view=ledger|daily-w6|daily-w7` URL param (preserves `year`/`batch`; `useTransition` + `router.replace`; `ledger` is the default and omits the param; absent/invalid → `ledger`; **legacy `?view=daily` → `daily-w6`**). The data does NOT re-fetch on a view change — every mode consumes the same `initialRows`. A typed `ViewMode` union (`'ledger' | 'daily-w6' | 'daily-w7'`) + `parseViewMode()` + `plantViewOf()` + a render switch keep it extensible (future: per-product, stats).
  - **Ledger mode (default):** the editable Industrial-Spreadsheet grid described above (unchanged — date auto-format, Enter-anchor nav, warehouse-first highlight all intact).
  - **Daily W6 / Daily W7 modes (EDITABLE — Phase 1 + 2):** the visually-styled boss "PROD 2026" pivot (`ProductionDailyBlock`, `plantView='W6'|'W7'`) — two variants showing **production OUTPUT** for each plant, **always grouped by `prod_date`** (PROD-ONLY; the recv/prod toggle was removed). The period picker + view switcher STAY visible; the editable-only toolbar controls HIDE; the `loadError` banner still shows. **Source filter:** rows are filtered by `source_location_code` before pivoting — **W6 keeps `{TNK 1, TNK 2, TNK 3, TNK 4, W6}`, W7 keeps `{W7}`; FLEC and DVO EXCLUDED from both** (inventory withdrawals, not output). **Bagging kept** (bagged tank/W6/W7 material is still output). **Hierarchy:** PROD_DATE → SHIFT (M/E/N) → GRADE (3X50/2X6/3.5/4X8) → SOURCE → **RECV_DATE** — a **LEAF = one (source, recv_date) pull**. The **SOURCE cell `rowSpan`s its recv-date rows**, so each source shows ONCE with its recv dates stacked **chronologically** in a dedicated **Recv Date** column (= how many pulls that tank had that prod date). **Columns:** `Date · Shift · Grade · Source · Recv Date · C1–C4 · RK1–RK4 · Bagging · Sub-Total · Total`. **Only rollup = the per-DATE "Daily total" footer** (the grade subtotal was removed). **Padded + outlined day blocks:** each day pads with blank FILLER rows up to `MIN_DAY_ROWS` (=6, tunable) — filler numeric cells get a faint `bg-muted/30` (reserved empty slot, not missing data). **Complete 2-tier header:** the **5 identity headers `rowSpan={2}`** (fill full header height, no empty white top-left, `.frozen-corner`, vertically centered) + the tinted group banner over the code cells — one solid bordered band edge-to-edge. **Three border tokens (one each, used everywhere):** **GRID** = uniform 1px `border-b border-r border-border` (solid, crisp) on every cell, both axes (merged cells skip internal horizontal lines); **GROUP** = 2px `border-l-2 border-l-border` divider between the column groups (Crushers|Kilns|Bagging|Totals), carried through header+body+filler+footer; **BOX** = 2px day outline (top row / footer / Date col / Total col) — SOFT NEUTRAL `border-border` (same color as gridlines, stands out by 2px weight not by color; NOT black). The `<table>` carries a `border border-border` perimeter (fully closed). Clean card bg, **no tint**. **Layout — REAL merged cells (`rowSpan`):** DATE spans the day's REAL + FILLER rows (**NOT** the footer); SHIFT spans its shift; GRADE its grade's leaf rows (`= leafCount`); SOURCE its recv-date rows; `dateRowSpan = dayBody + filler`, day has `dayBody + filler + 1` `<tr>`s (footer separate) — **rectangular**. Merged content **align-top**; merged cells sticky-left + opaque. **BOLD identity columns** (Date/Shift/Grade/Source/Recv Date); numeric cells normal weight (daily total bold). **No data-bars** (removed). **FIXED equipment columns** — all 8 always render; blank when 0. **Frozen panes** on **5 identity cols** (Date/Shift/Grade/Source/**Recv Date**) + the 2-tier header (`.frozen-edge` on Recv Date). **Full-width day footer:** a TRUE edge-to-edge totals row — a "Daily total" label `colSpan={5}` over ALL FIVE identity cols (sticky at `left:0`), then the numeric totals; one continuous solid `bg-muted` band, bold numbers, group dividers carried. **Excel-matched widths** (Date 76 / Shift 48 / Grade 100 / Source 140 / Recv 80 / equip 56 / Bag 66 / Sub 70 / Total 84), `width:max-content`, `px-1.5 py-0.5` / `text-[11px]`. **Visual treatment:** **shift = a BOLD colored letter** (M amber / E violet / N indigo — not a pill, can't overflow) + per-grade **chips**; group banner tints (Crushers amber / Kilns rose / Bagging emerald / Totals) on the group cells only; toolbar carries a `{plantView}` pill. Section-level fade only, NO per-row animation. Per-variant empty state. **EDITABLE (Phase 1):** equipment/bagging NUMBER cells on REAL leaf rows are click-to-edit (Enter/Tab/blur commit, Esc cancel) and round-trip through the EXISTING `saveProductionEvents(dirtyRows, deletedIds)` (no new backend): **edit** → UPDATE (dirty row WITH the cell's event `id`, amber ring); **clear** → DELETE (id → `deletedIds`, `AlertDialog` confirm); **type into a blank cell** → a **column-aware `Popover`** → stage an INSERT (no id, blue tint). The popover shows read-only **context chips** (auto Plant, prod date, shift, grade, target column) and branches: **crushers/kilns** → **Source only** (selector over `SOURCE_SETS[plantView]`, pre-filled to the row's source; no warehouse/side/flec); **Bagging** → **Source + Warehouse + Side + Flec count**. The pivot records each cell's event id(s); the COLUMN is the disposition (`columnDisposition()`: Cn→`partner_crusher`, RKn→`partner_kiln`, Bagging→`flec_bagging`+null). INSERT dims = the leaf + the popover's Source + `selectedPeriod.batch`, with **`plant_code` AUTO-STAMPED = `plantView`** (W6/W7 — no blank Plant in the Ledger); batch_year trigger-derived, not sent. A cell mapping to >1 event is **collision-LOCKED** (🔒, edit in Ledger; 1 such cell exists). A toolbar Save/Discard bar (+"N unsaved") appears on `hasChanges`; Save → action → `onSaveSuccess()` remount; errors via `errorToast`. **EDITABLE (Phase 2):** the blank FILLER rows are **"new pull" input slots** — identity cells are **Excel-style input + `<datalist>` typeahead** (not `<Select>`): Shift/Grade/Source filter native suggestions, normalized + validated on commit (`normalizeIdentity`); **Recv Date** is typed (normalized) and starts EMPTY (no prefill). Then editable equipment/Bagging weights. A weight commit **validates identity is complete** (shift+grade+source+recv, else a gentle toast) before staging; a complete filler stages **one INSERT per filled column** (C1+C2 = 2 events sharing the pull dims) with `plant_code` auto = `plantView`, dims from the row, batch from the period (crushers/kilns direct; Bagging via a small warehouse/side/flec popover). **Add-row drawer:** the `MIN_DAY_ROWS` rectangle stays; auto-grow-on-fill was removed — a hover-revealed "+ Add row" pill (at the footer label's top edge, `group-hover/day`, opacity-only) appends a row to that day via `extraRows`. A toolbar **"Add day"** popover (validated date in-period) spawns a NEW empty fillable day block (`addedDays`, sorted; empty added days save nothing). Drafts render FLAT until save+refresh; the Date `rowSpan` covers real+draft+slot rows so the grid stays rectangular; draft inserts feed the SAME `saveProductionEvents` payload + `hasChanges`/Discard. **Excel-fidelity cells:** **uniform row height** (`ROW_H` `h-7` on every `<tr>` incl. the footer, `align-middle`); **no height expansion on edit** (the inline `EditInput` matches the static cell metrics exactly — `EDIT_INPUT`: `text-[11px]`/`px-1.5`/`leading-none`/`bg-transparent`/no border/ring/`h-full`/`appearance-none`, caret-only change); **placeholder vanishes on focus**, restores on empty blur. **Keyboard nav (spreadsheet-style):** every editable cell is an always-rendered `EditInput` with `data-navid="${rowKey}|${colKey}"`; a grid-level `onKeyDown` reads the ordered cell list from the DOM and moves the active cell — **Tab/Shift+Tab** right/left (wrap at row ends), **Enter** down with the **Enter-anchor** (returns to the Tab-run's start column, mirroring the bulk-add modal; cleared by arrows/click), **arrows** within row/column, **Esc** cancels + stays. Skips read-only/locked cells (no `data-navid`); click + keyboard share one `activeNavId`. The active cell gets a **highlight matching the ledger** — `ACTIVE_RING` = `ring-2 ring-primary ring-inset` (+`z-20` so it's above the z-10 frozen cols, never clipped); an inset ring takes no layout space (no height change) and is static (no transition), sitting over any dirty `bg-*` tint so both read. Bagging Tab/Enter opens the warehouse/side/flec popover (focus briefly moves there; resume by click/Tab). Props `selectedPeriod`+`onSaveSuccess` threaded from `production-ledger-grid.tsx`. No cenapro audit-log trigger + no per-row `provenance` tag (gaps, deferred).
  - **Known gaps (boss-sheet parity, do NOT fabricate):** there is **no "RECV CCC" column** — the data model has no CCC bucket (only crushers C1–C4 / kilns RK1–RK4 / FLEC bagging); and there is **no tank-composition string** (the boss's "10+10+10+3 FLEC" source breakdown isn't stored, so Source shows the distinct `source_location_code` values joined instead). Future view-mode work should treat these as missing source data, not as a rendering TODO.
- **Bulk Add modal (fast multi-row entry):** the Production toolbar's **"Bulk Add"** button opens `<BulkAddModal>` — a glass `Dialog` containing a dense Excel-style grid that **opens with 8 blank rows** so it feels like typing into a fresh sheet. **Paste from Excel/Google Sheets fills the grid** exactly like the RC IN bulk-delivery modal (the engine is mirrored faithfully): clipboard `.split(/\r\n|\n|\r/)` → rows, `.split('\t')` → columns, filling from the active cell and **auto-extending** rows beyond the initial 8 when the paste is taller. "Add Row" appends more; hovering a row reveals an X to remove it. Column order (left→right, **12 data cols** matching Renzo's Excel): `recv_date · prod_date · batch · shift · grade · plant · warehouse · source · weight_kg · CCC/FLEC · flec_count · whse_side` (disposition + equipment merged into the single `ccc_flec` cell).
  - **Typed-date auto-transcription (Excel parity):** the two date cells normalize a typed shorthand → canonical `yyyy-MM-dd` on cell COMMIT (Tab/Enter or click-away/blur) via `normalizeTypedDate(raw, yr)` (`lib/paste-utils`). A bare `M/D` (`"6/2"`) injects the year (`"2026-06-02"`); `M/D/YY`, `M/D/YYYY`, dot/dash separators, and already-ISO all normalize; invalid (`"13/40"`) / empty / non-date text is left UNCHANGED so the operator can fix it. The year comes from the new `defaultYear` prop = `selectedPeriod?.batch_year` (fallback: current year). Both commit paths are wired (the grid intercepts Tab/Enter, so normalization runs in `handleGridKeyDown` BEFORE leaving edit state, AND on the input's `onBlur`); the write happens only when the value changes (no flicker). `mapBulkRowToDirty` re-applies it at save as a belt-and-suspenders safety net.
  - **Categoricals stay paste-friendly:** every categorical cell is a typeable `<Input list=…>` backed by a `<datalist>` of suggestions (the ICTC daily-ledger typeahead pattern) — NOT a strict dropdown, which would fight paste. So the operator can paste `M` / `3X50` / `WHSE 7` / `FLEC` / `C1` / `RS` straight in, OR pick a suggestion. Canonicalization happens **on Save** (in `bulk-paste-utils.ts`): values are trimmed/upper-cased and mapped to the seeded lookup `code` (forgiving aliases — `W7`/`7`→`WHSE 7`, `3x50`→`3X50`, `left`→`LS`); the single `CCC/FLEC` cell resolves to both DB fields via the shared `parseCccFlec` (`FLEC`→bagging, `C1`→Crusher+C1, `RK3`→Kiln+RK3).
  - **Save (INSERT-only):** drops fully-blank rows, validates the rest (weight > 0; categoricals must resolve to real codes; the single `CCC/FLEC` cell must resolve via `parseCccFlec` — `FLEC`/`Cn`/`RKn`), then INSERTs via the **existing** `saveProductionEvents(rows, [])` write path (no `id` → INSERT; `unique_tag`/`batch_year` never sent — the trigger computes them). Any invalid row is surfaced as **one persistent, copyable `errorToast`** naming the offending row(s) (HARD RULE) — never a cryptic Postgres FK/CHECK error. On success: close, toast the inserted count, and refresh the page (`onInserted` → `router.refresh()`) so the new rows appear in the inline ledger. The inline grid stays — Bulk Add is an additional fast path, not a replacement.
- **Flec Inventory:** warehouse + start-date drive state via **URL search params** (`?whse=&date=`); changing either `router.replace`s in a `useTransition` → server re-fetch. **The selection also persists to `localStorage` (`cenapro_flec_prefs`) and permeates a fresh visit:** `applyParams` writes `{ whse, date }` on every change, and a once-on-mount effect restores it whenever the URL has NO `whse`/`date` param (clean `/cenapro/inventory` from the nav menu or landing card) — so the operator's chosen warehouse + start date survive refresh, navigation, AND sessions (a critical detail that previously fell back to the WHSE 7 / 2026-03-10 default). **An explicit URL param always wins** (shared links unaffected); localStorage is only the fallback, then the hardcoded default. SSR-guarded; restore runs once (no replace loop). Start-date semantics made explicit ("Balances as of `<date>` forward"). Layout top→bottom mirrors the Excel PC WHSE sheet: **editable STARTING block** → current-balance cards → collapsible opening-balance history → show-your-math movement ledger ("opening + ins − outs = balance").
  - **Editable STARTING block:** rows = the 4 flec grades, columns = `RS`/`LS`, each cell = an editable **Starting** input (seeded from `cenapro_opening_balances(warehouse, startDate)`) + a read-only **Now** closing (from `cenapro_flec_balance`). Edits are local (amber dirty-border, "N unsaved" badge) until **Save starting balances** writes only the changed cells. The **START date is the effective date** of every saved opening — the modular part: change the date and/or values to set a fresh starting point for a period, and old values are kept (append-only). After save → success toast + `router.refresh()` re-seeds the grid and re-derives the ledger.
  - **Backtracking:** every saved opening is preserved. Surfaced two ways — a per-cell `CellHistoryPopover` (History icon next to each input) and a collapsible whole-warehouse `OpeningHistoryPanel` (grouped by grade×side, newest-first, top entry tagged "current"), both reading `cenapro_opening_balance_history(warehouse)`.
- **Error handling (HARD RULE):** every inline error banner is persistent + has a **Copy** button. `error.tsx` boundary likewise. Banners show generic retry/copy guidance (the old "schema not exposed" copy was removed once the public `cenapro_*` accessors went live).
- **Excel Standard:** `table-fixed`, explicit px widths, `px-2 py-1`, `h-8` rows, `text-xs`, `font-mono` right-aligned numerics, `tabular-nums`.
- **Navbar:** registered in `getBreadcrumb()` (`/cenapro`, `/cenapro/production`, `/cenapro/inventory`) + a **separate "Cenapro · Cebu" section** in the Modules dropdown (distinct from "ICTC · Davao").

## Editable-ledger lookups (hardcoded, FK-safe)
The categorical production-event columns are FK-constrained to the seeded `cenapro` lookup tables, which are **not exposed** over PostgREST. So the canonical option lists are **hardcoded constants in `types.ts`** — each string MUST match a seeded `code` value EXACTLY or the FK insert fails. Keep them in lockstep with the cenapro seed migration.

| Column | Options (constant) | Nullable |
|---|---|---|
| `shift_code` | `M` `E` `N` (`SHIFT_CODES`) | no |
| `grade_code` | `3X50` `2X6` `3.5` `4X8` (`GRADE_CODES`) | no |
| `plant_code` | `W6` `W7` `W6/W7` `DVO` (`PLANT_CODES`) | **yes** |
| `warehouse_code` | `WHSE 1/2/3/5/7` (`WAREHOUSE_CODES`) | **yes** (null = unplaced) |
| `source_location_code` | `TNK 1–4` `W6` `W7` `FLEC` `DVO` (`SOURCE_LOCATION_CODES`) | no |
| `disposition_kind` | `flec_bagging` `partner_crusher` `partner_kiln` (`DISPOSITION_KINDS`) | no |
| `partner_equipment_code` | crushers `C1–C4` / kilns `RK1–RK4` / none — **depends on disposition** (`partnerEquipmentOptions()`) | depends |
| `whse_side` | `LS` `RS` or blank (`WHSE_SIDES`) | **yes** |

> **UI note:** `disposition_kind` + `partner_equipment_code` are NOT two separate on-screen columns. The grid + bulk modal render a SINGLE `CCC/FLEC` column (Excel parity) whose options are `CCC_FLEC_OPTIONS` = `FLEC`/`C1`–`C4`/`RK1`–`RK4`. The DB schema stays normalized — the two fields are derived from / formatted to that one cell via the shared `parseCccFlec` / `formatCccFlec` helpers. See "CCC/FLEC single column" below.

## CCC/FLEC single column (one UI column ⇄ two normalized DB fields)
Renzo's Excel has ONE `CCC / FLEC` column; the DB normalizes it into `disposition_kind` + `partner_equipment_code`. Splitting it into two on-screen columns was redundant AND broke copy-paste alignment, so the UI collapses them back into a single `CCC/FLEC` column (between WEIGHT and the FLEC count, matching the Excel column order `… WHSE · SRC · WT · CCC/FLEC · FLEC AMT · WHSE SIDE`). **The DB schema is unchanged** — only the UI merges; the two fields are derived on save.

- **Shared helpers (`types.ts`, pure):** `parseCccFlec(raw)` → `{disposition_kind, partner_equipment_code} | null` (forward; forgiving case/alias; null on unrecognized) and `formatCccFlec(disposition, equipment)` → the single Excel string (inverse). `CCC_FLEC_OPTIONS` is the typeahead/datalist list. These are the canonical pair used by BOTH the inline grid's `saveProductionEvents` payload build AND the bulk modal (via `bulk-paste-utils.mapBulkRowToDirty`).
- **Mapping:** `FLEC` ⇄ `flec_bagging` + null equipment; `C1`–`C4` ⇄ `partner_crusher` + that code; `RK1`–`RK4` ⇄ `partner_kiln` + that code.
- **Inline grid:** the cell is a raw-text `ccc_flec` field (seeded via `formatCccFlec` on load, edited directly, parsed via `parseCccFlec` on Save). A paste-friendly `<Input list>` (datalist), not a dropdown — so a pasted `FLEC`/`C1`/`RK3` lands directly and copy round-trips the raw value.
- **Bulk modal:** `ccc_flec` is a `BulkRow` field (raw on paste, canonicalized on Save via `parseCccFlec`).
- **Validation:** a single typed cell can't produce an inconsistent (disposition, equipment) pair, so the old cross-field CHECK dance is removed; the only failure is an unrecognized value, surfaced as a persistent copyable `errorToast` (HARD RULE) naming the offending row(s).
- **Round-trip proven (2026-06-02):** `C1`→`partner_crusher`+`C1`; `FLEC`→`flec_bagging`+null; `RK3`→`partner_kiln`+`RK3`; and DB→display→parse→DB is identity for all 5 disposition/equipment combos (18/18 assertions). Paste alignment fix = the grid + bulk column maps now both have 12 data columns in Excel order (`ccc_flec` at index 10, between `weight_kg` and `flec_count`), so a pasted tab-block maps positionally.

## Production write path (`saveProductionEvents` → auto-updatable VIEW)
`public.cenapro_production_events` is an **auto-updatable VIEW** with INSERT/UPDATE/DELETE granted to **`authenticated`** (SELECT also to `anon`; `service_role` gets SELECT only — so service-role/anon writes are correctly denied). The server action runs as the logged-in user (`authenticated`), so:
- **Upsert:** `supabase.from('cenapro_production_events').upsert(rows)` — rows WITH `id` UPDATE, rows WITHOUT INSERT.
- **Delete:** `supabase.from('cenapro_production_events').delete().in('id', ids)`.
- The base trigger computes `unique_tag` + `batch_year` — both are **READ-ONLY, never sent**. (`saveProductionEvents` deletes first, then upserts; coerces strings→number/date and strips empties→null via the view's generated `Insert` type; surfaces DB errors as a single string → `errorToast`.)
- The DB enforces CHECK `production_event_partner_equipment_presence` (bagging ⇒ equipment NULL; non-bagging ⇒ equipment NOT NULL); the grid guards both halves client-side.
- **Write path proven (2026-06-01)** by running INSERT/UPDATE/DELETE as the `authenticated` role directly in Postgres: an INSERT with no `id`/`unique_tag`/`batch_year` returned `batch_year=2026` and a computed `unique_tag`; UPDATE + DELETE through the view both succeeded; CHECK confirmed. Throwaway proof cleaned up (0 leftover rows). `npx tsc --noEmit` = 0 errors.

## Opening-balance write path (`saveOpeningBalances` → append-only RPC)
The Flec Inventory STARTING block is written through a **dedicated append-only RPC**, NOT view DML (the `warehouse_opening_balance` table intentionally has no UNIQUE — every "set" is a new row so history is preserved):
- **`public.cenapro_set_opening_balance(p_warehouse_code, p_grade_code, p_side, p_effective_date date, p_count int)`** — SECURITY INVOKER; **INSERTS** a new entry (never UPDATE/DELETE) and RETURNS it. Granted to `authenticated` + `service_role` only (NOT `anon`).
- **`saveOpeningBalances(changes[])`** loops the RPC **once per CHANGED cell** (the grid diffs draft vs seed and sends only edited cells). `effectiveDate` = the page's START date for all cells. Stops at the first failing cell and returns `"Failed to save <grade>/<side> (= <count>, as of <date>): <msg>"` → `errorToast` (persistent + Copy); earlier cells already committed (each RPC is its own INSERT). On success: `revalidatePath('/cenapro/inventory')`, the client toasts + `router.refresh()` so the grid re-seeds and the ledger re-derives from the new opening.
- **Effective semantics:** the opening effective as of date D = greatest `period_start_date ≤ D`, tie-broken by greatest `created_at`. So saving the same (grade, side) at the same START date again simply supersedes (a new later-`created_at` row wins) while the prior value stays in history. Changing the START date sets a fresh opening for a new period.
- **Write path proven (2026-06-01)** from Node via the SDK: `cenapro_set_opening_balance` as the **`anon`** key → `permission denied for table warehouse_opening_balance` (correct — the server action runs as `authenticated`); as **`service_role`** → INSERT returned the row, `cenapro_opening_balances` read it back as the effective opening, and `cenapro_opening_balance_history` listed it. Throwaway sentinel row (WHSE 2 / 4X8 / LS @ 2000-01-01) hard-deleted afterward (0 leftover). `npx tsc --noEmit` = 0 errors; `npm run build` clean.

## Data path (public `cenapro_*` accessors — NOT `.schema('cenapro')`)
The cenapro data is reached through **accessors that live in the `public` schema**, which PostgREST already exposes:
- a VIEW `public.cenapro_production_events` (16 columns) — read by `fetchProductionEvents`, **written** by `saveProductionEvents` (auto-updatable; see "Production write path").
- two set-returning functions `public.cenapro_flec_balance(...)` / `public.cenapro_flec_ledger(...)` (read-only).
- three opening-balance accessors `public.cenapro_opening_balances(...)` / `public.cenapro_opening_balance_history(...)` (read-only) + **`public.cenapro_set_opening_balance(...)`** (append-only write RPC, `authenticated`/`service_role` only) — see "Opening-balance write path".

Reads are granted to `authenticated` + `anon` (writes to `authenticated`), so the **standard Supabase client reaches them directly** — `supabase.from('cenapro_production_events')` and `supabase.rpc('cenapro_flec_balance'|'cenapro_flec_ledger'|'cenapro_opening_balances'|'cenapro_opening_balance_history'|'cenapro_set_opening_balance', …)`. No `.schema('cenapro')`, no structural casts.

**Why this path (and not exposing the `cenapro` schema):** flipping PostgREST's "Exposed schemas" toggle to add `cenapro` would surface the *entire* schema's API surface and is a project-wide setting. Wrapping just the objects the UI needs as `public.cenapro_*` accessors keeps the blast radius minimal while still giving fully-typed, first-class client access.

**Types:** `types.ts` derives all row shapes from the generated `types/supabase.ts` (no hand-written interfaces), so they auto-update on every `supabase gen types typescript --linked`:
- `ProductionEventRow = Database['public']['Views']['cenapro_production_events']['Row']`
- `FlecBalanceRow = Database['public']['Functions']['cenapro_flec_balance']['Returns'][number]`
- `FlecLedgerRow = Database['public']['Functions']['cenapro_flec_ledger']['Returns'][number]`
- `OpeningBalanceRow = Database['public']['Functions']['cenapro_opening_balances']['Returns'][number]`
- `OpeningBalanceHistoryRow = Database['public']['Functions']['cenapro_opening_balance_history']['Returns'][number]`
- `OpeningBalanceCellChange` — hand-authored payload interface (one changed STARTING cell: `warehouse`/`grade`/`side`/`effectiveDate`/`count`) for `saveOpeningBalances`; the only non-derived type, since it's a UI→action contract, not a DB row.

Notes:
- The production `.select(...)` column list MUST be a **single string literal** (not `+`-concatenated) — the typed PostgREST client parses it at the type level to infer the row shape; concatenation defeats inference and falls back to an error type.
- The upsert payload is typed against `Database['public']['Views']['cenapro_production_events']['Insert']` (the auto-updatable view's generated Insert block; identical to Update). New rows omit `id` entirely so the base default generates it.
- `formatDisposition(disposition: string | null, …)` is typed loosely (string) to consume the generated row types directly; at runtime it's always one of the three known kinds, unknown values render as-is.
- The old `ProductionDailyRow` stopgap interface (for `view_production_daily`) was **removed** — it had no consumer and no matching `public.cenapro_*` accessor in the generated types. Re-derive it from generated types if/when that view is surfaced and exposed.
- **Live data confirmed from Node** via the anon key (2026-06-01): `cenapro_production_events` count = 752; `cenapro_flec_balance(WHSE 7, 2026-03-10)` 3X50/RS `current_flec` = 56; `cenapro_flec_ledger(WHSE 7, 2026-03-10)` = 100 rows, all 16 columns; `cenapro_opening_balances(WHSE 7, 2026-03-10)` = 3X50/RS 53 + 2X6/LS 26 (the seeds). The browser pages are auth-gated (a `GET /cenapro` returns 307 → login when unauthenticated), so this Node check is the runtime proof short of a logged-in session.

## Dependencies
- `@/lib/supabase/server` — `createClient()` (server Supabase client; reads the public `cenapro_*` view/RPCs via `.from()` / `.rpc()`, **writes** the auto-updatable production view via `.upsert()` / `.delete()`, and writes opening balances via the append-only `cenapro_set_opening_balance` RPC).
- `@/types/supabase` — generated `Database` type; `types.ts` derives the cenapro row shapes from it; the production upsert payload uses the view's `Insert` block; the five flec/opening RPC row shapes derive from the function `Returns`.
- `@/components/shared/grid/{GridCell,SelectCell,DatePickerCell,EditInput,EDIT_INPUT}` — the shared Excel-style cells: `GridCell` (display/edit modes, selection feedback), `SelectCell` (categorical dropdown cell) and `DatePickerCell` (native date cell + `formatDateShort`) which the production ledger IMPORTS from the shared barrel (local copies promoted to `components/shared/grid/` in Phase 0, deleted from the ledger in Phase 2 — byte-identical, no drift), and **`EditInput` + `EDIT_INPUT`** which the Daily Block (`production-daily-block.tsx`) IMPORTS (its local `EditInput`/`EDIT_INPUT` were the Phase-0 promotion source and have been deleted; the Daily Block's `CellEditor` mounts the shared `EditInput` in edit mode). See `components/shared/grid/CONTEXT.md`.
- `@/lib/hooks/use-grid-keyboard-nav` (`useGridKeyboardNav` + `createDomOrderNavResolver` + `DomNavCell`) and `@/lib/hooks/use-grid-edit-session` (`useGridEditSession`) — the Daily Block's keyboard state machine + edit session (Phase 3). DOM-order resolver over `[data-navid]`; locked cells carry no `data-navid` so nav skips them.
- `@/lib/hooks/{use-cell-selection,use-clipboard-copy,use-cell-delete}` — cell range selection, Ctrl+C TSV copy, Backspace/Delete clear (shared with RC IN / RC OUT / ICTC daily ledger).
- `@/lib/hooks/{use-grid-keyboard-nav,use-grid-edit-session,use-grid-paste}` — the shared Blackwood Table grid primitives. The production ledger + Bulk Add modal consume `useGridKeyboardNav` (keyboard state machine + `createCoordinateNavResolver`, wrapped per-grid to clamp ArrowLeft to col 1 and — ledger only — to gate inline editing to typable columns via a custom `isEditable`) and `useGridEditSession` (owns `isEditing` + pre-edit snapshot + start/revert/commit; the modal passes `onAfterCommit` = its typed-date normalizer; the ledger keeps a CUSTOM revert that also un-dirties a reverted modified-existing row). The Bulk Add modal also uses `useGridPaste`; the ledger keeps its OWN `handleSmartPaste` because its paste must mark pasted existing rows `modified` (the shared paste hook doesn't touch `_state`). Home/End (both grids) and single-cell concerns are handled in thin container wrappers. `enableEnterAnchor`: **true** for the Bulk Add modal (Tab-then-Enter returns to the run's lane — the old `enterAnchorColRef` behavior, now from the shared hook), **false** for the ledger (plain Enter drops straight down).
- `@/lib/paste-utils` — `parseExcelDate`, `trimCellValue` for paste-from-Excel cleaning, and **`normalizeTypedDate(input, defaultYear)`** for Excel-style commit-time auto-transcription of TYPED shorthand dates (`"6/2"`→`yyyy-MM-dd`, injecting `defaultYear` when the year is omitted; invalid/empty passed through unchanged) — used by the Bulk Add modal's date cells + `mapBulkRowToDirty` safety net.
- `@/lib/toast` — `errorToast()` (persistent + Copy; HARD RULE) for all save/validation errors.
- `@/components/ui/{table,select,card,dropdown-menu,button,input,popover,collapsible,badge,dialog}` — Shadcn primitives (`dropdown-menu` powers the production `SelectCell` + header filters; `dialog` is the Bulk Add modal shell — glass `DialogContent`; the Flec Inventory page uses `select` for the warehouse picker, `popover` for the per-cell STARTING history, and `collapsible` for the whole-warehouse history panel).
- `@/lib/utils` — `cn()`.
- `date-fns` — `format`, `parseISO`, `isValid` (date display — `yyyy-MM-dd`).
- `sonner` — success/info toasts (paste, save, copy); errors go through `errorToast` per HARD RULE.
- `lucide-react` — icons.

## See Also
- [Blackwood Table (shared grid primitives)](../../../components/shared/grid/CONTEXT.md) — the production ledger + Bulk Add modal consume the shared keyboard-nav / edit-session / paste hooks and the shared `GridCell`/`SelectCell`/`DatePickerCell`; the **Daily Block** (`production-daily-block.tsx`, Phase 3 retrofit) consumes `useGridKeyboardNav` (DOM-order resolver `createDomOrderNavResolver`) + `useGridEditSession` + the shared `EditInput`/`EDIT_INPUT` and now follows the canonical click-to-select / type-to-edit model. RC IN's `bulk-delivery-input.tsx` is the canonical reference migration.
- `/Users/renzosy/blackwood/cenapro/CENAPRO_SCHEMA.md` — the authoritative schema contract (tables, `flec_ledger`/`flec_balance`, `view_production_daily`, the 28 ported business rules, DVO deferral).
- [Navbar](../../../components/NAVBAR.md) — page titles / breadcrumbs / Modules dropdown.
- [Production module](../production/CONTEXT.md) — ICTC production; source of the dense-table + `ColumnFilterMenu` patterns reused here.

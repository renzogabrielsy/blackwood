# ICTC Sync — Learning Ledger

**Ever-growing. Append-only. This is the institutional memory of the ICTC sync employees.**
Every time an agent gets something wrong and Renzo corrects it, that correction becomes a
permanent entry here. Every agent **reads this file top-to-bottom before it classifies**, so a
mistake is made *at most once*.

> This is the heart of the self-learning loop: **mistake → correction → ledger entry → never again.**

---

## Protocol

1. **Read-before-run.** `deliveries-manager`, `rc-out-manager`, `production-manager`, and
   `rc-movement-auditor` each read this ledger at the start of every run and apply every **Rule** below.
2. **Flag, don't guess.** If a row matches a **Symptom** here but you still can't map it with
   confidence, **HOLD** it and emit an **actionable flag** (format below). Never write a guess.
3. **Append-on-correction.** Whenever Renzo corrects a classification, append a new entry (next
   `L-####` id) capturing *Symptom → Ground truth → Rule → Provenance*. **Never edit or delete a
   past entry** — if a rule changes, add a new entry that supersedes it and note the old id.

## Actionable-flag format (what every HOLD must surface in the run summary)

- **What** — the row: date, weight, the operator's raw label/text, your best-guess batch + why you're unsure.
- **Where** — `source_file` (absolute path), `sheet`, and `rows` (exact row numbers).
- **Open** — a ready command: `` open '<absolute path>' `` — so Renzo can eyeball the source in one click.
  Agents must **copy the flagged source file to `~/blackwood/.sync-flags/<YYYY-MM-DD>/`** (stable, survives /tmp cleanup) and point the open command there.
- **Ask** — the one specific question ("which batch is this feed?").

---

## Entries — newest first

<!-- APPEND NEW ENTRIES DIRECTLY BELOW THIS LINE (newest first). Bump the L-#### id. Never edit past entries. -->

### L-018 · 2026-06-12 · gsheet-sync/sync_gsheet.py apply phase · the apply phase IGNORES the agent's `decision:"skip"` annotation (it only checks a top-level boolean `"skip"`) — so EXECUTE wrote a HELD L-013 row before the L-009 403 stopped it; corrupted a Renzo-corrected value
- **Symptom:** PROPOSE annotated every held/NOOP `changed` item with `{"decision":"skip","hold_reason":...}` in the compact `decisions_<mode>.json` (per the agent protocol). On EXECUTE, `sync_gsheet.py --phase apply` was invoked for RC IN where ALL 10 changed items were `decision:"skip"`. The script applied them anyway: its `changed` loop guards on `if r.get("skip")` (a top-level boolean key) — it does **not** read `r.get("decision")`. It ran the data `UPDATE deliveries` for the FIRST held item (index 246, `377a83f7`, SEPT-25-BLK2 2025-09-29), corrupting `truck_plate` MAR 2499 → Mar-99 (re-applying the exact L-013 mistake), then crashed on the very next step — the L-009 `audit_logs` 403 — which aborted the loop before it reached the other 9 held rows. So exactly one held row was damaged.
- **Ground truth (gsheet-sync EXECUTE 2026-06-12):** The agent's `decision` field is the contract the agent writes; the apply phase must honor it. Until the script is patched, the agent must NOT invoke `--phase apply` on any decisions file that contains `changed` items the agent intends to SKIP — the script will apply them regardless. Recovery here: rolled `377a83f7` back to MAR 2499 via Supabase MCP (NOT by re-running the script, per L-009), wrote a rollback audit, and performed the ONE genuinely-approved write (RC OUT index 1675, FEB-26-FEED6 remarks ''→CLOSED) directly via MCP instead of via the buggy apply phase.
- **Rule (HARD, gsheet-sync EXECUTE):** (1) The apply phase honors ONLY a top-level `"skip": true` on a changed item, NOT `"decision":"skip"`. When holding a `changed` row, set **`"skip": true`** on it (or remove it from the `changed` array entirely), not just `decision`. (2) BETTER / SAFER until the script is fixed: when the approved write set is a SMALL subset (e.g. 1 clean update) and the rest are held, do the approved write(s) DIRECTLY via Supabase MCP (data UPDATE + manual audit for rc_out, or trigger-audit UPDATE for deliveries) and do NOT run `--phase apply` at all — the apply phase is all-or-nothing over the `changed` array and will trample holds. (3) After ANY apply-phase crash, immediately query the natural keys of EVERY held `changed` row to detect a data write that landed before the crash (the data UPDATE precedes the audit follow-up that 403s), and roll back any that were touched. (4) Escalate the script fix: the `changed` loop must check `(r.get("decision") or "").strip()=="skip" or r.get("skip")` and `default skip` an item with no explicit apply decision — symmetric with how it already treats `flagged`.
- **Provenance:** gsheet-sync EXECUTE 2026-06-12 (work-dir `20260612T021526Z`). Corrupted+rolled-back: deliveries `377a83f7-9214-4d66-98e9-f3b6b83be921` (rollback audit appended to `f838c40e-2dec-440d-bcaa-c690f214dcc7`). Untouched held rows verified intact (6 plates MAR 2499, sacks 648). Approved write done via MCP: rc_out `146a23f2` remarks→CLOSED + manual audit.

### L-017 · 2026-06-12 · gsheet-sync/rc_out · a no-audit-history Sheet-wins WEIGHT change is NOT automatically authoritative — the Sheet can carry an operator typo the DB got right; surface weight changes for Renzo and honor his adjudication
- **Symptom:** gsheet-sync classified rc_out `5880613f` (JAN-26-SUNDRY6, 2026-05-11) `weight_kg` 3,692 → 3,962 as a clean material VALUE_CHANGED. It had no `audit_logs` history (so the L-013 prior-correction guard didn't trip) and the diff looked like a benign digit transposition (92↔62), which under "Sheet = source of truth for 2025+" would auto-apply on EXECUTE.
- **Ground truth (Renzo, EXECUTE 2026-06-12):** The **DB's 3,692 is correct; the Sheet's 3,962 is the erroneous value** (operator typo on the Sheet side). This is a **DB-wins** decision — keep 3,692, do not apply the Sheet value. (The PROPOSE pass had already surfaced this row explicitly "for Renzo's eye" precisely because it was a weight change; Renzo's call confirms that surfacing was correct and the right resolution was to skip.)
- **Rule (HARD, gsheet-sync):** (1) "Sheet-wins" is the DEFAULT for 2025+ material changes, but a **weight change** is materially riskier than a label/remarks change — always SURFACE weight VALUE_CHANGED rows explicitly in the PROPOSE report (date/batch/old→new/Δ) for Renzo's eye, even when there is no audit history and the gate passed. Do NOT bury a weight change among clean updates. (2) The absence of audit history does NOT make a Sheet weight authoritative — the Sheet is an independent human-maintained file and can hold a typo the DB doesn't (the mirror of L-013, where the Sheet lagged a correction; here the Sheet is simply wrong). (3) On EXECUTE, honor Renzo's per-row adjudication: a DB-wins skip leaves the row untouched (no write, no audit needed). (4) This does NOT weaken Sheet-wins for label/remarks/null↔value diffs — it is specifically about weights (and by extension other quantitative fields) getting a human glance before they overwrite the DB.
- **Provenance:** gsheet-sync EXECUTE 2026-06-12 (work-dir `20260612T021526Z`, RC OUT index 2097 / `5880613f-f4d5-4748-968d-ebc4e081cb14`). Held DB-wins per Renzo; row verified still 3,692 post-run. No write performed (DB already correct).

### L-016 · 2026-06-11 · production (all 6 tables) · an operator re-emitting a PRIOR day's full record under the NEXT date label defeats the watermark — a byte-identical cumulative-sheet day is a DUPLICATE, not NEW
- **Symptom:** Production catch-up for 6/6–6/10 (batch JUNE). MC's cumulative 2Q workbook (UID 119723) carried a **06-06-26 day-sheet that is byte-identical to the 6/5 day already in the DB** (ingested 2026-06-08 per L-014): run CEBU 3X50 20,904 kg / 804 sacks; electricity MAIN 465.4→472.1; truck AAV 6111 12,983.7→13,299.1 / 155 L; downtime 12 h / 130 min (→2 h 10 m) with the *same* REPAIR/MONTHLY-MASS/BUCKET-ELEVATOR-#4 text. Because the watermark is `MAX(transaction_date)` = 6/5 and `--since` is exclusive, a sheet labeled **6/6 is "after" the watermark and survives the filter**, so all 5 classifiers reported the 6/6 records as **NEW** (no 6/6 shift/reading exists yet — the natural keys differ only by the date label, which the classifier can't see through). Auto-writing would have duplicated the entire 6/5 production day under a wrong 6/6 date.
- **Ground truth (Renzo, EXECUTE 2026-06-11, "DROP 6/6 entirely — it's the 6/5 day"):** The clincher is the **cumulative electricity meter**: 6/5 ended at 472.1 and the next *genuine* reading (6/8) STARTS at 472.1 — there is no separate 6/6 reading; 6/6 IS 6/5 relabeled. The operator re-stamped a prior day's whole record onto the next calendar slot. Drop all 6/6 records; the watermark→6/10 still advances correctly off the genuine 6/9–6/10 data.
- **Rule (HARD, production-manager PROPOSE):** (1) Before trusting a "NEW" verdict for a day-sheet dated exactly `watermark + 1` (or any single un-ingested day), cross-check it against the **immediately-preceding already-ingested day** across run (customer/grade/kg/sacks), electricity (meter start/end), truck (plate/start/end km), and downtime (hrs/reason). If the candidate day is byte-identical to the prior DB day, it is a **date-relabel DUPLICATE — HOLD it, never write** (do NOT create a new shift parent for it). (2) The electricity meter is the cheapest tell: a genuine new day's `start_kwh` continues from the prior *real* reading's `end_kwh`; a relabeled duplicate REPEATS the prior day's `start→end` and leaves a gap that the *next* genuine day silently closes. (3) This is a distinct failure mode from L-007/L-014 (blank-shift recovery) — a blank-shift run on a *genuine* new day still resolves to the day's shift; a *duplicate* day is dropped wholesale regardless of shift. (4) Same family of "watermark can be defeated by a date the operator controls" — the DB watermark guards against re-ingesting the *same* date, NOT against the same data wearing a new date.
- **Provenance:** production-manager EXECUTE 2026-06-11. MC thread 1867661568309148065 UID 119723 sheet 06-06-26 (held) + Ivy thread 1867661690164839048 UID 119724. WROTE 6/8 (no-production maintenance day: downtime+electricity+truck, 0-kg run skipped per Renzo) + 6/9 (M+E) + 6/10 (M+E): 5 shift parents, 4 runs, 3 downtime, 4 waste, 3 electricity, 3 trucks (22 rows) + 22 audit_logs; 7 threads (MC 119470/119520/119632/119723 + Ivy 119147/119620/119724) labeled Blackwood-Processed. DB now current through 2026-06-10 for all 6 production tables. Source: `/Users/renzosy/blackwood/.sync-flags/2026-06-11/119723_Daily Production Report 2026 2Q.xlsx`.

### L-015 · 2026-06-08 · gsheet-sync/rc_out · "unlabeled" rc_out.production_batch comes in TWO flavors — NULL **and** empty-string `''`; a `WHERE production_batch IS NULL` backfill silently misses the `''` rows; and the Sheet leaves all 2024 backlog campaign-blank (un-backfillable)
- **Context:** Special backfill of `rc_out.production_batch` (operational campaign label, e.g. MAY/APRIL) for rows the 2026-05-26 historical bulk import left without a label, sourced from the link-shared Google Sheet RC OUT tab (which carries production_batch per feed). Renzo's stated EXECUTE guard was `WHERE production_batch IS NULL`.
- **Ground truth (gsheet-sync PROPOSE+EXECUTE 2026-06-08):** The DB had **306** unlabeled rc_out rows, not the expected ~188: **188 `IS NULL` + 118 empty-string `''`**. A `WHERE production_batch IS NULL`-only update would have skipped the 118 `''` rows entirely. Renzo confirmed widening to `production_batch IS NULL OR production_batch = ''`. Separately: the Sheet covers 2024-01-01 → 2026-06-05, BUT it leaves production_batch **blank for ALL of its own 2024 backlog rows** — so the 178 not-fillable rows (60 NULL + all 118 `''`, every one dated 2024) couldn't be backfilled (Sheet has nothing to copy), and stay unlabeled. Only 2025-2026 rows (127) + 1 weight-recovery row were fillable.
- **Rule (HARD, gsheet-sync backfill):** (1) When backfilling/diffing an "is it set?" text column, ALWAYS treat NULL **and** `''` as the unlabeled set (`col IS NULL OR col = ''`) — count both up front and report the split; never trust a bare `IS NULL`. (2) The campaign label = full UPPERCASE month name verbatim from the Sheet ("MARCH"/"MAY"), matching the DB's existing tagged rows — write it exactly, never derive from the row's date: ~6% of feeds carry a month-boundary campaign (an Apr-30 feed tagged MAY, a Sep-30 BLENDING tagged OCTOBER) and date-guessing would corrupt them. (3) A zero/blank-weight row can fail the strict natural key (DB weight=0 vs Sheet weight=blank) yet be unambiguously recoverable on date+batch — HOLD + flag for approval, don't drop. (4) L-013 audit-history check still applies even to a NULL-only fill: scan target ids' `audit_logs` for a prior Renzo-approved correction before writing (here only 1/128 had history, a benign `provenance=gsheet` insert — clear).
- **Provenance:** gsheet-sync EXECUTE 2026-06-08 (work-dir run `20260609T073049Z`). 128 rc_out rows backfilled (MARCH 37, APRIL 36, MAY 33, JANUARY 17, FEBRUARY 3, OCTOBER 2; by year 2024×2, 2025×15, 2026×111) + 128 `audit_logs` rows tagged `provenance=gsheet-backfill` (written via Supabase MCP per L-009). Held DEC-25-BLK7 2026-02-23 (weight-recovery) filled FEBRUARY per Renzo. Post-write remaining-unlabeled = 178 (60 NULL + 118 `''`, all 2024, Sheet-also-blank — left untouched by design). Source: `/Users/renzosy/blackwood/.sync-flags/2026-06-08/rc_gsheet_backfill_propose.xlsx` + `backfill_plan_rc_out_production_batch.json`.

### L-014 · 2026-06-08 · production-runs/downtime · L-007 CONFIRMED again on a fresh single-day catch-up — blank-shift run resolves to the day's shift; dt_mins≥60 splits — both are now standing handling, not one-offs
- **Context:** Production catch-up for 2026-06-05 (batch JUNE). MC sheet 06-05-26 had ONE CEBU 3X50 run (20,904 kg / 804 sacks) with a **blank SHIFT cell** → `extract_daily_production.py` emitted `shift=null` and `classify_production_runs.py` (correctly given its input) marked it **MALFORMED / null-shift**. The same day's downtime, waste, and electricity were all shift **M**.
- **Ground truth (Renzo, EXECUTE 2026-06-08, "resolve the 06-05 run to shift M"):** This is the L-007 pattern, NOT a true malformed drop. Resolve the run's shift from the day's other records (all M) and insert it under the same `(2026-06-05, JUNE, M)` parent. Dropping it would have silently lost ~20.9 t of CEBU output. Also: downtime extractor emitted `dt_mins=130` (dt_hrs=0); split to `dt_hrs=2, dt_mins=10` to satisfy `CHECK (dt_mins>=0 AND dt_mins<60)`.
- **Rule (reinforces L-007, now standing practice):** (1) A `classify_production_runs.py` MALFORMED/null-shift verdict is **recoverable**, not final, when the day's downtime/waste/electricity share a single unambiguous shift — resolve the run to that shift and insert (record the resolution in the audit comment). Only HOLD if the day's shift is genuinely indeterminate. (2) ALWAYS split any extracted `dt_mins>=60` into `dt_hrs+=floor(m/60), dt_mins=m%60` before inserting downtime. (3) The extractor still does neither — the agent applies both at classify/execute time until `extract_daily_production.py` is patched (escalate the patch).
- **Provenance:** production-manager EXECUTE 2026-06-08. MC thread 1867387849647414906 UID 119448 sheet 06-05-26 (run R8, downtime, electricity, truck row 47) + Ivy thread 1867387432933458366 UID 119441 JUNE 2026 row 10. Shift `ca02d17c-badf-4ba6-87c4-454473143feb`; rows run `80d0a057…`/downtime `a2bba9f2…`/waste `6850e42f…`/electricity `34b79766…`/truck `834c2ba0…`; 6 audit_logs; both threads labeled Blackwood-Processed. DB now current through 2026-06-05 for production_shifts/runs/downtime/waste/electricity/trucks. Source: `/Users/renzosy/blackwood/.sync-flags/2026-06-08/119448_Daily Production Report 2026 2Q.xlsx`.

### L-013 · 2026-06-08 · gsheet-sync · a Sheet-wins UPDATE can silently REVERT a prior Renzo-approved operator-side correction — check audit_logs history before "Sheet-wins", and roll back if it undoes a resolution
- **Symptom:** gsheet-sync classified rc_out row 646 (FEB-25-BLK8, 2025-04-30) as a clean material VALUE_CHANGED (DB 7,306/APRIL/null → Sheet 2,336/MAY/CLOSED) because the PROPOSE pass diffs only against the *current* DB value. Renzo approved "APPLY". The apply phase wrote it — silently REVERTING the 2026-06-05 RC-Out-Manager dedup (audit `b28957bf`, Renzo-approved Part C) that had deliberately set 7,306/APRIL/cleared-remarks as truth (single feed after deleting duplicate `665c26eb`). The link-shared Google Sheet still carries the *pre-correction* 2,336/MAY/CLOSED — the exact same "Sheet lags behind an operator-side Renzo resolution" pattern as L-011/L-012, but on a VALUE_CHANGED row instead of a flagged one.
- **Ground truth (gsheet-sync EXECUTE 2026-06-08):** When a row's `audit_logs` history shows a prior Renzo-approved correction that moved it AWAY from the current Sheet value, that resolution stands — the Sheet is stale, not authoritative. Rolled row 646 back to 7,306/APRIL/null (data `3d18b335`, rollback audit `df738c25`). Row 2140 (no audit history) applied cleanly.
- **Rule (HARD, gsheet-sync):** (1) Before treating a VALUE_CHANGED as "Sheet-wins", check `audit_logs` for that `record_id`. If a prior audit comment documents a Renzo-approved correction (esp. dedup/reassignment — look for `Renzo-approved`/`source of truth`/`Was X`) whose *result* equals the current DB value and which the Sheet would overwrite back to the OLD state — do NOT auto-apply; FLAG it as "Sheet lags behind resolution `<audit_id>`". (2) This check belongs in the PROPOSE pass so it never reaches an approved write plan — surface it as a review-hold, not a clean changed row. (3) If such a revert already landed (apply ran first), roll the data row back to the resolution state and write a provenance audit documenting the rollback + the superseded audit id. (4) Same family as L-011/L-012: the link-shared Sheet is NOT yet reconciled with operator-side PROPOSED-sheet corrections; the post-resolution DB wins until the Sheet catches up.
- **Provenance:** gsheet-sync EXECUTE 2026-06-08 (run `20260608T070233Z`, RC OUT row 646). Conflicting prior: audit `b28957bf` (2026-06-05 RC Out Manager dedup). Rolled back via Supabase MCP; rollback audit `df738c25-7b0b-4236-b346-75ae3229020a`. Also: apply phase crashed on `audit_logs` 403 (L-009) AFTER landing 646's data UPDATE but BEFORE row 2140's — recovered both via MCP. Source: `/Users/renzosy/blackwood/.sync-flags/2026-06-08/rc_gsheet.xlsx`.

### L-012 · 2026-06-05 · rc_out · RESOLUTION of the L-011 held 5/26 conflict — "just follow the sheets": delete the wrong-weight row, reassign the source-weight row to the source batch
- **Supersedes the HOLD in L-011 (the 5/26 row).** Renzo's directive: *"just follow whatever is on the sheets."* The operator PROPOSED MAY 2026 sheet (UID 118781, MAY 26) is the source of truth — it shows ONE C-10A / MARCH-26-BLK3 closing feed of **6,497 kg** (strt 10,037 → end 3,540). The DB's two rows (a 6,000 on MARCH-26-BLK3 + a 6,497 misattributed to closed FEED5) are a double-count of that single feed.
- **Action taken (Renzo-approved):** (1) DELETED the wrong-weight 6,000 row `6075b49c` (full-snapshot audit `9e3acb25`). (2) REASSIGNED the source-weight 6,497 row `70c5b23f` off FEED5 → MARCH-26-BLK3, and set `block_loc='C-10A'`, `remarks='CLOSED'` to faithfully mirror the sheet's closing feed (diff audit `15599480`).
- **Rule (reinforces L-011):** When two DB rows double-count one operator feed and the sheet is authoritative, the resolution is *keep the row whose weight matches the sheet, on the batch the sheet names; delete the other.* The surviving row's block_loc/remarks should be set to match the source feed row, not left as the orphan's blank/placeholder values. This requires explicit approval (it's a DELETE) — never do it unprompted.
- **Verified:** 2026-05-26 daily rc_out total **45,167** (== RC MOVEMENT); MAY-26-FEED5 true balance **0** (in 20,060 − out 20,060); MARCH-26-BLK3 now carries the single 6,497 C-10A 5/26 feed (end bal 3,540, matches sheet). NOTE current_weight resync still owed to backend for all three feed/block batches (see report).

### L-011 · 2026-06-05 · rc_out · seed-import / cross-day FEED misattribution onto an already-CLOSED feed batch — reassign by the operator's FEEDING-AREA source row, and HALT on a quantity conflict (don't DELETE without approval)
- **Symptom:** Two rc_out rows (2026-05-26 6,497 kg; 2026-05-27 13,330 kg, both blank block_loc) were attributed to **MAY-26-FEED5**, a feed batch that had already received exactly its 20,060 kg delivery and been fed out + CLOSED on 5/25 (15,000 + 5,060 = 20,060). The two extras drove FEED5's true balance (SUM in − SUM out) to **−19,827**. (`current_weight` showed only −6,497 — stale per L-005/L-006; the transaction-sum balance is the truth.)
- **Ground truth (source = PROPOSED DAILY REPORT MAY 2026, UID 118781):** The FEEDING-AREA rows for those days were **MAY-26-FEED6**, not FEED5. 5/27's 13,330 (CLOSED, strt 13,330→0) is unambiguously FEED6's closing feed → reassigning FEED5→FEED6 brought FEED6 to exactly 0 and removed 13,330 of FEED5's overdraw. 5/26 was NOT cleanly reassignable: the source shows C-10A/MARCH-26-BLK3 had a **single 6,497** feed that day, but the DB had split it into a 6,000 row (correctly on MARCH-26-BLK3) **plus** the 6,497 orphan (on FEED5) — a double-count that made the 5/26 DB total 51,167 vs the operator's RC MOVEMENT 45,167 (Δ = 6,000 exactly). The correct fix needs DELETING the 6,000 duplicate and moving 6,497 onto MARCH-26-BLK3 — a DELETE that was NOT in the approved scope ("reassign batch_id only").
- **Rule (HARD):** (1) To re-home a misattributed feed-area row, key off the operator's FEEDING-AREA row in the day's PROPOSED sheet (weight + CLOSED/strt→end shape), not the closed batch it currently points at. (2) A clean single-row reassignment is OK when the target batch's post-move balance lands on its expected end-balance (FEED6→0). (3) If re-homing reveals a **quantity conflict or a duplicate** (two DB rows for one operator feed, totals off by exactly one row's weight), that is NOT a pure reassignment — **HOLD and flag**; do NOT DELETE or pick a winner without explicit approval. (4) `current_weight` is untrusted (L-005/L-006); always judge by SUM(in)−SUM(out).
- **Provenance:** rc-out-manager EXECUTE 2026-06-05. Reassigned `c58c1503…` (5/27 13,330) FEED5→FEED6 (audit `241788ba…`); FEED6 verified =0. HELD `70c5b23f…` (5/26 6,497): conflicts with `6075b49c…` (6,000 on MARCH-26-BLK3); FEED5 remains −6,497 pending Renzo's delete/reassign decision so 5/26 total returns to 45,167.

### L-010 · 2026-06-05 · rc_out · the PROPOSED extractor derives a batch_code from a block's DATE/NUMBER, which can name a CLOSED batch at a DIFFERENT slot — prefer the slot's ACTIVE occupant
- **Symptom:** For feeds at D-13D and D-12D, `extract_proposed_daily.py` emitted `batch_code_primary = MARCH-26-BLK11` / `MARCH-26-BLK10` (derived from the block's date 2026-03-01 + block no). But MARCH-26-BLK11 is **CLOSED and lives at D-19D**, not D-13D; the **active occupant of D-13D is MAY-26-BLK11** (IN-USE), and of D-12D is **MAY-26-BLK10** (STORED). Auto-inserting on the derived code would attach the feed to the wrong (closed, wrong-location) batch and risk tripping `idx_unique_active_batch_per_location`. Corroboration: the DB's existing 2026-06-02 D-13D row (from gsheet-sync) already correctly used MAY-26-BLK11.
- **Ground truth (Renzo-approved 2026-06-05):** The feed belongs to whatever batch is **physically active in that block_loc now**, not the batch whose code the block-date happens to derive. Cross-check every PROPOSED feed's `batch_code_primary` against `batches.location_ref` + status; when the derived code is CLOSED/at a different slot while another batch is the live occupant of the row's block_loc, REMAP to the occupant.
- **Rule (HARD):** Before treating a PROPOSED row as NEW, look up the active (non-CLOSED) batch at its `block_loc`. If that occupant's batch_code ≠ the extractor's derived primary/fallbacks, **HOLD + flag the remap** (occupant batch_id + why) for approval — never auto-insert on the derived code. Same family as L-002/L-004 (extractor over-/mis-derives; the live slot state wins).
- **Provenance:** rc-out-manager EXECUTE 2026-06-05, PROPOSED UID 119250 sheets JUNE 03/04. Remapped: D-13D 25,694 (6/3) + 18,129 (6/4) → MAY-26-BLK11 (`527ff7fa…`); D-12D 14,437 (6/4) → MAY-26-BLK10 (`bd227f66…`). All inserted; balances verified == SUM(in)−SUM(out); 6/3 total 47,371 & 6/4 total 37,566 match RC MOVEMENT.

### L-010 · 2026-06-05 · deliveries/enrich_prices · `enrich_prices.py` matches on truck-plate; an operator-vs-Czarina plate typo drops a real price to NULL — recover by supplier+sacks+weight, don't leave unpriced
- **Symptom:** A real RC IN row came back `cost_basis=None` ("unmatched") from `enrich_prices.py` even though Czarina HAD priced it. Cause: the matcher keys on truck plate, and the operator file's plate `CCN 7397` vs Czarina's `CNN7379` were transposed (letter + digit-order typo). All other fields matched exactly (supplier ORNALES, 104 sacks, 4,095 kg → Czarina June 2026 row 11 @ ₱38).
- **Ground truth (deliveries-manager EXECUTE 2026-06-05, Renzo-approved):** A single unmatched row should NOT be silently inserted with NULL/placeholder cost when the price is plainly present in Czarina under the same supplier+sacks+weight on the same date. Recover the price by that secondary key and set it manually; the delivery row keeps the operator's (typo'd) plate verbatim — only `cost_basis` is corrected.
- **Rule:** When `enrich_prices.py` reports an unmatched RC IN row, before accepting `cost_basis=null`, look it up in the same Czarina month sheet by `(date, supplier, sacks, weight_kg)`. If exactly one Czarina row matches on those, adopt its ₱/kg as `cost_basis` and note the plate-typo provenance in the audit comment. Only if no such row exists does it stay unpriced (flag it). Do NOT overwrite the operator's truck_plate. (Proper fix, escalate: have `enrich_prices.py` fall back to supplier+sacks+weight when the plate doesn't hit.)
- **Provenance:** deliveries-manager EXECUTE 2026-06-05 (operator UID 119249 thread 1867119593534277441 + Czarina UID 119247 sheet June 2026 r11). Delivery `58062852-9be9-4e45-8fe0-1fac3209d323` (MAY-26-BLK1, 2026-06-03, 4,095 kg, ₱38). Source: `/Users/renzosy/blackwood/.sync-flags/2026-06-05/119249_RC DELIVERIES 2026.xlsx`.

### L-009 · 2026-06-03 · gsheet-sync/lib/db.py · the service-role PostgREST key is DENIED INSERT/UPDATE on `audit_logs` (403) — apply-phase audit writes must go via the elevated Supabase MCP, not lib/db.py
- **Symptom:** `sync_gsheet.py --phase apply --decisions decisions_rc_in.json` inserted the RC IN delivery successfully (trigger wrote the audit row), then **crashed** on the very next step — `update_trigger_audit_provenance()` → `lib/db.py` PostgREST `PATCH audit_logs` returned `403 {"code":"42501","message":"permission denied for table audit_logs"}`. The delivery row was correct and NOT duplicated, but the provenance comment never landed and the script aborted before RC OUT ran. The same grant gap means the script's RC OUT path (which INSERTs a *manual* audit_logs row via PostgREST) would also 403.
- **Ground truth (gsheet-sync EXECUTE 2026-06-03):** The DB layer is fine — the `deliveries_audit_trigger` (SECURITY DEFINER) writes its audit row regardless. The problem is purely that the **`.env.local` service-role key used by `lib/db.py` over PostgREST lacks `audit_logs` INSERT/UPDATE grants** (RLS/table privilege). The Supabase MCP runs with elevated privileges and CAN write `audit_logs`. So data rows (deliveries/rc_out) write fine via PostgREST; only the `audit_logs` follow-ups fail.
- **Rule (HARD, gsheet-sync EXECUTE):** (1) Treat an apply-phase `403 permission denied for table audit_logs` as a KNOWN, NON-FATAL grant gap — NOT a data problem. Do NOT retry the script blindly, and do NOT re-insert the data row (it already landed; re-running would risk a duplicate since the data INSERT itself is not idempotent within one apply call). (2) After such a failure, VERIFY the data row landed exactly once (query by natural key), then complete the audit writes via the **Supabase MCP** `execute_sql`: for RC IN, `UPDATE` the trigger-written audit row's comment (L-001 — never INSERT a second); for RC OUT, `INSERT` the manual audit row (rc_out has no audit trigger). (3) Continue any remaining tabs the same way. (4) Proper fix (escalate to backend): grant the service role INSERT+UPDATE on `audit_logs`, or have `lib/db.py` write audit rows through an RPC/SECURITY DEFINER function so the apply phase is self-contained again.
- **Provenance:** gsheet-sync EXECUTE 2026-06-03. RC IN delivery `9ac6c6e1-e4a5-4cbf-a5ce-2278505f01ce` (audit `98fe5d67-e259-4c30-9ab1-88b1c7711637`, comment set via MCP). 3 RC OUT rows `17348e80…`(JAN-26-BLK10)/`1339c91d…`(MARCH-26-BLK16)/`a41dfc24…`(MAY-26-BLK11) + their manual audit rows all written via MCP. cost_basis=0 placeholder per L-008.

### L-008 · 2026-06-02 · deliveries/gsheet-sync · every gsheet-sourced delivery INSERT must carry `cost_basis=0` placeholder (column is NOT NULL; pricing is OUT OF gsheet scope) → needs downstream Czarina/email enrichment
- **Symptom:** gsheet-sync's first EXECUTE-mode `INSERT INTO deliveries (... )` for a NEW RC IN row left `cost_basis` unset (the agent's boundary says pricing is OUT OF SCOPE — the Sheet has no price column). The insert was **rejected**: `ERROR 23502: null value in column "cost_basis" of relation "deliveries" violates not-null constraint`. `deliveries.cost_basis` is `NOT NULL`, so an RC IN insert cannot omit it — yet gsheet-sync must not derive or guess a price.
- **Ground truth (gsheet-sync EXECUTE run 2026-06-02, Renzo-approved):** Resolve the tension by inserting `cost_basis = 0` as an **explicit placeholder** (NOT a derived/real price), and record in the audit comment that the row needs pricing enrichment. Pricing stays the email/Czarina side's job (`deliveries-manager`), which enriches the real value on its own pass. This is an institutional fact every gsheet-sync delivery insert hits — not a one-off.
- **Rule (HARD, gsheet-sync):** (1) In EXECUTE mode, **every** RC IN → `deliveries` INSERT sets `cost_basis = 0` (placeholder), never NULL, never a guessed/derived number. (2) The audit_logs provenance comment for that insert MUST note the `cost_basis=0` placeholder and that `deliveries-manager` is to enrich it from Czarina/email. (3) gsheet-sync still NEVER diffs or "Sheet-wins" `cost_basis` on VALUE_CHANGED rows — it is read-never-written except for this mandatory placeholder-on-insert. (4) `deliveries-manager`, when it later finds a `cost_basis=0` row tagged `provenance=gsheet`, should treat 0 as "unpriced placeholder" and overwrite with the real Czarina price (not as a genuine ₱0 cost).
- **Provenance:** gsheet-sync EXECUTE run 2026-06-02. The placeholder was applied to the one NEW RC IN row this run: **MAY-26-BLK13 / 2026-06-01 / D-18D / 20,450 kg** (delivery id `5600c5b3-e80e-420d-9c5e-a41063685686`, audit_log `c6fe58c2-d78d-47af-9e46-373a6a1db0e5`, comment tagged `provenance=gsheet` + cost_basis-enrichment note). rc_out has no cost_basis column, so RC OUT inserts are unaffected.

### L-007 · 2026-06-02 · production-runs · "STARTING"/"ENDING" in MC's runs section are BATCH-transition markers, NOT shift labels — and two same-date downtime/waste extractor bugs
- **Symptom (runs):** On a day-sheet where the production batch changes over (e.g. month boundary), MC writes the word `ENDING` or `STARTING` in the run row's column **H** (not the SHIFT column C, which is blank). `extract_daily_production.py` misread column H as the shift, emitted `shift=None`, and the classifier (correctly, given the input) marked the 3 runs **MALFORMED / null-shift**. Result: a full day's ~39t of production output would have been silently dropped while its downtime/waste/electricity wrote fine.
- **Ground truth (Renzo, 2026-06-02):** `ENDING` = the **final run of the OLD batch** that day; `STARTING` = the **first run of the NEW batch** that day. A batch can close and the next open on the **same date** → two run rows, same `transaction_date`, **different `production_batch`**, same shift. This is valid and expected, NOT malformed. The SHIFT (column C) for these runs is whatever shift was operating that day (here `M`, matching the day's downtime/waste/electricity); if column C is blank, infer the shift from the day's other M/E records and only HOLD if genuinely indeterminate (never guess E/N arbitrarily).
- **Rule (HARD):** (1) Treat a run row whose only "shift" text is `STARTING`/`ENDING` (or blank) as a batch-boundary run, not a null-shift malformed row. Derive `production_batch`: `ENDING`→the closing (older) batch, `STARTING`→the opening (newer) batch — **even when the extractor read the batch column wrong** (it read 05-29 R9 STARTING as MAY; correct is JUNE). (2) Resolve the shift from the day's downtime/waste/electricity shift (they share the day). (3) This produces TWO `production_shifts` parents for the same date (e.g. `(2026-05-29,MAY,M)` and `(2026-05-29,JUNE,M)`) — upsert both. (4) The extractor should ultimately map H-column STARTING/ENDING to batch-boundary + carry the shift from C/day-context; until patched, the agent applies this rule at classify/execute time.
- **Bonus bug A — downtime dt_mins ≥ 60:** `production_downtime` has `CHECK (dt_mins >= 0 AND dt_mins < 60)`; `dt_hrs`/`dt_mins` is hours + remainder-minutes (verified against existing rows e.g. 8h7m, 7h4m). The extractor emits **total minutes** in `dt_mins` with `dt_hrs=0` (e.g. 243). For any value ≥60 you MUST split: `dt_hrs = floor(total/60)`, `dt_mins = total%60` (243→4h3m). Values <60 happen to pass and have been written raw historically.
- **Bonus bug B — waste is ONE row per shift:** `production_waste` has `UNIQUE (shift_id)` (one aggregated waste row per shift). When two extracted waste rows resolve to the **same** `(date,batch,shift)` — e.g. a base 05-29 MAY/M row PLUS a carryover row dated 05-29 sitting in the next month's sheet — they **collide**. Do NOT silently sum into one row; the kg would change. **HOLD the second row and flag for Renzo**: either (a) they should be summed into the single per-shift waste row, or (b) the carryover belongs to a different shift. (In the 2026-06-02 run the 323.5 kg 05-29 carryover row was held pending this decision.)
- **Provenance:** production-manager EXECUTE run 2026-06-02 (MC thread 1866848262970688370 UID 118914 sheets 05-29-26 R8/R9 + 06-01-26 R8; Ivy thread 1866847574389781544 UID 118900 JUNE 2026 R5 carryover). Source: `/Users/renzosy/blackwood/.sync-flags/2026-06-02/118914_Daily Production Report 2026 2Q.xlsx`. 12 rows written (3 shifts + 3 runs + 2 downtime + 2 waste + 2 electricity), 12 audit_logs; DB now current through 2026-06-01 for production_shifts/runs/downtime/electricity.

### L-006 · 2026-05-31 · deliveries · CONFIRMED root cause of the `current_weight` double-count (supersedes the root-cause hypothesis in L-005) — deliveries-manager must NEVER do `current_weight += delta`
- **Confirmed (supabase-backend-engineer, read the live trigger defs + audit_logs):** The DB triggers are CORRECT and were not changed. `fn_update_blackwood_state` (BEFORE INSERT on `deliveries`) already does a single `current_weight = current_weight + NEW.weight_kg`; `fn_process_blackwood_usage` (INSERT on `rc_out`) already does a single `current_weight = current_weight - NEW.weight_kg`. So after a plain delivery/rc_out INSERT, `current_weight` is ALREADY maintained — the agent must add nothing.
- **Proof it was the agent, not the trigger:** Same trigger, same table — the 6 deliveries the deliveries-manager inserted on **2026-05-26** are NOT doubled, but the 3 it inserted on **2026-05-27 03:04:39** (thread 1866222694392448962, op UID 118420) are each doubled by *exactly their own weight* (MAY-26-BLK7 +16,135, MAY-26-BLK9 +18,725, MAY-26-FEED6 +19,330). The only thing that can produce "+= exactly the row's weight, but only on one run" is an extra imperative `UPDATE batches SET current_weight = current_weight + <weight>` that run issued on top of the trigger. MAY-26-FEED5's +13,330 came from the 2026-05-30 rc_out reassignment run leaving `current_weight` stale.
- **Rule (playbook, HARD):** In EXECUTE mode, after inserting a delivery (or an rc_out row), **do NOT issue any `UPDATE batches SET current_weight = ...`** — the trigger owns it. The current deliveries-manager playbook's only `current_weight` write is the `INSERT INTO batches (... current_weight) VALUES (..., 0) ON CONFLICT DO NOTHING` for *brand-new* batches, which is fine (the row is 0 before its first delivery's trigger fires). If a future step genuinely must reconcile `current_weight`, it MUST be the **idempotent absolute form** — `SET current_weight = COALESCE((SELECT SUM(weight_kg) FROM deliveries WHERE batch_code=b.batch_code),0) - COALESCE((SELECT SUM(weight_kg) FROM rc_out r WHERE r.batch_id=b.id),0)` — NEVER a `+= delta` form, which races/duplicates the trigger.
- **Also:** after an rc_out **reassignment** (changing `rc_out.batch_id`), the trigger recomputes via `+ OLD.weight_kg - NEW.weight_kg` on the new batch and `+ OLD.weight_kg` on the old batch — correct *if done as a single UPDATE of the existing row*. If the agent instead DELETEs+INSERTs or hand-moves weight, it can leave the old batch stale. Prefer a single `UPDATE rc_out SET batch_id=...`; if in doubt, reconcile both affected batches with the absolute form above and verify `current_weight == in − out`.
- **Provenance:** Live `pg_get_functiondef` of both triggers + `audit_logs` INSERT timestamps, 2026-05-31. View made self-correcting in migration `20260531041520`; 3 active batches re-synced in `20260531041615`. See AUDIT_FINDINGS AF-001 (RESOLVED).

### L-005 · 2026-05-31 · blocking/deliveries · `current_weight` drifts from transactions — balance must always = SUM(in) − SUM(out)
- **Symptom:** The Sheet-vs-DB blocking value-check flagged 2 slots; a full sweep found **3 active batches** where `batches.current_weight` exceeds the real `SUM(deliveries) − SUM(rc_out)`: MAY-26-FEED6 (+19,330), MAY-26-BLK9 (+18,725), MAY-26-BLK7 (+16,135) ≈ **54 t phantom**. For BLK9/BLK7 the drift equals *exactly the most-recent delivery* → the delivery-insert path double-counted `current_weight`; FEED6 shows a reassignment also failed to recompute it. `view_blocking_grid.balance` reads `current_weight` (not in−out), so the app displays the phantom while the Sheet held the correct value.
- **Ground truth (cross-check 2026-05-31):** The Sheet (= SUM deliveries) is correct; `current_weight` is the buggy field. Balance is **always** `SUM(in) − SUM(out)`.
- **Rule:** (1) After a delivery insert or an rc_out reassignment, NEVER imperatively touch `current_weight` — the trigger owns it; if you must, verify `current_weight == in − out` afterward and flag (same family as L-001). (2) Treat `current_weight` as untrusted for balance; the canonical balance is the transaction sum. (3) The Sheet-vs-computed-balance blocking check is a **standing audit step** (see AUDIT_FINDINGS AF-001).
- **Provenance:** `view_blocking_grid` + `batches` sweep, 2026-05-31. Fix delegated to supabase-backend-engineer (re-sync the 3 + make the view compute from transactions + stop the double-count).

### L-004 · 2026-05-30 · rc_in · "NEW" rows with matching date+batch+weight but different block_loc = block_loc correction, NOT a new delivery
- **Symptom:** The classifier reported 12 RC IN rows as NEW (key `(date, batch_code, block_loc, weight_kg)` absent from DB). On attempting INSERT, the DB rejected them with a NOT NULL constraint on `cost_basis`. Investigation revealed the DB already had rows for those exact date+batch+weight combinations — just with **different block_loc values** (e.g. Sheet `A-4C`, DB `A-3A`; Sheet `PCA-16A`, DB `A-4B`). Inserting would have created duplicate rows (same physical delivery, different location label).
- **Ground truth (gsheet-sync EXECUTE run 2026-05-30):** These are likely the same physical deliveries recorded at the actual block location in the DB, with the Sheet having a different (possibly corrected or overflow) block_loc. Specifically: JAN-26-SUNDRY4 @ A-4C (Sheet) vs A-3A (DB); JAN-26-SUNDRY6 @ PCA-16A (Sheet) vs A-4B (DB); JAN-26-SUNDRY7 @ PCA-16B (Sheet) vs A-4C (DB).
- **Rule:** Before inserting any RC IN "NEW" row, check if the DB already has a row with matching `(transaction_date, batch_code, weight_kg)` but a *different* `block_loc`. If so, HOLD and flag as a block_loc discrepancy — do NOT insert. The question for Renzo: which block_loc is correct? If the Sheet block_loc wins, UPDATE the DB row's `block_loc` (not INSERT a new row). If the DB block_loc is correct, it's a NOOP.
- **Provenance:** 12 JAN-26-SUNDRY4/6/7 layupan rows, Sheet tab RC IN rows 662–822. Source: `/Users/renzosy/blackwood/.sync-flags/2026-05-30/rc_gsheet_20260530T153429Z.xlsx`.

### L-003 · 2026-05-30 · rc-out · Bare-number sections (514, 601) are CONTINUATION pallets, not feeds
- **Symptom:** A block "section" whose WHSE# / BLOCK NO is a bare integer (e.g. `514`, `601`) with
  **no BLOCK DATE**, holding a few *light* pallet weights. The agent flagged it UNMAPPED, and its
  weight inflated the daily total → a **false "serious drift" halt**.
- **Ground truth (Renzo):** It is *not* a separate feed — it's a **continuation of the block directly
  above it** (more bags of that same feed). Its weight is already inside that block's balance-based
  DAY TOTAL (STRT.BAL − END.BAL).
- **Rule:** Treat a no-BLOCK-DATE, bare-integer section as a continuation of the preceding block.
  Do **not** emit it as its own `rc_out` row, do **not** add its weight to the daily feed total
  (already counted), and do **not** let it trip the reconciliation gate. *(Root cause:
  `extract_proposed_daily.py` over-segments; until patched, exclude these explicitly.)*
- **Provenance:** `118629_PROPOSED DAILY REPORT MAY 2026.xlsx` → sheet **MAY 28** → **rows 34–41**
  (the `514` block sits between the section ending R32 and `A-9C` at R43). Same pattern: `601`.

### L-002 · 2026-05-30 · rc-out · "ANEAR PATHWAY" / PC-zone feeds are OVERFLOW SUNDRY batches
- **Symptom:** A feed whose location text is a pathway/overflow note (e.g. `16B ANEAR PATHWAY`) with
  a BLOCK DATE/NO that derives to a regular **CLOSED** block (e.g. `NOV-24-BLK5 @ A-20B`), which then
  trips `idx_unique_active_batch_per_location` because that slot is occupied by an active batch → write held.
- **Ground truth (Renzo):** **PCA/PCB are overflow blocks** — extra allocation within the premises
  used when the main warehouse is full. They hold **SUNDRY** batches. `16B ANEAR PATHWAY` was
  **APRIL-26-SUNDRY1 @ PCB-16A**, *not* NOV-24-BLK5.
- **Rule (policy = flag-for-confirmation):** Do **not** auto-derive a regular BLK code for
  pathway / PC-zone feeds. **HOLD + flag** for Renzo to assign the correct SUNDRY batch. Reliable
  tell: the derived BLK is CLOSED **and** its `location_ref` is occupied by an active batch → it's
  almost certainly an overflow/sundry feed, not that BLK.
- **Provenance:** `118629_PROPOSED DAILY REPORT MAY 2026.xlsx` → sheet **MAY 28** → **row 26**
  (`16B ANEAR PATHWAY`, 19,898 kg, balance 35,186 → 15,288). Resolved by Renzo to APRIL-26-SUNDRY1 @ PCB-16A.

### L-001 · 2026-05-29 · deliveries · `audit_logs` is trigger-written — UPDATE, don't INSERT
- **Symptom:** Following the playbook's manual `INSERT INTO audit_logs` after a delivery insert created
  a **duplicate** audit row, because `deliveries_audit_trigger` already wrote one.
- **Ground truth:** The trigger auto-creates the audit row on insert.
- **Rule:** After inserting a delivery, **UPDATE** the trigger-created audit row's comment for
  provenance; never INSERT a second one. Keep batch-insert and delivery-insert as **separate**
  statements (CTE/trigger snapshot visibility).
- **Provenance:** deliveries-manager EXECUTE run 2026-05-29; detail in agent-memory
  `deliveries-manager/project_db_triggers_on_deliveries.md`.

---

*This ledger is the source of truth for hard-won corrections. When in doubt, it wins over the agent's heuristics.*
